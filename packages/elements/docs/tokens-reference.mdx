import { Meta } from '@storybook/addon-docs/blocks';

<Meta title="Docs/Tokens reference" />

{/* GENERATED FILE — do not edit by hand.
    Source: the DTCG files under packages/tokens/tokens/.
    Generator: packages/tokens/scripts/generate-reference.mjs.
    Regenerate after any token source change:
      pnpm --filter @kimen/tokens run reference */}

# Tokens reference

> **Generated reference — do not edit by hand.** This page is emitted by
> `packages/tokens/scripts/generate-reference.mjs` from the DTCG sources in
> `packages/tokens/tokens/`. Regenerate it after changing token sources with
> `pnpm --filter @kimen/tokens run reference` and commit the result. It is
> generated manually and is not verified by a CI gate.

Every custom property in the public styling contract: 304 semantic
and 1064 component tokens. Values are the resolved **onmars light**
values; dark mode and the material3 theme reassign the same custom properties,
so the names and descriptions on this page apply to every theme (see
*Docs → Tokens & theming* for the model). Primitive tokens are internal and
deliberately not listed: components never consume them, and reassigning the
two layers below is the whole re-theming surface.

## Semantic tokens

Meaning, not appearance. This is the layer a brand reassigns to re-theme
every component at once.

### `ki.corner`

| Token | Value (onmars light) | Description |
| --- | --- | --- |
| `--ki-corner-control` | `8px` | Deprecated alias kept for pre-Fase-1 consumers; use ki.radius.md. |

### `ki.effect`

| Token | Value (onmars light) | Description |
| --- | --- | --- |
| `--ki-effect-component-backdrop-blur` | `12px` | MarsUI Blur/24 as the component glass backdrop. Components consume this semantic name for backdrop-filter: blur(...) — never the ki.blur primitive directly (primitive-css-consumption gate). |
| `--ki-effect-component-primary-default-shadow` | `0 1px 1px -0.5px rgba(0, 0, 0, 0.03), inset 0px 3px 3px 0px rgba(255, 255, 255, 0.12)` | MarsUI Component_effect/primary_default: drop 0/1/1/-0.5 plus inner White/12 0/3/3. Pair with backdrop-filter blur(var(--ki-effect-component-backdrop-blur)) — CSS cannot mix backdrop blur into box-shadow. |
| `--ki-effect-component-primary-hover-shadow` | `0 1px 1px -0.5px rgba(0, 0, 0, 0.03), 0 3px 3px -1.5px rgba(0, 0, 0, 0.03), 0 20px 20px -12px rgba(0, 0, 0, 0.03), inset 0px 3px 3px 0px rgba(255, 255, 255, 0.18)` | MarsUI Component_effect/primary_hover: drops 0/20/20/-12 + 0/3/3/-1.5 + 0/1/1/-0.5 plus inner White/18 0/3/3. Pair with backdrop-filter blur(var(--ki-effect-component-backdrop-blur)). |
| `--ki-effect-component-secondary-default-shadow` | `0 2px 1.5px -0.5px rgba(0, 0, 0, 0.03), inset 0px 2px 3px 0px rgba(255, 255, 255, 0.03)` | MarsUI Component_effect/secondary_default: drop 0/2/1.5/-0.5 plus inner White/3 0/2/3. Pair with backdrop-filter blur(var(--ki-effect-component-backdrop-blur)). |
| `--ki-effect-component-secondary-hover-shadow` | `0 1px 1px -0.5px rgba(0, 0, 0, 0.03), 0 3px 3px -1.5px rgba(0, 0, 0, 0.03), 0 20px 20px -12px rgba(0, 0, 0, 0.03), inset 0px 2px 3px 0px rgba(255, 255, 255, 0.08)` | MarsUI Component_effect/secondary_hover: drops as primary_hover plus inner White/8 0/2/3. Pair with backdrop-filter blur(var(--ki-effect-component-backdrop-blur)). |
| `--ki-effect-small-dark-shadow` | `0 2px 3px -1.5px rgba(0, 0, 0, 0.32)` | MarsUI Small_dark_shadow: drop 0/2/3/-1.5 in Elevation/shadow_dark (Black/32 light, Black/48 dark). Lifts small light glyphs off saturated fills — the radio selected dot and the switch pointer carry it in the masters. |

### `ki.elevation`

| Token | Value (onmars light) | Description |
| --- | --- | --- |
| `--ki-elevation-e1` | `0 1px 1px -0.5px rgba(0, 0, 0, 0.03)` | MarsUI Elevation/e1. Scheme-aware: every layer colors through ki.elevation.shadow. |
| `--ki-elevation-e2` | `0 1px 1px -0.5px rgba(0, 0, 0, 0.03), 0 3px 3px -1.5px rgba(0, 0, 0, 0.03)` | MarsUI Elevation/e2 (cumulative: e1 layers plus 0/3/3/-1.5). |
| `--ki-elevation-e3` | `0 1px 1px -0.5px rgba(0, 0, 0, 0.03), 0 3px 3px -1.5px rgba(0, 0, 0, 0.03), 0 20px 20px -12px rgba(0, 0, 0, 0.03)` | MarsUI Elevation/e3 (cumulative: e2 layers plus 0/20/20/-12). |
| `--ki-elevation-e4` | `0 1px 1px -0.5px rgba(0, 0, 0, 0.03), 0 3px 3px -1.5px rgba(0, 0, 0, 0.03), 0 20px 20px -12px rgba(0, 0, 0, 0.03), 0 32px 32px -16px rgba(0, 0, 0, 0.03)` | MarsUI Elevation/e4 (cumulative: e3 layers plus 0/32/32/-16). |
| `--ki-elevation-e5` | `0 1px 1px -0.5px rgba(0, 0, 0, 0.03), 0 3px 3px -1.5px rgba(0, 0, 0, 0.03), 0 20px 20px -12px rgba(0, 0, 0, 0.03), 0 32px 32px -16px rgba(0, 0, 0, 0.03), 0 56px 56px -28px rgba(0, 0, 0, 0.03)` | MarsUI Elevation/e5 (cumulative: e4 layers plus 0/56/56/-28). |
| `--ki-elevation-e6` | `0 1px 1px -0.5px rgba(0, 0, 0, 0.03), 0 3px 3px -1.5px rgba(0, 0, 0, 0.03), 0 20px 20px -12px rgba(0, 0, 0, 0.03), 0 32px 32px -16px rgba(0, 0, 0, 0.03), 0 56px 56px -28px rgba(0, 0, 0, 0.03), 0 72px 72px -36px rgba(0, 0, 0, 0.03)` | MarsUI Elevation/e6 (cumulative: e5 layers plus 0/72/72/-36). |
| `--ki-elevation-shadow` | `rgba(0, 0, 0, 0.03)` | semantic color for elevation shadow. |
| `--ki-elevation-shadow-dark` | `rgba(0, 0, 0, 0.32)` | semantic color for elevation shadow dark. |
| `--ki-elevation-shadow-med` | `rgba(0, 0, 0, 0.05)` | semantic color for elevation shadow med. |

### `ki.focus`

| Token | Value (onmars light) | Description |
| --- | --- | --- |
| `--ki-focus-danger` | `0 0 0 3px rgba(240, 81, 73, 0.2)` | MarsUI Focus/danger: persistent 3px spread ring of Outline/danger_base_em_alpha (danger-500 at 20% light / 40% dark) — the danger state signal on Input_field and Dropdown masters (measured rgb(252,220,219) over white). Pairs with a white fill and hairline border; Kimen keeps its opaque danger border on top as a declared a11y deviation. |
| `--ki-focus-gray` | `0 0 0 3px rgba(0, 0, 0, 0.08)` | MarsUI Focus/gray: 3px spread focus ring of Outline/med_em (scheme-aware via the outline token). |
| `--ki-focus-primary` | `0 0 0 3px rgba(132, 90, 190, 0.2)` | MarsUI Focus/primary: 3px spread focus ring of Outline/primary_base_em_alpha, read from the Figma master as #845abe33 = brand 500 at 20% (Kimen name: outline.primary-base-em-alpha / brand.500-alpha-2). The previous 40% (outline.primary-low-em-alpha / brand.500-alpha-4) came from specs/002-ki-button/design-extraction.md §2.5, which read the alpha off the Dark canvas mode and recorded it as the Light value; the ring shipped 2x too strong in light and 2.8x in dark across twelve components. |

### `ki.inverse-black`

| Token | Value (onmars light) | Description |
| --- | --- | --- |
| `--ki-inverse-black-alpha-12` | `rgba(255, 255, 255, 0.12)` | semantic color for inverse black alpha 12. |
| `--ki-inverse-black-alpha-18` | `rgba(255, 255, 255, 0.18)` | semantic color for inverse black alpha 18. |
| `--ki-inverse-black-alpha-24` | `rgba(255, 255, 255, 0.24)` | semantic color for inverse black alpha 24. |
| `--ki-inverse-black-alpha-3` | `rgba(255, 255, 255, 0.03)` | semantic color for inverse black alpha 3. |
| `--ki-inverse-black-alpha-32` | `rgba(255, 255, 255, 0.32)` | semantic color for inverse black alpha 32. |
| `--ki-inverse-black-alpha-40` | `rgba(255, 255, 255, 0.4)` | semantic color for inverse black alpha 40. |
| `--ki-inverse-black-alpha-48` | `rgba(255, 255, 255, 0.48)` | semantic color for inverse black alpha 48. |
| `--ki-inverse-black-alpha-56` | `rgba(255, 255, 255, 0.56)` | semantic color for inverse black alpha 56. |
| `--ki-inverse-black-alpha-6` | `rgba(255, 255, 255, 0.05)` | semantic color for inverse black alpha 6. |
| `--ki-inverse-black-alpha-64` | `rgba(255, 255, 255, 0.64)` | semantic color for inverse black alpha 64. |
| `--ki-inverse-black-alpha-72` | `rgba(255, 255, 255, 0.72)` | semantic color for inverse black alpha 72. |
| `--ki-inverse-black-alpha-80` | `rgba(255, 255, 255, 0.8)` | semantic color for inverse black alpha 80. |
| `--ki-inverse-black-alpha-88` | `rgba(255, 255, 255, 0.88)` | semantic color for inverse black alpha 88. |
| `--ki-inverse-black-alpha-9` | `rgba(255, 255, 255, 0.08)` | semantic color for inverse black alpha 9. |
| `--ki-inverse-black-alpha-solid` | `#ffffff` | semantic color for inverse black alpha solid. |

### `ki.inverse-white`

| Token | Value (onmars light) | Description |
| --- | --- | --- |
| `--ki-inverse-white-alpha-12` | `rgba(0, 0, 0, 0.12)` | semantic color for inverse white alpha 12. |
| `--ki-inverse-white-alpha-18` | `rgba(0, 0, 0, 0.18)` | semantic color for inverse white alpha 18. |
| `--ki-inverse-white-alpha-24` | `rgba(0, 0, 0, 0.24)` | semantic color for inverse white alpha 24. |
| `--ki-inverse-white-alpha-3` | `rgba(0, 0, 0, 0.03)` | semantic color for inverse white alpha 3. |
| `--ki-inverse-white-alpha-32` | `rgba(0, 0, 0, 0.32)` | semantic color for inverse white alpha 32. |
| `--ki-inverse-white-alpha-40` | `rgba(0, 0, 0, 0.4)` | semantic color for inverse white alpha 40. |
| `--ki-inverse-white-alpha-48` | `rgba(0, 0, 0, 0.48)` | semantic color for inverse white alpha 48. |
| `--ki-inverse-white-alpha-56` | `rgba(0, 0, 0, 0.56)` | semantic color for inverse white alpha 56. |
| `--ki-inverse-white-alpha-6` | `rgba(0, 0, 0, 0.05)` | semantic color for inverse white alpha 6. |
| `--ki-inverse-white-alpha-64` | `rgba(0, 0, 0, 0.64)` | semantic color for inverse white alpha 64. |
| `--ki-inverse-white-alpha-72` | `rgba(0, 0, 0, 0.72)` | semantic color for inverse white alpha 72. |
| `--ki-inverse-white-alpha-80` | `rgba(0, 0, 0, 0.8)` | semantic color for inverse white alpha 80. |
| `--ki-inverse-white-alpha-88` | `rgba(0, 0, 0, 0.88)` | semantic color for inverse white alpha 88. |
| `--ki-inverse-white-alpha-9` | `rgba(0, 0, 0, 0.08)` | semantic color for inverse white alpha 9. |
| `--ki-inverse-white-alpha-solid` | `#000000` | semantic color for inverse white alpha solid. |

### `ki.motion`

| Token | Value (onmars light) | Description |
| --- | --- | --- |
| `--ki-motion-distance-md` | `0.5rem` | Motion travel distance md (8px): entrance slide offsets (e.g. dialog rise). |
| `--ki-motion-distance-sm` | `0.25rem` | Motion travel distance sm (4px): small slide/settle offsets. |
| `--ki-motion-distance-xs` | `0.125rem` | Motion travel distance xs (2px): hover-lift offsets. |
| `--ki-motion-duration-fast` | `120ms` | Fast interaction transition duration. |
| `--ki-motion-duration-instant` | `0ms` | Zero-duration step: transitions that must complete immediately (reduced-motion fallbacks, instant handoffs). |
| `--ki-motion-duration-long-1` | `360ms` | M3 md.sys.motion.duration.long1 (450ms). onmars: MarsUI slow step (360ms, 3x the 120ms base). |
| `--ki-motion-duration-long-2` | `360ms` | M3 md.sys.motion.duration.long2 (500ms). onmars: MarsUI slow step (360ms). |
| `--ki-motion-duration-long-3` | `360ms` | M3 md.sys.motion.duration.long3 (550ms). onmars: MarsUI slow step (360ms). |
| `--ki-motion-duration-long-4` | `360ms` | M3 md.sys.motion.duration.long4 (600ms). onmars: MarsUI slow step (360ms). |
| `--ki-motion-duration-medium-1` | `240ms` | M3 md.sys.motion.duration.medium1 (250ms). onmars: MarsUI moderate step (240ms, 2x the 120ms base). |
| `--ki-motion-duration-medium-2` | `240ms` | M3 md.sys.motion.duration.medium2 (300ms). onmars: MarsUI moderate step (240ms). |
| `--ki-motion-duration-medium-3` | `240ms` | M3 md.sys.motion.duration.medium3 (350ms). onmars: MarsUI moderate step (240ms). |
| `--ki-motion-duration-medium-4` | `240ms` | M3 md.sys.motion.duration.medium4 (400ms). onmars: MarsUI moderate step (240ms). |
| `--ki-motion-duration-short-1` | `120ms` | Shortest step of the duration scale. M3 md.sys.motion.duration.short1 (50ms). onmars: the MarsUI single-speed language (120ms), short steps collapse onto ki.duration.fast. |
| `--ki-motion-duration-short-2` | `120ms` | M3 md.sys.motion.duration.short2 (100ms). onmars: MarsUI 120ms (ki.duration.fast). |
| `--ki-motion-duration-short-3` | `120ms` | M3 md.sys.motion.duration.short3 (150ms). onmars: MarsUI 120ms (ki.duration.fast). |
| `--ki-motion-duration-short-4` | `120ms` | M3 md.sys.motion.duration.short4 (200ms). onmars: MarsUI 120ms (ki.duration.fast). |
| `--ki-motion-easing-emphasized` | `cubic-bezier(0.25, 0.1, 0.25, 1)` | Expressive hero-transition curve. M3 md.sys.motion.easing.emphasized. onmars: the MarsUI standard curve (single-curve language). |
| `--ki-motion-easing-emphasized-accelerate` | `cubic-bezier(0.42, 0, 1, 1)` | Expressive exit curve. M3 md.sys.motion.easing.emphasized-accelerate. onmars: the MarsUI accelerate curve (ki.easing.in). |
| `--ki-motion-easing-emphasized-decelerate` | `cubic-bezier(0, 0, 0.58, 1)` | Expressive entrance curve. M3 md.sys.motion.easing.emphasized-decelerate. onmars: the MarsUI decelerate curve (ki.easing.out). |
| `--ki-motion-easing-out` | `cubic-bezier(0, 0, 0.58, 1)` | Decelerating interaction transition easing curve. |
| `--ki-motion-easing-spring` | `cubic-bezier(0.34, 1.56, 0.64, 1)` | Gentle overshoot curve for glass pops and playful state transfers. onmars: ki.easing.spring; no MarsUI Figma motion variable exists (documented completion of the motion language). |
| `--ki-motion-easing-standard` | `cubic-bezier(0.25, 0.1, 0.25, 1)` | Standard interaction transition easing curve. |
| `--ki-motion-easing-standard-accelerate` | `cubic-bezier(0.42, 0, 1, 1)` | Utility exit curve. M3 md.sys.motion.easing.standard-accelerate. onmars: the MarsUI accelerate curve (ki.easing.in). |
| `--ki-motion-easing-standard-decelerate` | `cubic-bezier(0, 0, 0.58, 1)` | Utility entrance curve. M3 md.sys.motion.easing.standard-decelerate. onmars: the MarsUI decelerate curve (ki.easing.out). |

### `ki.outline`

| Token | Value (onmars light) | Description |
| --- | --- | --- |
| `--ki-outline-base-em` | `rgba(0, 0, 0, 0.03)` | semantic color for outline base em. |
| `--ki-outline-black` | `#000000` | semantic color for outline black. |
| `--ki-outline-control` | `rgba(0, 0, 0, 0.48)` | The boundary of a control that can be EMPTY — field, checkbox, radio, switch. Such a control carries no label inside its own box, so the box IS what identifies it and must clear WCAG 1.4.11's 3:1 against every surface it can sit on (s0-s2). Pinned to an accessibility floor, NOT to a position in the emphasis ramp: rebalancing outline.base/low/med/high must never silently erase a control's edge. MarsUI draws Outline/Black-8 here, but as a bevel over a filled cell on a fixed canvas — transcribed literally it measured 1.19:1 against a fill identical to the page (audit 2026-07-25). Declared deviation. |
| `--ki-outline-control-hover` | `rgba(0, 0, 0, 0.56)` | Hover of outline-control. One step stronger, so hover stays a measurable delta on fields whose fill does not change on hover. |
| `--ki-outline-danger-base-em` | `#ffebed` | semantic color for outline danger base em. |
| `--ki-outline-danger-base-em-alpha` | `rgba(240, 81, 73, 0.2)` | semantic color for outline danger base em alpha. |
| `--ki-outline-danger-high-em` | `#f05149` | semantic color for outline danger high em. |
| `--ki-outline-danger-low-em` | `#ffccd0` | semantic color for outline danger low em. |
| `--ki-outline-danger-low-em-alpha` | `rgba(240, 81, 73, 0.4)` | semantic color for outline danger low em alpha. |
| `--ki-outline-danger-med-em` | `#f19997` | semantic color for outline danger med em. |
| `--ki-outline-high-em` | `rgba(0, 0, 0, 0.12)` | semantic color for outline high em. |
| `--ki-outline-info-base-em` | `#dff3ff` | semantic color for outline info base em. |
| `--ki-outline-info-base-em-alpha` | `rgba(19, 185, 236, 0.2)` | semantic color for outline info base em alpha. |
| `--ki-outline-info-high-em` | `#13b9ec` | semantic color for outline info high em. |
| `--ki-outline-info-low-em` | `#a1e3f7` | semantic color for outline info low em. |
| `--ki-outline-info-low-em-alpha` | `rgba(19, 185, 236, 0.4)` | semantic color for outline info low em alpha. |
| `--ki-outline-info-med-em` | `#71d5f4` | semantic color for outline info med em. |
| `--ki-outline-inverse-black` | `#ffffff` | semantic color for outline inverse black. |
| `--ki-outline-inverse-white` | `#000000` | semantic color for outline inverse white. |
| `--ki-outline-low-em` | `rgba(0, 0, 0, 0.05)` | semantic color for outline low em. |
| `--ki-outline-med-em` | `rgba(0, 0, 0, 0.08)` | semantic color for outline med em. |
| `--ki-outline-none` | `rgba(255, 255, 255, 0)` | semantic color for outline none. |
| `--ki-outline-primary-base-em` | `#ece4f5` | semantic color for outline primary base em. |
| `--ki-outline-primary-base-em-alpha` | `rgba(132, 90, 190, 0.2)` | semantic color for outline primary base em alpha. |
| `--ki-outline-primary-button-bottom` | `rgba(0, 0, 0, 0.18)` | semantic color for outline primary button bottom. |
| `--ki-outline-primary-button-top` | `rgba(0, 0, 0, 0.08)` | semantic color for outline primary button top. |
| `--ki-outline-primary-high-em` | `#845abe` | semantic color for outline primary high em. |
| `--ki-outline-primary-low-em` | `#cebee6` | semantic color for outline primary low em. |
| `--ki-outline-primary-low-em-alpha` | `rgba(132, 90, 190, 0.4)` | semantic color for outline primary low em alpha. |
| `--ki-outline-primary-med-em` | `#b59cd8` | semantic color for outline primary med em. |
| `--ki-outline-secondary-button-bottom` | `rgba(0, 0, 0, 0.12)` | semantic color for outline secondary button bottom. |
| `--ki-outline-secondary-button-top` | `rgba(0, 0, 0, 0.05)` | semantic color for outline secondary button top. |
| `--ki-outline-strong` | `rgba(0, 0, 0, 0.48)` | semantic color for outline strong. |
| `--ki-outline-success-base-em` | `#e3f6e2` | semantic color for outline success base em. |
| `--ki-outline-success-base-em-alpha` | `rgba(64, 155, 63, 0.2)` | semantic color for outline success base em alpha. |
| `--ki-outline-success-high-em` | `#409b3f` | semantic color for outline success high em. |
| `--ki-outline-success-low-em` | `#c8ebc7` | semantic color for outline success low em. |
| `--ki-outline-success-low-em-alpha` | `rgba(64, 155, 63, 0.4)` | semantic color for outline success low em alpha. |
| `--ki-outline-success-med-em` | `#9cda9b` | semantic color for outline success med em. |
| `--ki-outline-warning-base-em` | `#fdeace` | semantic color for outline warning base em. |
| `--ki-outline-warning-base-em-alpha` | `rgba(248, 172, 58, 0.2)` | semantic color for outline warning base em alpha. |
| `--ki-outline-warning-high-em` | `#f8ac3a` | semantic color for outline warning high em. |
| `--ki-outline-warning-low-em` | `#fbd59d` | semantic color for outline warning low em. |
| `--ki-outline-warning-low-em-alpha` | `rgba(248, 172, 58, 0.4)` | semantic color for outline warning low em alpha. |
| `--ki-outline-warning-med-em` | `#fbcb84` | semantic color for outline warning med em. |
| `--ki-outline-white` | `#ffffff` | semantic color for outline white. |

### `ki.overlay`

| Token | Value (onmars light) | Description |
| --- | --- | --- |
| `--ki-overlay-high-em` | `rgba(0, 0, 0, 0.4)` | semantic color for overlay high em. |
| `--ki-overlay-high-em-inverse` | `rgba(255, 255, 255, 0.12)` | semantic color for overlay high em inverse. |
| `--ki-overlay-med-em` | `rgba(0, 0, 0, 0.24)` | semantic color for overlay med em. |
| `--ki-overlay-med-em-inverse` | `rgba(255, 255, 255, 0.03)` | semantic color for overlay med em inverse. |

### `ki.spacing`

| Token | Value (onmars light) | Description |
| --- | --- | --- |
| `--ki-spacing-block` | `0.75rem` | Deprecated alias kept for pre-Fase-1 consumers; use ki.space.xl. |
| `--ki-spacing-inline` | `0.5rem` | Deprecated alias kept for pre-Fase-1 consumers; use ki.space.md. |

### `ki.state-layer`

| Token | Value (onmars light) | Description |
| --- | --- | --- |
| `--ki-state-layer-dragged` | `rgba(255, 255, 255, 0.12)` | Dragged interaction state layer. M3 md.sys.state.dragged.state-layer-opacity: 16% of the on-color. onmars: MarsUI overlay two steps above hover_overlay (White/12 light, Black/12 dark). |
| `--ki-state-layer-focus` | `rgba(255, 255, 255, 0.08)` | Focus interaction state layer. M3 md.sys.state.focus.state-layer-opacity: 12% of the on-color. onmars: MarsUI overlay one step above hover_overlay (White/8 light, Black/8 dark). |
| `--ki-state-layer-hover` | `rgba(255, 255, 255, 0.05)` | Hover interaction state layer. M3 md.sys.state.hover.state-layer-opacity: 8% of the on-color. onmars: the MarsUI hover_overlay (White/5 light, Black/5 dark). |
| `--ki-state-layer-pressed` | `rgba(255, 255, 255, 0.08)` | Pressed interaction state layer. M3 md.sys.state.pressed.state-layer-opacity: 12% of the on-color (same as focus). onmars: MarsUI overlay one step above hover_overlay (White/8 light, Black/8 dark). |

### `ki.surface`

| Token | Value (onmars light) | Description |
| --- | --- | --- |
| `--ki-surface-base` | `#ffffff` | Deprecated alias kept for pre-Fase-1 consumers; use ki.surface.s0. |
| `--ki-surface-black` | `#000000` | semantic color for surface black. |
| `--ki-surface-danger-base-em` | `#fff3f2` | semantic color for surface danger base em. |
| `--ki-surface-danger-base-em-alpha` | `rgba(240, 81, 73, 0.12)` | semantic color for surface danger base em alpha. |
| `--ki-surface-danger-high-em` | `#cd2118` | semantic color for surface danger high em. |
| `--ki-surface-danger-high-em-hover` | `#a01a13` | semantic color for surface danger high em hover. |
| `--ki-surface-danger-low-em` | `#ffebed` | semantic color for surface danger low em. |
| `--ki-surface-danger-low-em-alpha` | `rgba(240, 81, 73, 0.2)` | semantic color for surface danger low em alpha. |
| `--ki-surface-danger-med-em` | `#f05149` | semantic color for surface danger med em. |
| `--ki-surface-disabled-base-em` | `#f2f2f4` | semantic color for surface disabled base em. |
| `--ki-surface-disabled-high-em` | `#afb3bb` | semantic color for surface disabled high em. |
| `--ki-surface-disabled-low-em` | `#ececf0` | semantic color for surface disabled low em. |
| `--ki-surface-disabled-med-em` | `#dddfe4` | semantic color for surface disabled med em. |
| `--ki-surface-hover-overlay` | `rgba(255, 255, 255, 0.05)` | semantic color for surface hover overlay. |
| `--ki-surface-hover-overlay-inverse` | `rgba(0, 0, 0, 0.03)` | semantic color for surface hover overlay inverse. |
| `--ki-surface-info-base-em` | `#d0f1fb` | semantic color for surface info base em. |
| `--ki-surface-info-base-em-alpha` | `rgba(19, 185, 236, 0.12)` | semantic color for surface info base em alpha. |
| `--ki-surface-info-high-em` | `#0f94bd` | semantic color for surface info high em. |
| `--ki-surface-info-low-em` | `#dff3ff` | semantic color for surface info low em. |
| `--ki-surface-info-low-em-alpha` | `rgba(19, 185, 236, 0.2)` | semantic color for surface info low em alpha. |
| `--ki-surface-info-med-em` | `#13b9ec` | semantic color for surface info med em. |
| `--ki-surface-inverse-black` | `#ffffff` | semantic color for surface inverse black. |
| `--ki-surface-inverse-white` | `#000000` | semantic color for surface inverse white. |
| `--ki-surface-pressed-overlay-inverse` | `rgba(0, 0, 0, 0.05)` | Pressed interaction overlay for a surface whose fill is LIGHT, so the wash darkens: the next rung above hover-overlay-inverse (Black/3 -> Black/5 light, White/3 -> White/5 dark). The mirror of ki.state-layer.pressed, which is the next rung above hover-overlay and washes the other way. Two conventions exist because ki-button paints primary on a dark fill and secondary on a light one; a single pressed overlay would lighten one of them on press and read as less pressed, not more. |
| `--ki-surface-primary-base-em` | `#f7f4fb` | semantic color for surface primary base em. |
| `--ki-surface-primary-base-em-alpha` | `rgba(132, 90, 190, 0.12)` | semantic color for surface primary base em alpha. |
| `--ki-surface-primary-high-em` | `#5f3993` | semantic color for surface primary high em. |
| `--ki-surface-primary-high-em-hover` | `#845abe` | semantic color for surface primary high em hover. |
| `--ki-surface-primary-low-em` | `#ece4f5` | semantic color for surface primary low em. |
| `--ki-surface-primary-low-em-alpha` | `rgba(132, 90, 190, 0.2)` | semantic color for surface primary low em alpha. |
| `--ki-surface-primary-med-em` | `#845abe` | semantic color for surface primary med em. |
| `--ki-surface-primary-med-em-hover` | `#5f3993` | Hover of surface primary-med-em. Intensifies in the direction the scheme reads as 'more': darker on a light canvas (600), brighter on a dark one (400). Selection controls whose resting fill is the MarsUI default brand surface (brand-500) need a hover that is a real delta in BOTH schemes — primary-high-em collapses onto primary-med-em in dark. |
| `--ki-surface-raised` | `#f9f9fa` | Deprecated alias kept for pre-Fase-1 consumers; use ki.surface.s1. |
| `--ki-surface-s0` | `#ffffff` | semantic color for surface s0. |
| `--ki-surface-s1` | `#f9f9fa` | semantic color for surface s1. |
| `--ki-surface-s2` | `#f2f2f4` | semantic color for surface s2. |
| `--ki-surface-s3` | `#ececf0` | semantic color for surface s3. |
| `--ki-surface-s4` | `#dddfe4` | semantic color for surface s4. |
| `--ki-surface-s5` | `#c3c6cc` | semantic color for surface s5. |
| `--ki-surface-secondary-low-em` | `#ece4f5` | Secondary container surface. M3 md.sys.color.secondary-container (secondary tone 90 light / 30 dark). onmars single-accent: resolves to the primary container equivalent. |
| `--ki-surface-secondary-med-em` | `#845abe` | Secondary accent surface. M3 md.sys.color.secondary (secondary tone 40 light / 80 dark). onmars is a single-accent theme: the secondary accent resolves to the primary accent surface. |
| `--ki-surface-special-blur-base-dark` | `rgba(255, 255, 255, 0.88)` | semantic color for surface special blur base dark. |
| `--ki-surface-special-blur-base-light` | `rgba(255, 255, 255, 0.8)` | semantic color for surface special blur base light. |
| `--ki-surface-special-dark-s0-light-s1` | `#f9f9fa` | semantic color for surface special dark s0 light s1. |
| `--ki-surface-special-dark-s0-light-s2` | `#f2f2f4` | semantic color for surface special dark s0 light s2. |
| `--ki-surface-special-dark-s0-light-s3` | `#ececf0` | semantic color for surface special dark s0 light s3. |
| `--ki-surface-special-dark-s0-light-s4` | `#dddfe4` | semantic color for surface special dark s0 light s4. |
| `--ki-surface-special-dark-s0-light-s5` | `#c3c6cc` | semantic color for surface special dark s0 light s5. |
| `--ki-surface-special-light-s0-dark-s1` | `#ffffff` | semantic color for surface special light s0 dark s1. |
| `--ki-surface-special-light-s0-dark-s2` | `#ffffff` | semantic color for surface special light s0 dark s2. |
| `--ki-surface-special-light-s0-dark-s3` | `#ffffff` | semantic color for surface special light s0 dark s3. |
| `--ki-surface-special-light-s0-dark-s4` | `#ffffff` | semantic color for surface special light s0 dark s4. |
| `--ki-surface-special-light-s0-dark-s5` | `#ffffff` | semantic color for surface special light s0 dark s5. |
| `--ki-surface-special-light-s1-dark-s2` | `#f9f9fa` | semantic color for surface special light s1 dark s2. |
| `--ki-surface-special-light-s2-dark-s3` | `#f2f2f4` | semantic color for surface special light s2 dark s3. |
| `--ki-surface-special-light-s3-dark-s4` | `#ececf0` | semantic color for surface special light s3 dark s4. |
| `--ki-surface-special-light-s4-dark-s5` | `#dddfe4` | semantic color for surface special light s4 dark s5. |
| `--ki-surface-special-secondary-alpha-base` | `rgba(242, 242, 244, 0.8)` | semantic color for surface special secondary alpha base. |
| `--ki-surface-success-base-em` | `#f4faf3` | semantic color for surface success base em. |
| `--ki-surface-success-base-em-alpha` | `rgba(64, 155, 63, 0.12)` | semantic color for surface success base em alpha. |
| `--ki-surface-success-high-em` | `#2c6a2b` | semantic color for surface success high em. |
| `--ki-surface-success-high-em-hover` | `#265526` | semantic color for surface success high em hover. |
| `--ki-surface-success-low-em` | `#e3f6e2` | semantic color for surface success low em. |
| `--ki-surface-success-low-em-alpha` | `rgba(64, 155, 63, 0.2)` | semantic color for surface success low em alpha. |
| `--ki-surface-success-med-em` | `#409b3f` | semantic color for surface success med em. |
| `--ki-surface-tertiary-low-em` | `#ece4f5` | Tertiary container surface. M3 md.sys.color.tertiary-container (tertiary tone 90 light / 30 dark). onmars single-accent: resolves to the primary container equivalent. |
| `--ki-surface-tertiary-med-em` | `#845abe` | Tertiary accent surface. M3 md.sys.color.tertiary (tertiary tone 40 light / 80 dark). onmars is a single-accent theme: the tertiary accent resolves to the primary accent surface. |
| `--ki-surface-tint` | `#845abe` | Surface tint color mixed into elevated surfaces. M3 md.sys.color.surface-tint (= primary; tone 40 light, 80 dark via the brand ramp). onmars: the brand accent — MarsUI elevation is shadow-based, so the tint stays the primary accent. |
| `--ki-surface-warning-base-em` | `#fffeea` | semantic color for surface warning base em. |
| `--ki-surface-warning-base-em-alpha` | `rgba(248, 172, 58, 0.12)` | semantic color for surface warning base em alpha. |
| `--ki-surface-warning-high-em` | `#f29408` | semantic color for surface warning high em. |
| `--ki-surface-warning-low-em` | `#fdeace` | semantic color for surface warning low em. |
| `--ki-surface-warning-low-em-alpha` | `rgba(248, 172, 58, 0.2)` | semantic color for surface warning low em alpha. |
| `--ki-surface-warning-med-em` | `#f8ac3a` | semantic color for surface warning med em. |
| `--ki-surface-white` | `#ffffff` | semantic color for surface white. |

### `ki.text`

| Token | Value (onmars light) | Description |
| --- | --- | --- |
| `--ki-text-base` | `#0a0c11` | Deprecated alias kept for pre-Fase-1 consumers; use ki.text.high-em. |
| `--ki-text-base-em` | `#c3c6cc` | semantic color for text base em. |
| `--ki-text-black` | `#000000` | semantic color for text black. |
| `--ki-text-danger` | `#f05149` | Deprecated alias kept for pre-Fase-1 consumers; use ki.text.danger-med-em. |
| `--ki-text-danger-base-em` | `#ffebed` | semantic color for text danger base em. |
| `--ki-text-danger-high-em` | `#cd2118` | semantic color for text danger high em. |
| `--ki-text-danger-low-em` | `#f19997` | semantic color for text danger low em. |
| `--ki-text-danger-med-em` | `#f05149` | semantic color for text danger med em. |
| `--ki-text-high-em` | `#0a0c11` | semantic color for text high em. |
| `--ki-text-info-base-em` | `#dff3ff` | semantic color for text info base em. |
| `--ki-text-info-high-em` | `#085268` | Darkened 600->800 in the light-scheme SOURCES (badge AA on the base-em tint; 010 D2 contingency, founder sign-off at merge). Reaches three of four contexts: onmars light, material3 light AND material3 dark — m3 defines no info/warning overrides, so its dark inherits these values by cascade; only onmars dark is pinned by the dark-mode file. |
| `--ki-text-info-low-em` | `#71d5f4` | semantic color for text info low em. |
| `--ki-text-info-med-em` | `#13b9ec` | semantic color for text info med em. |
| `--ki-text-inverse-black` | `#ffffff` | semantic color for text inverse black. |
| `--ki-text-inverse-primary` | `#b59cd8` | Primary accent legible on the inverse surface. M3 md.sys.color.inverse-primary (primary tone 80 light / 40 dark). onmars: the opposite-scheme primary text tone (brand 300 light, brand 600 dark). |
| `--ki-text-inverse-white` | `#000000` | semantic color for text inverse white. |
| `--ki-text-low-em` | `#8c929c` | semantic color for text low em. |
| `--ki-text-med-em` | `#5b616d` | semantic color for text med em. |
| `--ki-text-muted` | `#5b616d` | Deprecated alias kept for pre-Fase-1 consumers; use ki.text.med-em. |
| `--ki-text-none` | `rgba(255, 255, 255, 0)` | semantic color for text none. |
| `--ki-text-primary-base-em` | `#ece4f5` | semantic color for text primary base em. |
| `--ki-text-primary-high-em` | `#5f3993` | semantic color for text primary high em. |
| `--ki-text-primary-low-em` | `#b59cd8` | semantic color for text primary low em. |
| `--ki-text-primary-med-em` | `#845abe` | semantic color for text primary med em. |
| `--ki-text-primary-on-danger` | `#ffffff` | semantic color for text primary on danger. |
| `--ki-text-primary-on-primary` | `#ffffff` | semantic color for text primary on primary. |
| `--ki-text-primary-on-primary-light` | `#5f3993` | semantic color for text primary on primary light. |
| `--ki-text-primary-on-secondary` | `#ffffff` | Primary-emphasis text on the secondary accent surface. M3 md.sys.color.on-secondary (secondary tone 100 light / 20 dark). onmars single-accent: resolves to text primary on primary. |
| `--ki-text-primary-on-secondary-light` | `#5f3993` | Primary-emphasis text on the secondary container surface. M3 md.sys.color.on-secondary-container (secondary tone 10 light / 90 dark). onmars single-accent: resolves to text primary on primary light. |
| `--ki-text-primary-on-success` | `#ffffff` | semantic color for text primary on success. |
| `--ki-text-primary-on-tertiary` | `#ffffff` | Primary-emphasis text on the tertiary accent surface. M3 md.sys.color.on-tertiary (tertiary tone 100 light / 20 dark). onmars single-accent: resolves to text primary on primary. |
| `--ki-text-primary-on-tertiary-light` | `#5f3993` | Primary-emphasis text on the tertiary container surface. M3 md.sys.color.on-tertiary-container (tertiary tone 10 light / 90 dark). onmars single-accent: resolves to text primary on primary light. |
| `--ki-text-secondary-on-primary` | `rgba(255, 255, 255, 0.72)` | semantic color for text secondary on primary. |
| `--ki-text-secondary-on-primary-light` | `#7648b7` | semantic color for text secondary on primary light. |
| `--ki-text-secondary-on-success` | `rgba(255, 255, 255, 0.72)` | semantic color for text secondary on success. |
| `--ki-text-success-base-em` | `#e3f6e2` | semantic color for text success base em. |
| `--ki-text-success-high-em` | `#2c6a2b` | semantic color for text success high em. |
| `--ki-text-success-low-em` | `#9cda9b` | semantic color for text success low em. |
| `--ki-text-success-med-em` | `#409b3f` | semantic color for text success med em. |
| `--ki-text-text-on-neutral` | `#ffffff` | semantic color for text text on neutral. |
| `--ki-text-warning-base-em` | `#fdeace` | semantic color for text warning base em. |
| `--ki-text-warning-high-em` | `#945b05` | Darkened 600->800 in the light-scheme SOURCES (badge AA on the base-em tint; 010 D2 contingency, founder sign-off at merge). Reaches three of four contexts: onmars light, material3 light AND material3 dark — m3 defines no info/warning overrides, so its dark inherits these values by cascade; only onmars dark is pinned by the dark-mode file. |
| `--ki-text-warning-low-em` | `#fbcb84` | semantic color for text warning low em. |
| `--ki-text-warning-med-em` | `#f8ac3a` | semantic color for text warning med em. |
| `--ki-text-white` | `#ffffff` | semantic color for text white. |

### `ki.transparent-inverse`

| Token | Value (onmars light) | Description |
| --- | --- | --- |
| `--ki-transparent-inverse` | `rgba(255, 255, 255, 0)` | semantic color for transparent inverse. |

### `ki.typography`

| Token | Value (onmars light) | Description |
| --- | --- | --- |
| `--ki-typography-family-base` | `Inter, system-ui, sans-serif` | Deprecated alias kept for pre-Fase-1 consumers; use ki.typography.family.body. |
| `--ki-typography-family-body` | `Inter, system-ui, sans-serif` | semantic font family for typography family body. |
| `--ki-typography-family-display` | `Inter, system-ui, sans-serif` | semantic font family for typography family display. |
| `--ki-typography-family-mono` | `'JetBrains Mono', ui-monospace, monospace` | semantic font family for typography family mono. |
| `--ki-typography-family-serif` | `Merriweather, Georgia, serif` | semantic font family for typography family serif. |
| `--ki-typography-letter-spacing-lg` | `-0.07em` | semantic dimension for typography letter spacing lg. |
| `--ki-typography-letter-spacing-md` | `-0.05em` | semantic dimension for typography letter spacing md. |
| `--ki-typography-letter-spacing-none` | `0em` | semantic dimension for typography letter spacing none. |
| `--ki-typography-letter-spacing-sm` | `-0.03em` | semantic dimension for typography letter spacing sm. |
| `--ki-typography-letter-spacing-spaced-out` | `0.02em` | semantic dimension for typography letter spacing spaced out (MarsUI tracking spaced_out +2%, bound to overline styles). |
| `--ki-typography-letter-spacing-xs` | `-0.01em` | semantic dimension for typography letter spacing xs. |
| `--ki-typography-line-height-body-1` | `1.5rem` | semantic dimension for typography line height body 1. |
| `--ki-typography-line-height-body-2` | `1.5rem` | semantic dimension for typography line height body 2. |
| `--ki-typography-line-height-caption-1` | `1rem` | semantic dimension for typography line height caption 1. |
| `--ki-typography-line-height-caption-2` | `1rem` | semantic dimension for typography line height caption 2. |
| `--ki-typography-line-height-display-1` | `5rem` | semantic dimension for typography line height display 1. |
| `--ki-typography-line-height-display-2` | `5rem` | semantic dimension for typography line height display 2. |
| `--ki-typography-line-height-display-3` | `6rem` | semantic dimension for typography line height display 3. |
| `--ki-typography-line-height-heading-1` | `1.75rem` | semantic dimension for typography line height heading 1. |
| `--ki-typography-line-height-heading-2` | `2.25rem` | semantic dimension for typography line height heading 2. |
| `--ki-typography-line-height-heading-3` | `2.5rem` | semantic dimension for typography line height heading 3. |
| `--ki-typography-line-height-heading-4` | `3rem` | semantic dimension for typography line height heading 4. |
| `--ki-typography-line-height-heading-5` | `3.5rem` | semantic dimension for typography line height heading 5. |
| `--ki-typography-line-height-heading-6` | `4.25rem` | semantic dimension for typography line height heading 6. |
| `--ki-typography-line-height-overline` | `0.75rem` | semantic dimension for typography line height overline. |
| `--ki-typography-line-height-para` | `1.25rem` | semantic dimension for typography line height para. |
| `--ki-typography-line-height-title-1` | `1.5rem` | semantic dimension for typography line height title 1. |
| `--ki-typography-line-height-title-2` | `1.5rem` | semantic dimension for typography line height title 2. |
| `--ki-typography-size-body` | `0.8125rem` | Deprecated alias kept for pre-Fase-1 consumers; use ki.typography.size.body-1. |
| `--ki-typography-size-body-1` | `0.8125rem` | semantic dimension for typography size body 1. |
| `--ki-typography-size-body-2` | `0.9375rem` | semantic dimension for typography size body 2. |
| `--ki-typography-size-caption-1` | `0.625rem` | semantic dimension for typography size caption 1. |
| `--ki-typography-size-caption-2` | `0.75rem` | semantic dimension for typography size caption 2. |
| `--ki-typography-size-display-1` | `4.75rem` | semantic dimension for typography size display 1. |
| `--ki-typography-size-display-2` | `5.5rem` | semantic dimension for typography size display 2. |
| `--ki-typography-size-display-3` | `6.5rem` | semantic dimension for typography size display 3. |
| `--ki-typography-size-heading-1` | `1.375rem` | semantic dimension for typography size heading 1. |
| `--ki-typography-size-heading-2` | `1.5625rem` | semantic dimension for typography size heading 2. |
| `--ki-typography-size-heading-3` | `2rem` | semantic dimension for typography size heading 3. |
| `--ki-typography-size-heading-4` | `2.625rem` | semantic dimension for typography size heading 4. |
| `--ki-typography-size-heading-5` | `3.25rem` | semantic dimension for typography size heading 5. |
| `--ki-typography-size-heading-6` | `3.875rem` | semantic dimension for typography size heading 6. |
| `--ki-typography-size-overline` | `0.6875rem` | semantic dimension for typography size overline. |
| `--ki-typography-size-title-1` | `0.9375rem` | semantic dimension for typography size title 1. |
| `--ki-typography-size-title-2` | `1.125rem` | semantic dimension for typography size title 2. |
| `--ki-typography-weight-body-bold` | `800` | semantic font weight for typography weight body bold. |
| `--ki-typography-weight-body-medium` | `600` | semantic font weight for typography weight body medium. |
| `--ki-typography-weight-body-normal` | `500` | semantic font weight for typography weight body normal. |
| `--ki-typography-weight-body-semibold` | `700` | semantic font weight for typography weight body semibold. |
| `--ki-typography-weight-display-bold` | `700` | semantic font weight for typography weight display bold. |
| `--ki-typography-weight-display-medium` | `500` | semantic font weight for typography weight display medium. |
| `--ki-typography-weight-display-normal` | `400` | semantic font weight for typography weight display normal. |
| `--ki-typography-weight-display-semibold` | `600` | semantic font weight for typography weight display semibold. |

## Component tokens

The public styling contract of each element. Reassign them on `:root` for a
theme-wide change or on any subtree for a scoped one.

### `ki-alert`

| Token | Value (onmars light) | Description |
| --- | --- | --- |
| `--ki-alert-border-width` | `0rem` | dimension used by the alert component for border width (MarsUI never strokes alert surfaces: Alert master, Info Notes and Toasts all carry width 0; tone lives in the fill and text. material3 keeps its 2px border via its override). |
| `--ki-alert-danger-bg` | `rgba(240, 81, 73, 0.12)` | color used by the alert component for danger background (MarsUI Surface/danger_base_em_alpha: tone-500 at 12% light / alpha dark — the Info-Note tint, replacing the opaque 50/950 slabs). |
| `--ki-alert-danger-border` | `rgba(255, 255, 255, 0)` | color used by the alert component for danger border (MarsUI draws no tone border on any alert/note/toast surface). |
| `--ki-alert-danger-fg` | `#cd2118` | color used by the alert component for danger foreground. |
| `--ki-alert-dismiss-active-fg` | `#0a0c11` | color used by the alert component for dismiss active foreground. |
| `--ki-alert-dismiss-border-width` | `1px` | dimension used by the alert component for the dismiss chip bevel stroke (MarsUI 24px raised circle: 1px Outline/secondary_button_top hairline). |
| `--ki-alert-dismiss-hover-bg` | `rgba(0, 0, 0, 0.03)` | color used by the alert component for dismiss hover background (MarsUI hover mechanism: hover_overlay_inverse Black/3 wash — the dismiss chip previously had zero visible hover change). |
| `--ki-alert-dismiss-hover-fg` | `#0a0c11` | color used by the alert component for dismiss hover foreground. |
| `--ki-alert-dismiss-icon-size` | `1rem` | dimension used by the alert component for dismiss icon size (Figma close glyph box 16px inside the 24px raised circle; repointed from 18px per the fidelity audit). |
| `--ki-alert-dismiss-radius` | `1000px` | dimension used by the alert component for the dismiss chip shape (MarsUI Radius/radius_round full circle). |
| `--ki-alert-dismiss-rest-fg` | `#0a0c11` | color used by the alert component for dismiss rest foreground. |
| `--ki-alert-dismiss-size` | `1.5rem` | dimension used by the alert component for dismiss size. |
| `--ki-alert-focus-ring-color` | `#845abe` | color used by the alert component for focus ring color. |
| `--ki-alert-focus-ring-offset` | `0.125rem` | dimension used by the alert component for focus ring offset. |
| `--ki-alert-focus-ring-shadow` | `0 0 0 3px rgba(132, 90, 190, 0.2)` | shadow used by the alert dismiss control for the focus ring glow (MarsUI Focus/primary: 3px spread ring at 20% — see ki.focus.primary. specs/002-ki-button/design-extraction.md §2.5 recorded 40%, having read the alpha off the Dark canvas mode, same double mechanism as ki-button: glow plus the opaque indicator). |
| `--ki-alert-focus-ring-width` | `0.125rem` | dimension used by the alert component for focus ring width. |
| `--ki-alert-font-size` | `0.8125rem` | dimension used by the alert component for font size. |
| `--ki-alert-gap` | `0.75rem` | dimension used by the alert component for gap (MarsUI Alert master card gap Space/xl 12px). |
| `--ki-alert-heading-font-size` | `0.9375rem` | dimension used by the alert component for heading font size. |
| `--ki-alert-heading-font-weight` | `600` | font weight used by the alert component for heading font weight (MarsUI UI/Body 2/medium: weight 600 on the master heading). |
| `--ki-alert-info-bg` | `rgba(19, 185, 236, 0.12)` | color used by the alert component for info background (MarsUI Surface/info_base_em_alpha: tone-500 at 12% light / alpha dark — the Info-Note tint, replacing the opaque 50/950 slabs). |
| `--ki-alert-info-border` | `rgba(255, 255, 255, 0)` | color used by the alert component for info border (MarsUI draws no tone border on any alert/note/toast surface). |
| `--ki-alert-info-fg` | `#085268` | color used by the alert component for info foreground (MarsUI brief §1 systematic em pattern: tone high-em text on tone base-em surface, matching the success/danger rows and the ki-badge tone matrix; AA 7.3:1). |
| `--ki-alert-line-height` | `1.5rem` | dimension used by the alert component for line height. |
| `--ki-alert-message-line-height` | `1.25rem` | dimension used by the alert component for message line height (MarsUI description is 13/20 line_height/para; the heading keeps 15/24). |
| `--ki-alert-neutral-bg` | `#f9f9fa` | color used by the alert component for neutral background. |
| `--ki-alert-neutral-border` | `rgba(255, 255, 255, 0)` | color used by the alert component for neutral border. |
| `--ki-alert-neutral-fg` | `#0a0c11` | color used by the alert component for neutral foreground. |
| `--ki-alert-padding-block` | `0.75rem` | dimension used by the alert component for padding block (MarsUI Alert master card: 12px on all four sides). |
| `--ki-alert-padding-inline` | `0.75rem` | dimension used by the alert component for padding inline (MarsUI Alert master card: 12px on all four sides). |
| `--ki-alert-radius` | `10px` | dimension used by the alert component for radius (Info Notes pills measure component radius 8-10px; same 10px value, repointed to the component ramp so personality modes reach it. The card-form Alert master anchors 16px big_component/radius_xs — applies if ki-alert grows the icon-chip card form). |
| `--ki-alert-success-bg` | `rgba(64, 155, 63, 0.12)` | color used by the alert component for success background (MarsUI Surface/success_base_em_alpha: tone-500 at 12% light / alpha dark — the Info-Note tint, replacing the opaque 50/950 slabs). |
| `--ki-alert-success-border` | `rgba(255, 255, 255, 0)` | color used by the alert component for success border (MarsUI draws no tone border on any alert/note/toast surface). |
| `--ki-alert-success-fg` | `#2c6a2b` | color used by the alert component for success foreground. |
| `--ki-alert-text-gap` | `0.25rem` | dimension used by the alert component for the heading-to-message stack gap (MarsUI Text_wrap 4px, decoupled from the outer 12px card gap). |
| `--ki-alert-warning-bg` | `rgba(248, 172, 58, 0.12)` | color used by the alert component for warning background (MarsUI Surface/warning_base_em_alpha: tone-500 at 12% light / alpha dark — the Info-Note tint, replacing the opaque 50/950 slabs). |
| `--ki-alert-warning-border` | `rgba(255, 255, 255, 0)` | color used by the alert component for warning border (MarsUI draws no tone border on any alert/note/toast surface). |
| `--ki-alert-warning-fg` | `#945b05` | color used by the alert component for warning foreground (MarsUI brief §1 systematic em pattern: tone high-em text on tone base-em surface, matching the success/danger rows and the ki-badge tone matrix; AA 5.5:1). |

### `ki-avatar`

| Token | Value (onmars light) | Description |
| --- | --- | --- |
| `--ki-avatar-bg` | `#845abe` | color used by the avatar component for the initials/figure surface: Surface/primary_med_em, the exact variable behind the Type=text gradient start and the Type=icon solid fill. The Figma text gradient (primary_med_em -> primary_low_em_alpha 20%) is deliberately flattened to its solid start color — the token pipeline models no gradients and the spec defines one family-level bg (declared deviation, specs/019-ki-avatar/design-extraction.md decision 2, pending founder ratification). |
| `--ki-avatar-border-color` | `rgba(0, 0, 0, 0.03)` | color used by the avatar component for the surface border: Outline/base_em (Black/3 light, White/3 dark), the exact variable on Type=text variants. |
| `--ki-avatar-border-width` | `1px` | dimension used by the avatar component for the surface border (MarsUI 1px hairline on Type=text; applied to the token surface behind initials and figure, dropped under a loaded portrait — design-extraction decision 4). |
| `--ki-avatar-fg` | `#ffffff` | color used by the avatar component for the initials text and the generic-figure glyph (via currentColor): Text/primary_on_primary, the exact variable on every Type=text variant. |
| `--ki-avatar-font-family` | `Inter, system-ui, sans-serif` | font family used by the avatar component for initials (MarsUI Typeface/family/body: Inter). |
| `--ki-avatar-font-weight` | `600` | font weight used by the avatar component for initials (MarsUI Typeface/weight/body/medium: 600). |
| `--ki-avatar-lg-font-size` | `0.9375rem` | dimension used by the avatar component for lg initials (MarsUI UI/Body 2: 15px). |
| `--ki-avatar-lg-icon-size` | `1.5rem` | dimension used by the avatar component for the lg generic-figure glyph (MarsUI Icon/User 24px, Space/7xl). |
| `--ki-avatar-lg-size` | `3rem` | dimension used by the avatar component for the lg box (48px circle, Figma Space/14xl). |
| `--ki-avatar-md-font-size` | `0.8125rem` | dimension used by the avatar component for md initials (MarsUI UI/Para: body_1 13px). |
| `--ki-avatar-md-icon-size` | `1.25rem` | dimension used by the avatar component for the md generic-figure glyph (MarsUI Icon/User 20px, Space/5xl). |
| `--ki-avatar-md-size` | `2.5rem` | dimension used by the avatar component for the md box (40px circle, Figma Space/12xl, node 10011:947). |
| `--ki-avatar-radius` | `1000px` | dimension used by the avatar component for the shape: Radius/radius_round bound to every avatar variant — the circle is a radius token, never an attribute (002 shape precedent, FR-005). |
| `--ki-avatar-sm-font-size` | `0.75rem` | dimension used by the avatar component for sm initials (MarsUI UI/Caption 2: 12px). |
| `--ki-avatar-sm-icon-size` | `1.125rem` | dimension used by the avatar component for the sm generic-figure glyph (MarsUI Icon/User 18px, Space/4xl). |
| `--ki-avatar-sm-size` | `2rem` | dimension used by the avatar component for the sm box (32px circle, Figma Space/10xl). |
| `--ki-avatar-xl-font-size` | `1.125rem` | dimension used by the avatar component for xl initials (MarsUI UI/Title 2: 18px). |
| `--ki-avatar-xl-icon-size` | `1.75rem` | dimension used by the avatar component for the xl generic-figure glyph (MarsUI Icon/User 28px, Space/9xl). |
| `--ki-avatar-xl-size` | `3.5rem` | dimension used by the avatar component for the xl box (56px circle, Figma Space/16xl). |
| `--ki-avatar-xs-font-size` | `0.625rem` | dimension used by the avatar component for xs initials (MarsUI UI/Caption 1: 10px, same step as xxs — verified on Type=text Size=xs 10108:6226). |
| `--ki-avatar-xs-icon-size` | `0.875rem` | dimension used by the avatar component for the xs generic-figure glyph (MarsUI Icon/User 14px, same step as xxs — NOT the icon-button 16px ramp; verified on 10108:6230). |
| `--ki-avatar-xs-size` | `1.5rem` | dimension used by the avatar component for the xs box (24px circle, Figma Space/7xl). |
| `--ki-avatar-xxs-font-size` | `0.625rem` | dimension used by the avatar component for xxs initials (MarsUI UI/Caption 1: 10px). |
| `--ki-avatar-xxs-icon-size` | `0.875rem` | dimension used by the avatar component for the xxs generic-figure glyph (MarsUI Icon/User 14px, Space/2xl). |
| `--ki-avatar-xxs-size` | `1.25rem` | dimension used by the avatar component for the xxs box (20px circle, Figma Space/5xl; specs/019-ki-avatar/design-extraction.md). |

### `ki-avatar-group`

| Token | Value (onmars light) | Description |
| --- | --- | --- |
| `--ki-avatar-group-counter-bg` | `rgba(242, 242, 244, 0.8)` | color used by the avatar group component for the +N counter surface: Surface/special/secondary_alpha_base, the exact variable on the secondary-button-styled counter of every capped Avatar_group frame. |
| `--ki-avatar-group-counter-border-color` | `rgba(0, 0, 0, 0.05)` | color used by the avatar group component for the counter border: Outline/secondary_button_top, the exact variable on the counter pill (the secondary-button bevel top edge). |
| `--ki-avatar-group-counter-border-width` | `1px` | dimension used by the avatar group component for the counter border (MarsUI 1px hairline on the counter pill). |
| `--ki-avatar-group-counter-fg` | `#0a0c11` | color used by the avatar group component for the +N counter text: Text/high_em, the exact variable on the counter label. |
| `--ki-avatar-group-counter-radius` | `1000px` | dimension used by the avatar group component for the counter shape: Radius/component/radius_* resolves to 1000 in the MarsUI Round mode — the pill is a radius token, never an attribute. |
| `--ki-avatar-group-counter-shadow` | `0 2px 1.5px -0.5px rgba(0, 0, 0, 0.03), inset 0px 2px 3px 0px rgba(255, 255, 255, 0.03)` | shadow used by the avatar group component for the counter effects: the exact Component_effect/secondary_default pair (drop 0/2/1.5/-0.5 Elevation/shadow + inner White/3 0/2/3) without its backdrop blur (glass restraint, 021 decision 3). |
| `--ki-avatar-group-lg-counter-font-size` | `0.9375rem` | dimension used by the avatar group component for the lg counter text (body_2 15px on the 48px frame). |
| `--ki-avatar-group-lg-counter-padding-inline` | `0.75rem` | dimension used by the avatar group component for the lg counter inline padding (Space/xl 12px on the 48px frame). |
| `--ki-avatar-group-lg-overlap` | `0.75rem` | dimension used by the avatar group component for the lg member overlap (the exact -12px on the 48px Avatar_group frame 10090:2873). |
| `--ki-avatar-group-md-counter-font-size` | `0.8125rem` | dimension used by the avatar group component for the md counter text (body_1 13px on the 40px frame). |
| `--ki-avatar-group-md-counter-padding-inline` | `0.625rem` | dimension used by the avatar group component for the md counter inline padding (Space/lg 10px on the 40px frame). |
| `--ki-avatar-group-md-overlap` | `0.75rem` | dimension used by the avatar group component for the md member overlap (the exact -12px on the 40px Avatar_group frame 10087:2701). |
| `--ki-avatar-group-ring-color` | `rgba(255, 255, 255, 0)` | color used by the avatar group component for the separating ring: transparent while the onmars ring width is zero (FR-008; a future theme may resolve both). |
| `--ki-avatar-group-ring-width` | `0rem` | dimension used by the avatar group component for the separating ring around stacked members: NO Avatar_group frame draws a ring (verified across all five sizes), so onmars resolves the capability to zero — the ring stays a theme decision, never an attribute (FR-008). |
| `--ki-avatar-group-sm-counter-font-size` | `0.8125rem` | dimension used by the avatar group component for the sm counter text (body_1 13px on the 32px frame — one step above the sm initials, verified). |
| `--ki-avatar-group-sm-counter-padding-inline` | `0.5rem` | dimension used by the avatar group component for the sm counter inline padding (Space/md 8px on the 32px frame). |
| `--ki-avatar-group-sm-overlap` | `0.5rem` | dimension used by the avatar group component for the sm member overlap (the exact -8px on the 32px Avatar_group frame 10087:2601). |
| `--ki-avatar-group-xl-counter-font-size` | `1.125rem` | dimension used by the avatar group component for the xl counter text (title_2 18px on the 56px frame). |
| `--ki-avatar-group-xl-counter-padding-inline` | `0.875rem` | dimension used by the avatar group component for the xl counter inline padding (Space/2xl 14px on the 56px frame). |
| `--ki-avatar-group-xl-overlap` | `0.75rem` | dimension used by the avatar group component for the xl member overlap (the exact -12px on the 56px Avatar_group frame 10090:2973). |
| `--ki-avatar-group-xs-counter-font-size` | `0.625rem` | dimension used by the avatar group component for the xs counter text (synthesized 24px step following the avatar xs initials size, declared for founder review). |
| `--ki-avatar-group-xs-counter-padding-inline` | `0.375rem` | dimension used by the avatar group component for the xs counter inline padding (synthesized 24px step extending the verified 8/10/12/14 ramp, declared for founder review). |
| `--ki-avatar-group-xs-overlap` | `0.5rem` | dimension used by the avatar group component for the xs member overlap: the 24px group step exists in no Figma frame (five-step set), so it inherits the -8px overlap of the neighboring 20/32px frames (synthesized step, spec 019 Assumptions). |
| `--ki-avatar-group-xxs-counter-font-size` | `0.625rem` | dimension used by the avatar group component for the xxs counter text: no counter exists on the 20px frame, so the value follows the avatar xxs initials step (synthesized, declared for founder review). |
| `--ki-avatar-group-xxs-counter-padding-inline` | `0.5rem` | dimension used by the avatar group component for xxs counter padding inline (raised to clear the 8px member tuck so the + glyph is never occluded; the 20px Figma frame ships no counter — synthesized step, founder review pending). |
| `--ki-avatar-group-xxs-overlap` | `0.5rem` | dimension used by the avatar group component for the xxs member overlap (the -8px stack step on the 20px Avatar_group frame — named xs in Figma, the vocabulary skew recorded in spec 019 Assumptions). |

### `ki-badge`

| Token | Value (onmars light) | Description |
| --- | --- | --- |
| `--ki-badge-border-width` | `0rem` | dimension used by the badge component for border width (MarsUI tone tags carry width 0 in default state; only the neutral glass type draws its 1px bevel via ki.badge.neutral.border-width. material3 keeps its border via its override). |
| `--ki-badge-danger-bg` | `rgba(240, 81, 73, 0.12)` | color used by the badge component for danger background (MarsUI Surface/danger_base_em_alpha: tone-500 at 12% light — the tint composites over any surface and the dark alias adapts automatically). |
| `--ki-badge-danger-border` | `rgba(255, 255, 255, 0)` | color used by the badge component for danger border. |
| `--ki-badge-danger-fg` | `#cd2118` | color used by the badge component for danger foreground. |
| `--ki-badge-font-family` | `Inter, system-ui, sans-serif` | font family used by the badge component for font family. |
| `--ki-badge-font-weight` | `600` | font weight used by the badge component for font weight. |
| `--ki-badge-info-bg` | `rgba(19, 185, 236, 0.12)` | color used by the badge component for info background (MarsUI Surface/info_base_em_alpha: tone-500 at 12% light — the tint composites over any surface and the dark alias adapts automatically). |
| `--ki-badge-info-border` | `rgba(255, 255, 255, 0)` | color used by the badge component for info border. |
| `--ki-badge-info-fg` | `#085268` | color used by the badge component for info foreground. |
| `--ki-badge-md-font-size` | `0.75rem` | dimension used by the badge component for md font size (MarsUI UI/Caption 2: 12px at both sm and md; only xs drops to Caption 1). |
| `--ki-badge-md-height` | `1.75rem` | dimension used by the badge component for md height (MarsUI Tag md outer height 28px). |
| `--ki-badge-md-line-height` | `1rem` | dimension used by the badge component for md line height (MarsUI caption_2 16px). |
| `--ki-badge-md-padding-inline` | `0.625rem` | dimension used by the badge component for md padding inline (MarsUI total text inset 10px; paired with the border-width 0 + border-box change so the badge does not shrink). |
| `--ki-badge-md-radius` | `8px` | dimension used by the badge component for md radius (MarsUI Tag: Radius/component/radius_sm 8px at both sm and md). |
| `--ki-badge-neutral-bg` | `rgba(242, 242, 244, 0.8)` | color used by the badge component for neutral background (MarsUI secondary glass Tag: Surface/special/secondary_alpha_base Gray-100 alpha light / Dark-700 blur base dark — the same fill as ki-button secondary; completes the glass bevel/blur/shadow the component already draws. material3 keeps the opaque s2 chip via its override). |
| `--ki-badge-neutral-border` | `rgba(0, 0, 0, 0.05)` | color used by the badge component for neutral border (MarsUI secondary Tag bevel: Outline/secondary_button_top; drawn at ki.badge.neutral.border-width). |
| `--ki-badge-neutral-border-width` | `1px` | dimension used by the badge component for the neutral (secondary glass) border width: MarsUI draws the 1px bevel only on this type. |
| `--ki-badge-neutral-fg` | `#0a0c11` | color used by the badge component for neutral foreground. |
| `--ki-badge-sm-font-size` | `0.75rem` | dimension used by the badge component for sm font size. |
| `--ki-badge-sm-height` | `1.5rem` | dimension used by the badge component for sm height. |
| `--ki-badge-sm-line-height` | `1rem` | dimension used by the badge component for sm line height. |
| `--ki-badge-sm-padding-inline` | `0.5rem` | dimension used by the badge component for sm padding inline (MarsUI total text inset 8px; paired with the border-width 0 + border-box change). |
| `--ki-badge-sm-radius` | `8px` | dimension used by the badge component for sm radius (MarsUI Tag silhouette is an 8px rounded rect — Radius/component/radius_sm — never a pill). |
| `--ki-badge-success-bg` | `rgba(64, 155, 63, 0.12)` | color used by the badge component for success background (MarsUI Surface/success_base_em_alpha: tone-500 at 12% light — the tint composites over any surface and the dark alias adapts automatically). |
| `--ki-badge-success-border` | `rgba(255, 255, 255, 0)` | color used by the badge component for success border. |
| `--ki-badge-success-fg` | `#2c6a2b` | color used by the badge component for success foreground. |
| `--ki-badge-warning-bg` | `rgba(248, 172, 58, 0.12)` | color used by the badge component for warning background (MarsUI Surface/warning_base_em_alpha: tone-500 at 12% light — the tint composites over any surface and the dark alias adapts automatically). |
| `--ki-badge-warning-border` | `rgba(255, 255, 255, 0)` | color used by the badge component for warning border. |
| `--ki-badge-warning-fg` | `#945b05` | color used by the badge component for warning foreground. |

### `ki-button`

| Token | Value (onmars light) | Description |
| --- | --- | --- |
| `--ki-button-backdrop-blur` | `12px` | backdrop blur used by the button component glass variants (primary/secondary). material3 resolves to blur none via the semantic override. |
| `--ki-button-border-width` | `1px` | dimension used by the button component for border width (MarsUI bevel/outline stroke: 1px). |
| `--ki-button-focus-ring-color` | `#845abe` | color used by the button component for focus ring color. |
| `--ki-button-focus-ring-offset` | `0.125rem` | dimension used by the button component for focus ring offset. |
| `--ki-button-focus-ring-shadow` | `0 0 0 3px rgba(132, 90, 190, 0.2)` | shadow used by the button component for the focus ring glow (MarsUI Focus/primary: 3px spread ring). |
| `--ki-button-focus-ring-width` | `0.125rem` | dimension used by the button component for focus ring width. |
| `--ki-button-font-family` | `Inter, system-ui, sans-serif` | font family used by the button component for font family. |
| `--ki-button-font-weight` | `600` | font weight used by the button component for font weight. |
| `--ki-button-ghost-active-shadow` | `none` | value used by the button component for ghost active shadow. |
| `--ki-button-ghost-danger-active-bg` | `#ffebed` | color used by the button component for ghost danger active background. |
| `--ki-button-ghost-danger-active-border` | `rgba(255, 255, 255, 0)` | color used by the button component for ghost danger active border. |
| `--ki-button-ghost-danger-active-fg` | `#cd2118` | color used by the button component for ghost danger active foreground. |
| `--ki-button-ghost-danger-disabled-bg` | `rgba(255, 255, 255, 0)` | color used by the button component for ghost danger disabled background. |
| `--ki-button-ghost-danger-disabled-border` | `rgba(255, 255, 255, 0)` | color used by the button component for ghost danger disabled border. |
| `--ki-button-ghost-danger-disabled-fg` | `#5b616d` | color used by the button component for ghost danger disabled foreground. |
| `--ki-button-ghost-danger-hover-bg` | `rgba(240, 81, 73, 0.12)` | color used by the button component for ghost danger hover background. |
| `--ki-button-ghost-danger-hover-border` | `rgba(255, 255, 255, 0)` | color used by the button component for ghost danger hover border. |
| `--ki-button-ghost-danger-hover-fg` | `#cd2118` | color used by the button component for ghost danger hover foreground. |
| `--ki-button-ghost-danger-rest-bg` | `rgba(255, 255, 255, 0)` | color used by the button component for ghost danger rest background. |
| `--ki-button-ghost-danger-rest-border` | `rgba(255, 255, 255, 0)` | color used by the button component for ghost danger rest border. |
| `--ki-button-ghost-danger-rest-fg` | `#cd2118` | color used by the button component for ghost danger rest foreground. |
| `--ki-button-ghost-disabled-shadow` | `none` | value used by the button component for ghost disabled shadow. |
| `--ki-button-ghost-hover-shadow` | `none` | value used by the button component for ghost hover shadow. |
| `--ki-button-ghost-neutral-active-bg` | `#ece4f5` | color used by the button component for ghost neutral active background. |
| `--ki-button-ghost-neutral-active-border` | `rgba(255, 255, 255, 0)` | color used by the button component for ghost neutral active border. |
| `--ki-button-ghost-neutral-active-fg` | `#5f3993` | color used by the button component for ghost neutral active foreground. |
| `--ki-button-ghost-neutral-disabled-bg` | `rgba(255, 255, 255, 0)` | color used by the button component for ghost neutral disabled background. |
| `--ki-button-ghost-neutral-disabled-border` | `rgba(255, 255, 255, 0)` | color used by the button component for ghost neutral disabled border. |
| `--ki-button-ghost-neutral-disabled-fg` | `#5b616d` | color used by the button component for ghost neutral disabled foreground. |
| `--ki-button-ghost-neutral-hover-bg` | `rgba(132, 90, 190, 0.12)` | color used by the button component for ghost neutral hover background. |
| `--ki-button-ghost-neutral-hover-border` | `rgba(255, 255, 255, 0)` | color used by the button component for ghost neutral hover border. |
| `--ki-button-ghost-neutral-hover-fg` | `#5f3993` | color used by the button component for ghost neutral hover foreground. |
| `--ki-button-ghost-neutral-rest-bg` | `rgba(255, 255, 255, 0)` | color used by the button component for ghost neutral rest background. |
| `--ki-button-ghost-neutral-rest-border` | `rgba(255, 255, 255, 0)` | color used by the button component for ghost neutral rest border. |
| `--ki-button-ghost-neutral-rest-fg` | `#5f3993` | color used by the button component for ghost neutral rest foreground. |
| `--ki-button-ghost-rest-shadow` | `none` | value used by the button component for ghost rest shadow. |
| `--ki-button-ghost-success-active-bg` | `#e3f6e2` | color used by the button component for ghost success active background. |
| `--ki-button-ghost-success-active-border` | `rgba(255, 255, 255, 0)` | color used by the button component for ghost success active border. |
| `--ki-button-ghost-success-active-fg` | `#2c6a2b` | color used by the button component for ghost success active foreground. |
| `--ki-button-ghost-success-disabled-bg` | `rgba(255, 255, 255, 0)` | color used by the button component for ghost success disabled background. |
| `--ki-button-ghost-success-disabled-border` | `rgba(255, 255, 255, 0)` | color used by the button component for ghost success disabled border. |
| `--ki-button-ghost-success-disabled-fg` | `#5b616d` | color used by the button component for ghost success disabled foreground. |
| `--ki-button-ghost-success-hover-bg` | `rgba(64, 155, 63, 0.12)` | color used by the button component for ghost success hover background. |
| `--ki-button-ghost-success-hover-border` | `rgba(255, 255, 255, 0)` | color used by the button component for ghost success hover border. |
| `--ki-button-ghost-success-hover-fg` | `#2c6a2b` | color used by the button component for ghost success hover foreground. |
| `--ki-button-ghost-success-rest-bg` | `rgba(255, 255, 255, 0)` | color used by the button component for ghost success rest background. |
| `--ki-button-ghost-success-rest-border` | `rgba(255, 255, 255, 0)` | color used by the button component for ghost success rest border. |
| `--ki-button-ghost-success-rest-fg` | `#2c6a2b` | color used by the button component for ghost success rest foreground. |
| `--ki-button-label-padding-inline` | `0.25rem` | dimension used by the button component for the label Text_wrap inline padding (MarsUI 8003:300: 4px each side of the label; the flex gap subtracts the same value so icon-to-text keeps the composite distance). |
| `--ki-button-lg-font-size` | `0.9375rem` | dimension used by the button component for lg font size. |
| `--ki-button-lg-gap` | `0.375rem` | dimension used by the button component for lg gap. |
| `--ki-button-lg-height` | `3rem` | dimension used by the button component for lg height. |
| `--ki-button-lg-icon-size` | `1.25rem` | dimension used by the button component for lg icon size. |
| `--ki-button-lg-line-height` | `1.5rem` | dimension used by the button component for lg line height. |
| `--ki-button-lg-min-target` | `1.5rem` | dimension used by the button component for lg min target. |
| `--ki-button-lg-padding-inline` | `0.75rem` | dimension used by the button component for lg padding inline. |
| `--ki-button-lg-radius` | `12px` | dimension used by the button component for lg radius. |
| `--ki-button-md-font-size` | `0.8125rem` | dimension used by the button component for md font size. |
| `--ki-button-md-gap` | `0.375rem` | dimension used by the button component for md gap (MarsUI effective icon-to-text gap 6px). |
| `--ki-button-md-height` | `2.5rem` | dimension used by the button component for md height. |
| `--ki-button-md-icon-size` | `1.125rem` | dimension used by the button component for md icon size. |
| `--ki-button-md-line-height` | `1.5rem` | dimension used by the button component for md line height. |
| `--ki-button-md-min-target` | `1.5rem` | dimension used by the button component for md min target. |
| `--ki-button-md-padding-inline` | `0.625rem` | dimension used by the button component for md padding inline. |
| `--ki-button-md-radius` | `10px` | dimension used by the button component for md radius. |
| `--ki-button-padding-block` | `0.25rem` | dimension used by the button component for padding block (MarsUI: 4px in every size). |
| `--ki-button-primary-active-overlay` | `rgba(255, 255, 255, 0.08)` | Pressed state layer for the primary button: primary paints on a dark brand fill, so its wash lightens: the next rung above ki.surface.hover-overlay (White/5 -> White/8 light). Replaces this variant's share of a 45-cell active bg/fg/border matrix whose values were documented mirrors of rest and which WITHDREW the hover overlay rather than deepening it, an appearance legible only to a pointer user who had hovered first. |
| `--ki-button-primary-active-shadow` | `0 1px 1px -0.5px rgba(0, 0, 0, 0.03), inset 0px 3px 3px 0px rgba(255, 255, 255, 0.12)` | shadow used by the button component for primary active shadow (MarsUI defines no pressed state; mirrors rest). |
| `--ki-button-primary-danger-active-bg` | `#cd2118` | color used by the button component for primary danger active background. |
| `--ki-button-primary-danger-active-border` | `rgba(0, 0, 0, 0.08)` | color used by the button component for primary danger active border (MarsUI bevel top/inline edge). |
| `--ki-button-primary-danger-active-fg` | `#ffffff` | color used by the button component for primary danger active foreground. |
| `--ki-button-primary-danger-disabled-bg` | `#ececf0` | color used by the button component for primary danger disabled background. |
| `--ki-button-primary-danger-disabled-border` | `rgba(255, 255, 255, 0)` | color used by the button component for primary danger disabled border. |
| `--ki-button-primary-danger-disabled-fg` | `#5b616d` | color used by the button component for primary danger disabled foreground. |
| `--ki-button-primary-danger-hover-bg` | `#cd2118` | color used by the button component for primary danger hover background (MarsUI: rest fill; hover lightening comes from the hover overlay). |
| `--ki-button-primary-danger-hover-border` | `rgba(0, 0, 0, 0.08)` | color used by the button component for primary danger hover border (MarsUI bevel top/inline edge). |
| `--ki-button-primary-danger-hover-fg` | `#ffffff` | color used by the button component for primary danger hover foreground. |
| `--ki-button-primary-danger-rest-bg` | `#cd2118` | color used by the button component for primary danger rest background. Deliberate deviation: Figma specifies the 500 ramp (Surface/danger_med_em #f05149) but white on 500 is ≈3.5:1 &lt; AA 4.5:1; Kimen keeps the 700 ramp (precedent 010 D2, pending founder ratification). |
| `--ki-button-primary-danger-rest-border` | `rgba(0, 0, 0, 0.08)` | color used by the button component for primary danger rest border (MarsUI bevel top/inline edge). |
| `--ki-button-primary-danger-rest-fg` | `#ffffff` | color used by the button component for primary danger rest foreground. |
| `--ki-button-primary-disabled-shadow` | `none` | value used by the button component for primary disabled shadow. |
| `--ki-button-primary-hover-overlay` | `rgba(255, 255, 255, 0.05)` | color used by the button component for the primary hover overlay layer (MarsUI hover_overlay, White/5). |
| `--ki-button-primary-hover-shadow` | `0 1px 1px -0.5px rgba(0, 0, 0, 0.03), 0 3px 3px -1.5px rgba(0, 0, 0, 0.03), 0 20px 20px -12px rgba(0, 0, 0, 0.03), inset 0px 3px 3px 0px rgba(255, 255, 255, 0.18)` | shadow used by the button component for button primary hover shadow (MarsUI Component_effect/primary_hover). |
| `--ki-button-primary-neutral-active-bg` | `#845abe` | color used by the button component for primary neutral active background (MarsUI defines no pressed state; mirrors rest). |
| `--ki-button-primary-neutral-active-border` | `rgba(0, 0, 0, 0.08)` | color used by the button component for primary neutral active border (MarsUI bevel top/inline edge). |
| `--ki-button-primary-neutral-active-fg` | `#ffffff` | color used by the button component for primary neutral active foreground. |
| `--ki-button-primary-neutral-disabled-bg` | `#ececf0` | color used by the button component for primary neutral disabled background. Deliberate deviation: Figma disabled is Surface/disabled_base_em gray.100 with gray.400 text (≈1.4:1); Kimen keeps gray.200 for contrast (precedent 010 D2, pending founder ratification). |
| `--ki-button-primary-neutral-disabled-border` | `rgba(255, 255, 255, 0)` | color used by the button component for primary neutral disabled border. |
| `--ki-button-primary-neutral-disabled-fg` | `#5b616d` | color used by the button component for primary neutral disabled foreground. Deliberate deviation: Figma disabled text is text/base_em gray.400 (≈1.4:1 on gray.100); Kimen keeps text.muted gray.700 for readable disabled text (precedent 010 D2, pending founder ratification). |
| `--ki-button-primary-neutral-hover-bg` | `#845abe` | color used by the button component for primary neutral hover background (MarsUI: rest fill; hover lightening comes from the hover overlay). |
| `--ki-button-primary-neutral-hover-border` | `rgba(0, 0, 0, 0.08)` | color used by the button component for primary neutral hover border (MarsUI bevel top/inline edge). |
| `--ki-button-primary-neutral-hover-fg` | `#ffffff` | color used by the button component for primary neutral hover foreground. |
| `--ki-button-primary-neutral-rest-bg` | `#845abe` | color used by the button component for primary neutral rest background (MarsUI Surface/primary_med_em, brand 500). |
| `--ki-button-primary-neutral-rest-border` | `rgba(0, 0, 0, 0.08)` | color used by the button component for primary neutral rest border (MarsUI bevel top/inline edge). |
| `--ki-button-primary-neutral-rest-fg` | `#ffffff` | color used by the button component for primary neutral rest foreground. |
| `--ki-button-primary-rest-border-bottom` | `rgba(0, 0, 0, 0.18)` | color used by the button component for the primary bevel bottom edge (MarsUI Outline/primary_button_bottom). |
| `--ki-button-primary-rest-shadow` | `0 1px 1px -0.5px rgba(0, 0, 0, 0.03), inset 0px 3px 3px 0px rgba(255, 255, 255, 0.12)` | shadow used by the button component for button primary rest shadow (MarsUI Component_effect/primary_default). |
| `--ki-button-primary-success-active-bg` | `#2c6a2b` | color used by the button component for primary success active background. |
| `--ki-button-primary-success-active-border` | `rgba(0, 0, 0, 0.08)` | color used by the button component for primary success active border (MarsUI bevel top/inline edge). |
| `--ki-button-primary-success-active-fg` | `#ffffff` | color used by the button component for primary success active foreground. |
| `--ki-button-primary-success-disabled-bg` | `#ececf0` | color used by the button component for primary success disabled background. |
| `--ki-button-primary-success-disabled-border` | `rgba(255, 255, 255, 0)` | color used by the button component for primary success disabled border. |
| `--ki-button-primary-success-disabled-fg` | `#5b616d` | color used by the button component for primary success disabled foreground. |
| `--ki-button-primary-success-hover-bg` | `#2c6a2b` | color used by the button component for primary success hover background (MarsUI: rest fill; hover lightening comes from the hover overlay). |
| `--ki-button-primary-success-hover-border` | `rgba(0, 0, 0, 0.08)` | color used by the button component for primary success hover border (MarsUI bevel top/inline edge). |
| `--ki-button-primary-success-hover-fg` | `#ffffff` | color used by the button component for primary success hover foreground. |
| `--ki-button-primary-success-rest-bg` | `#2c6a2b` | color used by the button component for primary success rest background. Deliberate deviation: Figma specifies the 500 ramp (Surface/success_med_em #409b3f) but white on 500 is ≈3.5:1 &lt; AA 4.5:1; Kimen keeps the 700 ramp (precedent 010 D2, pending founder ratification). |
| `--ki-button-primary-success-rest-border` | `rgba(0, 0, 0, 0.08)` | color used by the button component for primary success rest border (MarsUI bevel top/inline edge). |
| `--ki-button-primary-success-rest-fg` | `#ffffff` | color used by the button component for primary success rest foreground. |
| `--ki-button-quaternary-active-shadow` | `none` | value used by the button component for quaternary active shadow. |
| `--ki-button-quaternary-danger-active-bg` | `#ffebed` | color used by the button component for quaternary danger active background. |
| `--ki-button-quaternary-danger-active-border` | `rgba(255, 255, 255, 0)` | color used by the button component for quaternary danger active border (MarsUI quaternary is borderless in every state). |
| `--ki-button-quaternary-danger-active-fg` | `#cd2118` | color used by the button component for quaternary danger active foreground. |
| `--ki-button-quaternary-danger-disabled-bg` | `rgba(255, 255, 255, 0)` | color used by the button component for quaternary danger disabled background. |
| `--ki-button-quaternary-danger-disabled-border` | `rgba(255, 255, 255, 0)` | color used by the button component for quaternary danger disabled border (MarsUI quaternary is borderless in every state). |
| `--ki-button-quaternary-danger-disabled-fg` | `#5b616d` | color used by the button component for quaternary danger disabled foreground. |
| `--ki-button-quaternary-danger-hover-bg` | `rgba(240, 81, 73, 0.12)` | color used by the button component for quaternary danger hover background. |
| `--ki-button-quaternary-danger-hover-border` | `rgba(255, 255, 255, 0)` | color used by the button component for quaternary danger hover border (MarsUI quaternary is borderless in every state). |
| `--ki-button-quaternary-danger-hover-fg` | `#cd2118` | color used by the button component for quaternary danger hover foreground. |
| `--ki-button-quaternary-danger-rest-bg` | `rgba(255, 255, 255, 0)` | color used by the button component for quaternary danger rest background. |
| `--ki-button-quaternary-danger-rest-border` | `rgba(255, 255, 255, 0)` | color used by the button component for quaternary danger rest border (MarsUI quaternary is borderless in every state). |
| `--ki-button-quaternary-danger-rest-fg` | `#cd2118` | color used by the button component for quaternary danger rest foreground. |
| `--ki-button-quaternary-disabled-shadow` | `none` | value used by the button component for quaternary disabled shadow. |
| `--ki-button-quaternary-hover-shadow` | `none` | value used by the button component for quaternary hover shadow. |
| `--ki-button-quaternary-neutral-active-bg` | `rgba(0, 0, 0, 0.05)` | color used by the button component for quaternary neutral active background (pressed wash one rung beyond hover: Black/5 light, White/5 dark). |
| `--ki-button-quaternary-neutral-active-border` | `rgba(255, 255, 255, 0)` | color used by the button component for quaternary neutral active border (MarsUI quaternary is borderless in every state). |
| `--ki-button-quaternary-neutral-active-fg` | `#5b616d` | color used by the button component for quaternary neutral active foreground (MarsUI quaternary label text/med_em gray; the outlined-purple rest identity was a pre-master synthesis). |
| `--ki-button-quaternary-neutral-disabled-bg` | `rgba(255, 255, 255, 0)` | color used by the button component for quaternary neutral disabled background. |
| `--ki-button-quaternary-neutral-disabled-border` | `rgba(255, 255, 255, 0)` | color used by the button component for quaternary neutral disabled border (MarsUI quaternary is borderless in every state). |
| `--ki-button-quaternary-neutral-disabled-fg` | `#5b616d` | color used by the button component for quaternary neutral disabled foreground. |
| `--ki-button-quaternary-neutral-hover-bg` | `rgba(0, 0, 0, 0.03)` | color used by the button component for quaternary neutral hover background (MarsUI hover mechanism: hover_overlay_inverse Black/3 wash over the transparent rest fill). |
| `--ki-button-quaternary-neutral-hover-border` | `rgba(255, 255, 255, 0)` | color used by the button component for quaternary neutral hover border (MarsUI quaternary is borderless in every state). |
| `--ki-button-quaternary-neutral-hover-fg` | `#5b616d` | color used by the button component for quaternary neutral hover foreground (MarsUI quaternary label text/med_em gray; the outlined-purple rest identity was a pre-master synthesis). |
| `--ki-button-quaternary-neutral-rest-bg` | `rgba(255, 255, 255, 0)` | color used by the button component for quaternary neutral rest background. |
| `--ki-button-quaternary-neutral-rest-border` | `rgba(255, 255, 255, 0)` | color used by the button component for quaternary neutral rest border (MarsUI quaternary is borderless in every state). |
| `--ki-button-quaternary-neutral-rest-fg` | `#5b616d` | color used by the button component for quaternary neutral rest foreground (MarsUI quaternary label text/med_em gray; the outlined-purple rest identity was a pre-master synthesis). |
| `--ki-button-quaternary-rest-shadow` | `none` | value used by the button component for quaternary rest shadow. |
| `--ki-button-quaternary-success-active-bg` | `#e3f6e2` | color used by the button component for quaternary success active background. |
| `--ki-button-quaternary-success-active-border` | `rgba(255, 255, 255, 0)` | color used by the button component for quaternary success active border (MarsUI quaternary is borderless in every state). |
| `--ki-button-quaternary-success-active-fg` | `#2c6a2b` | color used by the button component for quaternary success active foreground. |
| `--ki-button-quaternary-success-disabled-bg` | `rgba(255, 255, 255, 0)` | color used by the button component for quaternary success disabled background. |
| `--ki-button-quaternary-success-disabled-border` | `rgba(255, 255, 255, 0)` | color used by the button component for quaternary success disabled border (MarsUI quaternary is borderless in every state). |
| `--ki-button-quaternary-success-disabled-fg` | `#5b616d` | color used by the button component for quaternary success disabled foreground. |
| `--ki-button-quaternary-success-hover-bg` | `rgba(64, 155, 63, 0.12)` | color used by the button component for quaternary success hover background. |
| `--ki-button-quaternary-success-hover-border` | `rgba(255, 255, 255, 0)` | color used by the button component for quaternary success hover border (MarsUI quaternary is borderless in every state). |
| `--ki-button-quaternary-success-hover-fg` | `#2c6a2b` | color used by the button component for quaternary success hover foreground. |
| `--ki-button-quaternary-success-rest-bg` | `rgba(255, 255, 255, 0)` | color used by the button component for quaternary success rest background. |
| `--ki-button-quaternary-success-rest-border` | `rgba(255, 255, 255, 0)` | color used by the button component for quaternary success rest border (MarsUI quaternary is borderless in every state). |
| `--ki-button-quaternary-success-rest-fg` | `#2c6a2b` | color used by the button component for quaternary success rest foreground. |
| `--ki-button-secondary-active-overlay` | `rgba(0, 0, 0, 0.05)` | Pressed state layer for the secondary button: secondary paints on a light fill, so its wash darkens: the next rung above ki.surface.hover-overlay-inverse (Black/3 -> Black/5 light). Replaces this variant's share of a 45-cell active bg/fg/border matrix whose values were documented mirrors of rest and which WITHDREW the hover overlay rather than deepening it, an appearance legible only to a pointer user who had hovered first. |
| `--ki-button-secondary-active-shadow` | `0 2px 1.5px -0.5px rgba(0, 0, 0, 0.03), inset 0px 2px 3px 0px rgba(255, 255, 255, 0.03)` | shadow used by the button component for secondary active shadow (MarsUI defines no pressed state; mirrors rest). |
| `--ki-button-secondary-danger-active-bg` | `#ffebed` | color used by the button component for secondary danger active background. |
| `--ki-button-secondary-danger-active-border` | `rgba(0, 0, 0, 0.05)` | color used by the button component for secondary danger active border (MarsUI bevel top/inline edge, variant trait). |
| `--ki-button-secondary-danger-active-fg` | `#cd2118` | color used by the button component for secondary danger active foreground. |
| `--ki-button-secondary-danger-disabled-bg` | `#ececf0` | color used by the button component for secondary danger disabled background. |
| `--ki-button-secondary-danger-disabled-border` | `rgba(255, 255, 255, 0)` | color used by the button component for secondary danger disabled border. |
| `--ki-button-secondary-danger-disabled-fg` | `#5b616d` | color used by the button component for secondary danger disabled foreground. |
| `--ki-button-secondary-danger-hover-bg` | `#ffebed` | color used by the button component for secondary danger hover background. |
| `--ki-button-secondary-danger-hover-border` | `rgba(0, 0, 0, 0.05)` | color used by the button component for secondary danger hover border (MarsUI bevel top/inline edge, variant trait). |
| `--ki-button-secondary-danger-hover-fg` | `#cd2118` | color used by the button component for secondary danger hover foreground. |
| `--ki-button-secondary-danger-rest-bg` | `#fff3f2` | color used by the button component for secondary danger rest background. |
| `--ki-button-secondary-danger-rest-border` | `rgba(0, 0, 0, 0.05)` | color used by the button component for secondary danger rest border (MarsUI bevel top/inline edge, variant trait). |
| `--ki-button-secondary-danger-rest-fg` | `#cd2118` | color used by the button component for secondary danger rest foreground. |
| `--ki-button-secondary-disabled-shadow` | `none` | value used by the button component for secondary disabled shadow. |
| `--ki-button-secondary-hover-overlay` | `rgba(0, 0, 0, 0.03)` | color used by the button component for the secondary hover overlay layer (MarsUI hover_overlay_inverse, Black/3). |
| `--ki-button-secondary-hover-shadow` | `0 1px 1px -0.5px rgba(0, 0, 0, 0.03), 0 3px 3px -1.5px rgba(0, 0, 0, 0.03), 0 20px 20px -12px rgba(0, 0, 0, 0.03), inset 0px 2px 3px 0px rgba(255, 255, 255, 0.08)` | shadow used by the button component for secondary hover shadow (MarsUI Component_effect/secondary_hover). |
| `--ki-button-secondary-neutral-active-bg` | `rgba(242, 242, 244, 0.8)` | color used by the button component for secondary neutral active background (MarsUI defines no pressed state; mirrors rest). |
| `--ki-button-secondary-neutral-active-border` | `rgba(0, 0, 0, 0.05)` | color used by the button component for secondary neutral active border (MarsUI bevel top/inline edge). |
| `--ki-button-secondary-neutral-active-fg` | `#0a0c11` | color used by the button component for secondary neutral active foreground (MarsUI text/high_em). |
| `--ki-button-secondary-neutral-disabled-bg` | `#ececf0` | color used by the button component for secondary neutral disabled background. |
| `--ki-button-secondary-neutral-disabled-border` | `rgba(255, 255, 255, 0)` | color used by the button component for secondary neutral disabled border. |
| `--ki-button-secondary-neutral-disabled-fg` | `#5b616d` | color used by the button component for secondary neutral disabled foreground. |
| `--ki-button-secondary-neutral-hover-bg` | `rgba(242, 242, 244, 0.8)` | color used by the button component for secondary neutral hover background (MarsUI: rest fill; hover darkening comes from the hover overlay). |
| `--ki-button-secondary-neutral-hover-border` | `rgba(0, 0, 0, 0.05)` | color used by the button component for secondary neutral hover border (MarsUI bevel top/inline edge). |
| `--ki-button-secondary-neutral-hover-fg` | `#0a0c11` | color used by the button component for secondary neutral hover foreground (MarsUI text/high_em). |
| `--ki-button-secondary-neutral-rest-bg` | `rgba(242, 242, 244, 0.8)` | color used by the button component for secondary neutral rest background (MarsUI Surface/special/secondary_alpha_base: gray glass, not a brand tint). |
| `--ki-button-secondary-neutral-rest-border` | `rgba(0, 0, 0, 0.05)` | color used by the button component for secondary neutral rest border (MarsUI bevel top/inline edge). |
| `--ki-button-secondary-neutral-rest-fg` | `#0a0c11` | color used by the button component for secondary neutral rest foreground (MarsUI text/high_em). |
| `--ki-button-secondary-rest-border-bottom` | `rgba(0, 0, 0, 0.12)` | color used by the button component for the secondary bevel bottom edge (MarsUI Outline/secondary_button_bottom). |
| `--ki-button-secondary-rest-shadow` | `0 2px 1.5px -0.5px rgba(0, 0, 0, 0.03), inset 0px 2px 3px 0px rgba(255, 255, 255, 0.03)` | shadow used by the button component for secondary rest shadow (MarsUI Component_effect/secondary_default). |
| `--ki-button-secondary-success-active-bg` | `#e3f6e2` | color used by the button component for secondary success active background. |
| `--ki-button-secondary-success-active-border` | `rgba(0, 0, 0, 0.05)` | color used by the button component for secondary success active border (MarsUI bevel top/inline edge, variant trait). |
| `--ki-button-secondary-success-active-fg` | `#2c6a2b` | color used by the button component for secondary success active foreground. |
| `--ki-button-secondary-success-disabled-bg` | `#ececf0` | color used by the button component for secondary success disabled background. |
| `--ki-button-secondary-success-disabled-border` | `rgba(255, 255, 255, 0)` | color used by the button component for secondary success disabled border. |
| `--ki-button-secondary-success-disabled-fg` | `#5b616d` | color used by the button component for secondary success disabled foreground. |
| `--ki-button-secondary-success-hover-bg` | `#e3f6e2` | color used by the button component for secondary success hover background. |
| `--ki-button-secondary-success-hover-border` | `rgba(0, 0, 0, 0.05)` | color used by the button component for secondary success hover border (MarsUI bevel top/inline edge, variant trait). |
| `--ki-button-secondary-success-hover-fg` | `#2c6a2b` | color used by the button component for secondary success hover foreground. |
| `--ki-button-secondary-success-rest-bg` | `#f4faf3` | color used by the button component for secondary success rest background. |
| `--ki-button-secondary-success-rest-border` | `rgba(0, 0, 0, 0.05)` | color used by the button component for secondary success rest border (MarsUI bevel top/inline edge, variant trait). |
| `--ki-button-secondary-success-rest-fg` | `#2c6a2b` | color used by the button component for secondary success rest foreground. |
| `--ki-button-sm-font-size` | `0.8125rem` | dimension used by the button component for sm font size. |
| `--ki-button-sm-gap` | `0.25rem` | dimension used by the button component for sm gap (interpolated between xs and md effective gaps). |
| `--ki-button-sm-height` | `2rem` | dimension used by the button component for sm height. |
| `--ki-button-sm-icon-size` | `1rem` | dimension used by the button component for sm icon size. |
| `--ki-button-sm-line-height` | `1.5rem` | dimension used by the button component for sm line height. |
| `--ki-button-sm-min-target` | `1.5rem` | dimension used by the button component for sm min target. |
| `--ki-button-sm-padding-inline` | `0.5rem` | dimension used by the button component for sm padding inline. |
| `--ki-button-sm-radius` | `8px` | dimension used by the button component for sm radius. |
| `--ki-button-tertiary-active-shadow` | `0 2px 1.5px -0.5px rgba(0, 0, 0, 0.03), inset 0px 2px 3px 0px rgba(255, 255, 255, 0.03)` | shadow used by the button component for tertiary active shadow (MarsUI defines no pressed state; mirrors rest). |
| `--ki-button-tertiary-danger-active-bg` | `#f2f2f4` | color used by the button component for tertiary danger active background (MarsUI defines no pressed state; one step beyond hover in both schemes). |
| `--ki-button-tertiary-danger-active-border` | `rgba(0, 0, 0, 0.05)` | color used by the button component for tertiary danger active border (MarsUI secondary bevel top/inline edge — tertiary carries the secondary_button outline pair). |
| `--ki-button-tertiary-danger-active-fg` | `#cd2118` | color used by the button component for tertiary danger active foreground. |
| `--ki-button-tertiary-danger-disabled-bg` | `#ececf0` | color used by the button component for tertiary danger disabled background. |
| `--ki-button-tertiary-danger-disabled-border` | `rgba(255, 255, 255, 0)` | color used by the button component for tertiary danger disabled border. |
| `--ki-button-tertiary-danger-disabled-fg` | `#5b616d` | color used by the button component for tertiary danger disabled foreground. |
| `--ki-button-tertiary-danger-hover-bg` | `#f9f9fa` | color used by the button component for tertiary danger hover background (one light step over the rest fill; dark keeps the rest fill — the hover signal is Component_effect/secondary_hover). |
| `--ki-button-tertiary-danger-hover-border` | `rgba(0, 0, 0, 0.05)` | color used by the button component for tertiary danger hover border (MarsUI secondary bevel top/inline edge — tertiary carries the secondary_button outline pair). |
| `--ki-button-tertiary-danger-hover-fg` | `#cd2118` | color used by the button component for tertiary danger hover foreground. |
| `--ki-button-tertiary-danger-rest-bg` | `#ffffff` | color used by the button component for tertiary danger rest background (MarsUI tertiary fill Surface/special/light-s0_dark-s2: White light, Dark/800 dark). |
| `--ki-button-tertiary-danger-rest-border` | `rgba(0, 0, 0, 0.05)` | color used by the button component for tertiary danger rest border (MarsUI secondary bevel top/inline edge — tertiary carries the secondary_button outline pair). |
| `--ki-button-tertiary-danger-rest-fg` | `#cd2118` | color used by the button component for tertiary danger rest foreground. |
| `--ki-button-tertiary-disabled-shadow` | `none` | value used by the button component for tertiary disabled shadow. |
| `--ki-button-tertiary-hover-shadow` | `0 1px 1px -0.5px rgba(0, 0, 0, 0.03), 0 3px 3px -1.5px rgba(0, 0, 0, 0.03), 0 20px 20px -12px rgba(0, 0, 0, 0.03), inset 0px 2px 3px 0px rgba(255, 255, 255, 0.08)` | shadow used by the button component for tertiary hover shadow (MarsUI Component_effect/secondary_hover). |
| `--ki-button-tertiary-neutral-active-bg` | `#f2f2f4` | color used by the button component for tertiary neutral active background (MarsUI defines no pressed state; one step beyond hover in both schemes). |
| `--ki-button-tertiary-neutral-active-border` | `rgba(0, 0, 0, 0.05)` | color used by the button component for tertiary neutral active border (MarsUI secondary bevel top/inline edge — tertiary carries the secondary_button outline pair). |
| `--ki-button-tertiary-neutral-active-fg` | `#5b616d` | color used by the button component for tertiary neutral active foreground (MarsUI tertiary label text/med_em gray — Kimen tertiary maps to Figma tertiary; the purple-label row is primary_flat, documented alternative). |
| `--ki-button-tertiary-neutral-disabled-bg` | `#ececf0` | color used by the button component for tertiary neutral disabled background. |
| `--ki-button-tertiary-neutral-disabled-border` | `rgba(255, 255, 255, 0)` | color used by the button component for tertiary neutral disabled border. |
| `--ki-button-tertiary-neutral-disabled-fg` | `#5b616d` | color used by the button component for tertiary neutral disabled foreground. |
| `--ki-button-tertiary-neutral-hover-bg` | `#f9f9fa` | color used by the button component for tertiary neutral hover background (one light step over the rest fill; dark keeps the rest fill — the hover signal is Component_effect/secondary_hover). |
| `--ki-button-tertiary-neutral-hover-border` | `rgba(0, 0, 0, 0.05)` | color used by the button component for tertiary neutral hover border (MarsUI secondary bevel top/inline edge — tertiary carries the secondary_button outline pair). |
| `--ki-button-tertiary-neutral-hover-fg` | `#5b616d` | color used by the button component for tertiary neutral hover foreground (MarsUI tertiary label text/med_em gray — Kimen tertiary maps to Figma tertiary; the purple-label row is primary_flat, documented alternative). |
| `--ki-button-tertiary-neutral-rest-bg` | `#ffffff` | color used by the button component for tertiary neutral rest background (MarsUI tertiary fill Surface/special/light-s0_dark-s2: White light, Dark/800 dark). |
| `--ki-button-tertiary-neutral-rest-border` | `rgba(0, 0, 0, 0.05)` | color used by the button component for tertiary neutral rest border (MarsUI secondary bevel top/inline edge — tertiary carries the secondary_button outline pair). |
| `--ki-button-tertiary-neutral-rest-fg` | `#5b616d` | color used by the button component for tertiary neutral rest foreground (MarsUI tertiary label text/med_em gray — Kimen tertiary maps to Figma tertiary; the purple-label row is primary_flat, documented alternative). |
| `--ki-button-tertiary-rest-border-bottom` | `rgba(0, 0, 0, 0.12)` | color used by the button component for the tertiary bevel bottom edge (MarsUI Outline/secondary_button_bottom — same bevel pair as secondary). |
| `--ki-button-tertiary-rest-shadow` | `0 2px 1.5px -0.5px rgba(0, 0, 0, 0.03), inset 0px 2px 3px 0px rgba(255, 255, 255, 0.03)` | shadow used by the button component for tertiary rest shadow (MarsUI Component_effect/secondary_default — replaces the migrated M2-era stack, which the material3 theme keeps via its own override). |
| `--ki-button-tertiary-success-active-bg` | `#f2f2f4` | color used by the button component for tertiary success active background (MarsUI defines no pressed state; one step beyond hover in both schemes). |
| `--ki-button-tertiary-success-active-border` | `rgba(0, 0, 0, 0.05)` | color used by the button component for tertiary success active border (MarsUI secondary bevel top/inline edge — tertiary carries the secondary_button outline pair). |
| `--ki-button-tertiary-success-active-fg` | `#2c6a2b` | color used by the button component for tertiary success active foreground. |
| `--ki-button-tertiary-success-disabled-bg` | `#ececf0` | color used by the button component for tertiary success disabled background. |
| `--ki-button-tertiary-success-disabled-border` | `rgba(255, 255, 255, 0)` | color used by the button component for tertiary success disabled border. |
| `--ki-button-tertiary-success-disabled-fg` | `#5b616d` | color used by the button component for tertiary success disabled foreground. |
| `--ki-button-tertiary-success-hover-bg` | `#f9f9fa` | color used by the button component for tertiary success hover background (one light step over the rest fill; dark keeps the rest fill — the hover signal is Component_effect/secondary_hover). |
| `--ki-button-tertiary-success-hover-border` | `rgba(0, 0, 0, 0.05)` | color used by the button component for tertiary success hover border (MarsUI secondary bevel top/inline edge — tertiary carries the secondary_button outline pair). |
| `--ki-button-tertiary-success-hover-fg` | `#2c6a2b` | color used by the button component for tertiary success hover foreground. |
| `--ki-button-tertiary-success-rest-bg` | `#ffffff` | color used by the button component for tertiary success rest background (MarsUI tertiary fill Surface/special/light-s0_dark-s2: White light, Dark/800 dark). |
| `--ki-button-tertiary-success-rest-border` | `rgba(0, 0, 0, 0.05)` | color used by the button component for tertiary success rest border (MarsUI secondary bevel top/inline edge — tertiary carries the secondary_button outline pair). |
| `--ki-button-tertiary-success-rest-fg` | `#2c6a2b` | color used by the button component for tertiary success rest foreground. |
| `--ki-button-xl-font-size` | `1.125rem` | dimension used by the button component for xl font size. |
| `--ki-button-xl-gap` | `0.5rem` | dimension used by the button component for xl gap (MarsUI effective icon-to-text gap 8px). |
| `--ki-button-xl-height` | `3.5rem` | dimension used by the button component for xl height. |
| `--ki-button-xl-icon-size` | `1.5rem` | dimension used by the button component for xl icon size. |
| `--ki-button-xl-line-height` | `1.5rem` | dimension used by the button component for xl line height. |
| `--ki-button-xl-min-target` | `1.5rem` | dimension used by the button component for xl min target. |
| `--ki-button-xl-padding-inline` | `0.875rem` | dimension used by the button component for xl padding inline (MarsUI 14px). |
| `--ki-button-xl-radius` | `14px` | dimension used by the button component for xl radius. |
| `--ki-button-xs-font-size` | `0.625rem` | dimension used by the button component for xs font size (MarsUI caption_1 10px). |
| `--ki-button-xs-gap` | `0.25rem` | dimension used by the button component for xs gap (MarsUI effective icon-to-text gap 4px). |
| `--ki-button-xs-height` | `1.5rem` | dimension used by the button component for xs height. |
| `--ki-button-xs-icon-size` | `0.875rem` | dimension used by the button component for xs icon size (MarsUI 14px). |
| `--ki-button-xs-line-height` | `1rem` | dimension used by the button component for xs line height (MarsUI caption_1 16px). |
| `--ki-button-xs-min-target` | `1.5rem` | dimension used by the button component for xs min target. |
| `--ki-button-xs-padding-inline` | `0.375rem` | dimension used by the button component for xs padding inline. |
| `--ki-button-xs-radius` | `6px` | dimension used by the button component for xs radius. |

### `ki-card`

| Token | Value (onmars light) | Description |
| --- | --- | --- |
| `--ki-card-backdrop-blur` | `48px` | backdrop blur for the card glass surface (MarsUI Dashboard masters BACKGROUND_BLUR 96 = CSS 48px). Opt-in: ki-card.css does not consume it yet — enabling it is a component change gated on a perf measurement; material3 resolves blur none via its override. |
| `--ki-card-bg` | `#ffffff` | color used by the card component for background (MarsUI Dashboard masters bind Surface/special/light-s0_dark-s1: white light / Dark/900 dark; #f9f9fa is the nested media panel, so the old s1 value inverted the surface hierarchy). |
| `--ki-card-body-padding` | `1rem` | dimension used by the card component for body padding. |
| `--ki-card-border-color` | `rgba(0, 0, 0, 0.03)` | color used by the card component for border color (MarsUI Outline/base_em hairline — painted on large Chart cards; Dashboard_info leaves width 0, which stays the default via ki.card.border-width). |
| `--ki-card-border-width` | `0rem` | dimension used by the card component for border width. |
| `--ki-card-elevation` | `0 1px 1px -0.5px rgba(0, 0, 0, 0.03)` | Card resting elevation: MarsUI elevation ramp level e1 (brief §5, drop 0/1/1/-0.5 in the scheme shadow color; replaces the migrated M2-era shadow). |
| `--ki-card-fg` | `#0a0c11` | color used by the card component for foreground. |
| `--ki-card-footer-padding` | `1rem` | dimension used by the card component for footer padding. |
| `--ki-card-gap` | `0.75rem` | dimension used by the card component for gap (MarsUI Dashboard_info/Chart inter-region rhythm 12px; material3 keeps zero via its override). |
| `--ki-card-header-padding` | `1rem` | dimension used by the card component for header padding. |
| `--ki-card-media-padding` | `0.5rem` | dimension used by the card component for media padding (MarsUI Dashboard masters inset media panels 8px inline; material3 keeps full-bleed zero via its override). |
| `--ki-card-media-radius` | `20px` | dimension used by the card component for slotted media corner radius (MarsUI nested media panel Radius/big_component/radius_sm 20px; formalizes the fallback ki-card.css already consumes). |
| `--ki-card-padding-block` | `1rem` | dimension used by the card component for the surface block padding (16px carried by the card surface itself so stacked region paddings no longer double to 32px; formalizes the fallback ki-card.css already consumes). |
| `--ki-card-radius` | `16px` | dimension used by the card component for radius (MarsUI card surfaces ride the big_component scale: Dashboard_info 400x128 binds radius_xs 16px; large Chart cards anchor radius_lg 28px). |

### `ki-checkbox`

| Token | Value (onmars light) | Description |
| --- | --- | --- |
| `--ki-checkbox-border-width` | `0.125rem` | dimension used by the checkbox component for border width. |
| `--ki-checkbox-checked-active-bg` | `#845abe` | color used by the checkbox component for checked active background. |
| `--ki-checkbox-checked-active-border` | `rgba(255, 255, 255, 0)` | color used by the checkbox component for checked active border. |
| `--ki-checkbox-checked-active-fg` | `#ffffff` | color used by the checkbox component for checked active foreground. |
| `--ki-checkbox-checked-disabled-bg` | `#dddfe4` | color used by the checkbox component for checked disabled background (MarsUI Surface/disabled_med_em Gray/300 light / White-18 dark). |
| `--ki-checkbox-checked-disabled-border` | `rgba(255, 255, 255, 0)` | color used by the checkbox component for checked disabled border. |
| `--ki-checkbox-checked-disabled-fg` | `#c3c6cc` | color used by the checkbox component for checked disabled mark (MarsUI Text/base_em Gray/400 light / White-32 dark; retires the deprecated ki.text.muted alias). |
| `--ki-checkbox-checked-hover-bg` | `#5f3993` | color used by the checkbox component for checked hover background. |
| `--ki-checkbox-checked-hover-border` | `rgba(255, 255, 255, 0)` | color used by the checkbox component for checked hover border. |
| `--ki-checkbox-checked-hover-fg` | `#ffffff` | color used by the checkbox component for checked hover foreground. |
| `--ki-checkbox-checked-rest-bg` | `#845abe` | color used by the checkbox component for checked rest background. |
| `--ki-checkbox-checked-rest-border` | `rgba(255, 255, 255, 0)` | color used by the checkbox component for checked rest border. |
| `--ki-checkbox-checked-rest-fg` | `#ffffff` | color used by the checkbox component for checked rest foreground. |
| `--ki-checkbox-control-size` | `1.25rem` | dimension used by the checkbox component for control size. |
| `--ki-checkbox-focus-ring-color` | `#845abe` | color used by the checkbox component for focus ring color. |
| `--ki-checkbox-focus-ring-offset` | `0.125rem` | dimension used by the checkbox component for focus ring offset. |
| `--ki-checkbox-focus-ring-shadow` | `0 0 0 3px rgba(132, 90, 190, 0.2)` | shadow used by the checkbox component for the focus ring glow (MarsUI Focus/primary: 3px spread ring at 20% — see ki.focus.primary. specs/002-ki-button/design-extraction.md §2.5 recorded 40%, having read the alpha off the Dark canvas mode, same double mechanism as ki-button: glow plus the opaque indicator). |
| `--ki-checkbox-focus-ring-width` | `0.125rem` | dimension used by the checkbox component for focus ring width. |
| `--ki-checkbox-gap` | `0.75rem` | dimension used by the checkbox component for gap (MarsUI md label pairing: Space/xl 12px for the 20px box). |
| `--ki-checkbox-indeterminate-active-bg` | `rgba(132, 90, 190, 0.2)` | color used by the checkbox component for indeterminate active background (MarsUI Surface/primary_low_em_alpha: brand-500 at 20% light / 56% dark; the master defines no indeterminate hover, so every state keeps the tint). |
| `--ki-checkbox-indeterminate-active-border` | `#845abe` | color used by the checkbox component for indeterminate active border (Figma draws no border on the 20% tint, but the tint alone measures ~1.3:1 against light pages; the opaque brand ring keeps the WCAG 1.4.11 3:1 control boundary — gate-driven declared deviation, same law that pinned ki.outline.control). |
| `--ki-checkbox-indeterminate-active-fg` | `#5f3993` | color used by the checkbox component for indeterminate active dash (Figma binds Text/primary_med_em brand-500, which measures 2.7:1 against the 56% tint in dark; one em step up keeps the 3:1 non-text law in both schemes — gate-driven compliant alternative). |
| `--ki-checkbox-indeterminate-disabled-bg` | `#dddfe4` | color used by the checkbox component for indeterminate disabled background (MarsUI Surface/disabled_med_em Gray/300 light / White-18 dark). |
| `--ki-checkbox-indeterminate-disabled-border` | `rgba(255, 255, 255, 0)` | color used by the checkbox component for indeterminate disabled border. |
| `--ki-checkbox-indeterminate-disabled-fg` | `#c3c6cc` | color used by the checkbox component for indeterminate disabled mark (MarsUI Text/base_em Gray/400 light / White-32 dark; retires the deprecated ki.text.muted alias). |
| `--ki-checkbox-indeterminate-hover-bg` | `rgba(132, 90, 190, 0.2)` | color used by the checkbox component for indeterminate hover background (MarsUI Surface/primary_low_em_alpha: brand-500 at 20% light / 56% dark; the master defines no indeterminate hover, so every state keeps the tint). |
| `--ki-checkbox-indeterminate-hover-border` | `#845abe` | color used by the checkbox component for indeterminate hover border (Figma draws no border on the 20% tint, but the tint alone measures ~1.3:1 against light pages; the opaque brand ring keeps the WCAG 1.4.11 3:1 control boundary — gate-driven declared deviation, same law that pinned ki.outline.control). |
| `--ki-checkbox-indeterminate-hover-fg` | `#5f3993` | color used by the checkbox component for indeterminate hover dash (Figma binds Text/primary_med_em brand-500, which measures 2.7:1 against the 56% tint in dark; one em step up keeps the 3:1 non-text law in both schemes — gate-driven compliant alternative). |
| `--ki-checkbox-indeterminate-rest-bg` | `rgba(132, 90, 190, 0.2)` | color used by the checkbox component for indeterminate rest background (MarsUI Surface/primary_low_em_alpha: brand-500 at 20% light / 56% dark; the master defines no indeterminate hover, so every state keeps the tint). |
| `--ki-checkbox-indeterminate-rest-border` | `#845abe` | color used by the checkbox component for indeterminate rest border (Figma draws no border on the 20% tint, but the tint alone measures ~1.3:1 against light pages; the opaque brand ring keeps the WCAG 1.4.11 3:1 control boundary — gate-driven declared deviation, same law that pinned ki.outline.control). |
| `--ki-checkbox-indeterminate-rest-fg` | `#5f3993` | color used by the checkbox component for indeterminate rest dash (Figma binds Text/primary_med_em brand-500, which measures 2.7:1 against the 56% tint in dark; one em step up keeps the 3:1 non-text law in both schemes — gate-driven compliant alternative). |
| `--ki-checkbox-invalid-bg` | `#ffebed` | color used by the checkbox component for invalid background. |
| `--ki-checkbox-invalid-border` | `#f05149` | color used by the checkbox component for invalid border. |
| `--ki-checkbox-invalid-fg` | `#cd2118` | color used by the checkbox component for invalid foreground. |
| `--ki-checkbox-label-font-size` | `0.9375rem` | dimension used by the checkbox component for label font size (MarsUI md pairing: body_2 15px for the 20px box). |
| `--ki-checkbox-label-font-weight` | `500` | font weight used by the checkbox component for label font weight. |
| `--ki-checkbox-label-line-height` | `1.5rem` | dimension used by the checkbox component for label line height (MarsUI body_2 24px). |
| `--ki-checkbox-min-target` | `1.5rem` | dimension used by the checkbox component for min target. |
| `--ki-checkbox-radius` | `6px` | dimension used by the checkbox component for radius (MarsUI Radius/radius_sm 6px bound on the box; Figma corner smoothing is an accepted rasterization gap). |
| `--ki-checkbox-unchecked-active-bg` | `#f2f2f4` | color used by the checkbox component for unchecked active background. |
| `--ki-checkbox-unchecked-active-border` | `#845abe` | color used by the checkbox component for unchecked active border. |
| `--ki-checkbox-unchecked-active-fg` | `#0a0c11` | color used by the checkbox component for unchecked active foreground. |
| `--ki-checkbox-unchecked-disabled-bg` | `#dddfe4` | color used by the checkbox component for unchecked disabled background (MarsUI Surface/disabled_med_em Gray/300 light / White-18 dark). |
| `--ki-checkbox-unchecked-disabled-border` | `rgba(0, 0, 0, 0.08)` | color used by the checkbox component for unchecked disabled border. |
| `--ki-checkbox-unchecked-disabled-fg` | `#c3c6cc` | color used by the checkbox component for unchecked disabled mark (MarsUI Text/base_em Gray/400 light / White-32 dark; retires the deprecated ki.text.muted alias). |
| `--ki-checkbox-unchecked-hover-bg` | `#f9f9fa` | color used by the checkbox component for unchecked hover background. |
| `--ki-checkbox-unchecked-hover-border` | `#845abe` | color used by the checkbox component for unchecked hover border. |
| `--ki-checkbox-unchecked-hover-fg` | `#0a0c11` | color used by the checkbox component for unchecked hover foreground. |
| `--ki-checkbox-unchecked-rest-bg` | `#ffffff` | color used by the checkbox component for unchecked rest background. |
| `--ki-checkbox-unchecked-rest-border` | `rgba(0, 0, 0, 0.48)` | color used by the checkbox component for unchecked rest border. |
| `--ki-checkbox-unchecked-rest-fg` | `#0a0c11` | color used by the checkbox component for unchecked rest foreground. |

### `ki-dialog`

| Token | Value (onmars light) | Description |
| --- | --- | --- |
| `--ki-dialog-backdrop-bg` | `rgba(0, 0, 0, 0.4)` | color used by the dialog component for backdrop background. |
| `--ki-dialog-backdrop-blur` | `12px` | backdrop blur used by the dialog component glass surface (MarsUI BACKGROUND_BLUR Blur/24 = CSS 12px; material3 resolves to blur none via the semantic override; perf-measure before enabling per DNA section 5). |
| `--ki-dialog-bg` | `#ffffff` | color used by the dialog component for background (flat fallback of the MarsUI glass gradient: Surface/Special/light-s0_dark-s2; components paint the gradient from ki.dialog.bg-start to ki.dialog.bg-end). |
| `--ki-dialog-bg-end` | `#ffffff` | color used by the dialog component for the glass gradient end (MarsUI Surface/Special/light-s0_dark-s2: white light / Dark/800 dark). |
| `--ki-dialog-bg-start` | `rgba(255, 255, 255, 0.8)` | color used by the dialog component for the glass gradient start (MarsUI Surface/Special/blur_base_light White/80; dark resolves rgba(36,36,36,0.8)). Paint background: linear-gradient(180deg, var(--ki-dialog-bg-start), var(--ki-dialog-bg-end)). |
| `--ki-dialog-border` | `1px solid rgba(0, 0, 0, 0.03)` | border used by the dialog component for border (MarsUI 1px solid Outline/base_em Black/3 light / White/3 dark; retires the migrated Black/5 foundation reference). |
| `--ki-dialog-fg` | `#0a0c11` | color used by the dialog component for foreground. |
| `--ki-dialog-focus-ring-color` | `#845abe` | color used by the dialog component for focus ring color. |
| `--ki-dialog-focus-ring-offset` | `0.125rem` | dimension used by the dialog component for focus ring offset. |
| `--ki-dialog-focus-ring-width` | `0.125rem` | dimension used by the dialog component for focus ring width. |
| `--ki-dialog-gap` | `1.25rem` | dimension used by the dialog component for gap. |
| `--ki-dialog-heading-font-size` | `1.125rem` | dimension used by the dialog component for heading font size. |
| `--ki-dialog-heading-font-weight` | `700` | font weight used by the dialog component for heading font weight. |
| `--ki-dialog-heading-line-height` | `1.5rem` | dimension used by the dialog component for heading line height. |
| `--ki-dialog-max-width` | `32rem` | dimension used by the dialog component for max width (surface size scale; the former ki.space.26xl reference misused the spacing scale). |
| `--ki-dialog-min-width` | `24rem` | dimension used by the dialog component for min width (surface size scale; the former ki.space.25xl reference misused the spacing scale). |
| `--ki-dialog-motion-duration` | `240ms` | duration used by the dialog component for the enter transition (MarsUI moderate step 240ms; retires the migrated 0ms token that disabled the @starting-style fade). |
| `--ki-dialog-motion-easing` | `cubic-bezier(0, 0, 0.58, 1)` | easing used by the dialog component for the enter transition (MarsUI decelerate curve; retires the migrated linear pair). |
| `--ki-dialog-padding` | `1.5rem` | dimension used by the dialog component for padding. |
| `--ki-dialog-radius` | `20px` | dimension used by the dialog component for radius (MarsUI compact Modal master 16414:19665: Radius/big_component/radius_sm 20px; the large variant anchors 24px big_component/radius_md). |
| `--ki-dialog-shadow` | `0 1px 1px -0.5px rgba(0, 0, 0, 0.03), 0 3px 3px -1.5px rgba(0, 0, 0, 0.03), 0 20px 20px -12px rgba(0, 0, 0, 0.03), 0 32px 32px -16px rgba(0, 0, 0, 0.03)` | Dialog elevation: MarsUI elevation ramp level e4 (brief §5, high modal surface; replaces the migrated approximation). material3 keeps its own e3 override (M3 dialog = level3). |

### `ki-divider`

| Token | Value (onmars light) | Description |
| --- | --- | --- |
| `--ki-divider-color` | `rgba(0, 0, 0, 0.03)` | color used by the divider component for the rule: Outline/base_em (Black/3 light, White/3 dark), the exact variable bound to every Figma divider variant and the same semantic role ki-list uses for item dividers. |
| `--ki-divider-radius` | `1000px` | dimension used by the divider component for the end caps: the MarsUI ends axis is a pure-appearance, per-theme choice (FR-006); onmars keeps the rounded hairline (ends=rounded, pill caps). |
| `--ki-divider-spacing` | `0.5rem` | dimension used by the divider component for the reserved gutter: the 8px MarsUI variant frame with the rule centered inside it (FR-007). |
| `--ki-divider-thickness` | `1px` | dimension used by the divider component for the rule thickness: the MarsUI 1px hairline verified on the Divider_horizontal/Divider_vertical sets (specs/020-ki-divider/design-extraction.md). |

### `ki-icon-button`

| Token | Value (onmars light) | Description |
| --- | --- | --- |
| `--ki-icon-button-backdrop-blur` | `12px` | dimension used by the icon button component for the glass variants (primary/secondary) backdrop blur, identical to the button glass. material3 resolves to blur none via the semantic override. |
| `--ki-icon-button-border-width` | `1px` | dimension used by the icon button component for border width (MarsUI bevel/outline stroke: 1px, INNER — the square box already contains it). |
| `--ki-icon-button-focus-ring-color` | `#845abe` | color used by the icon button component for focus ring color (the opaque WCAG 1.4.11 indicator). |
| `--ki-icon-button-focus-ring-offset` | `0.125rem` | dimension used by the icon button component for focus ring offset. |
| `--ki-icon-button-focus-ring-shadow` | `0 0 0 3px rgba(132, 90, 190, 0.2)` | shadow used by the icon button component for the focus ring glow (MarsUI Focus/primary: 3px spread ring, V.1 double mechanism). |
| `--ki-icon-button-focus-ring-width` | `0.125rem` | dimension used by the icon button component for focus ring width. |
| `--ki-icon-button-ghost-active-shadow` | `none` | value used by the icon button component for ghost active shadow. |
| `--ki-icon-button-ghost-danger-active-bg` | `#ffebed` | color used by the icon button component for ghost danger active background (same semantic reference as the button matrix, 002). |
| `--ki-icon-button-ghost-danger-active-border` | `rgba(255, 255, 255, 0)` | color used by the icon button component for ghost danger active border. |
| `--ki-icon-button-ghost-danger-active-fg` | `#cd2118` | color used by the icon button component for ghost danger active foreground — the icon inherits it via currentColor. |
| `--ki-icon-button-ghost-danger-hover-bg` | `rgba(240, 81, 73, 0.12)` | color used by the icon button component for ghost danger hover background (same semantic reference as the button matrix, 002). |
| `--ki-icon-button-ghost-danger-hover-border` | `rgba(255, 255, 255, 0)` | color used by the icon button component for ghost danger hover border. |
| `--ki-icon-button-ghost-danger-hover-fg` | `#cd2118` | color used by the icon button component for ghost danger hover foreground — the icon inherits it via currentColor. |
| `--ki-icon-button-ghost-danger-rest-bg` | `rgba(255, 255, 255, 0)` | color used by the icon button component for ghost danger rest background (same semantic reference as the button matrix, 002). |
| `--ki-icon-button-ghost-danger-rest-border` | `rgba(255, 255, 255, 0)` | color used by the icon button component for ghost danger rest border. |
| `--ki-icon-button-ghost-danger-rest-fg` | `#cd2118` | color used by the icon button component for ghost danger rest foreground — the icon inherits it via currentColor. |
| `--ki-icon-button-ghost-disabled-shadow` | `none` | value used by the icon button component for ghost disabled shadow (Figma disabled drops every shadow). |
| `--ki-icon-button-ghost-hover-shadow` | `none` | value used by the icon button component for ghost hover shadow. |
| `--ki-icon-button-ghost-neutral-active-bg` | `#ece4f5` | color used by the icon button component for ghost neutral active background (same semantic reference as the button matrix, 002). |
| `--ki-icon-button-ghost-neutral-active-border` | `rgba(255, 255, 255, 0)` | color used by the icon button component for ghost neutral active border. |
| `--ki-icon-button-ghost-neutral-active-fg` | `#5f3993` | color used by the icon button component for ghost neutral active foreground — the icon inherits it via currentColor. |
| `--ki-icon-button-ghost-neutral-disabled-bg` | `rgba(255, 255, 255, 0)` | color used by the icon button component for ghost disabled background (tone-independent, like the button disabled cells; deliberate 010 D2 deviation from Figma gray.100 for contrast, pending founder ratification). |
| `--ki-icon-button-ghost-neutral-disabled-border` | `rgba(255, 255, 255, 0)` | color used by the icon button component for ghost disabled border (Figma disabled drops the bevel). |
| `--ki-icon-button-ghost-neutral-disabled-fg` | `#5b616d` | color used by the icon button component for ghost disabled foreground (tone-independent; 010 D2 deviation from Figma text/base_em, pending founder ratification). |
| `--ki-icon-button-ghost-neutral-hover-bg` | `rgba(132, 90, 190, 0.12)` | color used by the icon button component for ghost neutral hover background (same semantic reference as the button matrix, 002). |
| `--ki-icon-button-ghost-neutral-hover-border` | `rgba(255, 255, 255, 0)` | color used by the icon button component for ghost neutral hover border. |
| `--ki-icon-button-ghost-neutral-hover-fg` | `#5f3993` | color used by the icon button component for ghost neutral hover foreground — the icon inherits it via currentColor. |
| `--ki-icon-button-ghost-neutral-rest-bg` | `rgba(255, 255, 255, 0)` | color used by the icon button component for ghost neutral rest background (same semantic reference as the button matrix, 002). |
| `--ki-icon-button-ghost-neutral-rest-border` | `rgba(255, 255, 255, 0)` | color used by the icon button component for ghost neutral rest border. |
| `--ki-icon-button-ghost-neutral-rest-fg` | `#5f3993` | color used by the icon button component for ghost neutral rest foreground — the icon inherits it via currentColor. |
| `--ki-icon-button-ghost-rest-shadow` | `none` | value used by the icon button component for ghost rest shadow. |
| `--ki-icon-button-ghost-success-active-bg` | `#e3f6e2` | color used by the icon button component for ghost success active background (same semantic reference as the button matrix, 002). |
| `--ki-icon-button-ghost-success-active-border` | `rgba(255, 255, 255, 0)` | color used by the icon button component for ghost success active border. |
| `--ki-icon-button-ghost-success-active-fg` | `#2c6a2b` | color used by the icon button component for ghost success active foreground — the icon inherits it via currentColor. |
| `--ki-icon-button-ghost-success-hover-bg` | `rgba(64, 155, 63, 0.12)` | color used by the icon button component for ghost success hover background (same semantic reference as the button matrix, 002). |
| `--ki-icon-button-ghost-success-hover-border` | `rgba(255, 255, 255, 0)` | color used by the icon button component for ghost success hover border. |
| `--ki-icon-button-ghost-success-hover-fg` | `#2c6a2b` | color used by the icon button component for ghost success hover foreground — the icon inherits it via currentColor. |
| `--ki-icon-button-ghost-success-rest-bg` | `rgba(255, 255, 255, 0)` | color used by the icon button component for ghost success rest background (same semantic reference as the button matrix, 002). |
| `--ki-icon-button-ghost-success-rest-border` | `rgba(255, 255, 255, 0)` | color used by the icon button component for ghost success rest border. |
| `--ki-icon-button-ghost-success-rest-fg` | `#2c6a2b` | color used by the icon button component for ghost success rest foreground — the icon inherits it via currentColor. |
| `--ki-icon-button-lg-icon-size` | `1.5rem` | dimension used by the icon button component for lg icon size (MarsUI Placeholder_lg 24px). |
| `--ki-icon-button-lg-radius` | `12px` | dimension used by the icon button component for lg radius (12, same ramp as button). |
| `--ki-icon-button-lg-size` | `3rem` | dimension used by the icon button component for lg box (48px square). |
| `--ki-icon-button-md-icon-size` | `1.25rem` | dimension used by the icon button component for md icon size (MarsUI Placeholder_md 20px — the button md icon is 18px, 002 §3). |
| `--ki-icon-button-md-radius` | `10px` | dimension used by the icon button component for md radius (10, same ramp as button). |
| `--ki-icon-button-md-size` | `2.5rem` | dimension used by the icon button component for md box (40px square, Figma node 10078:3129). |
| `--ki-icon-button-primary-active-overlay` | `rgba(255, 255, 255, 0.08)` | Pressed state layer for the primary icon button: primary paints on a dark brand fill, so its wash lightens: the next rung above ki.surface.hover-overlay (White/5 -> White/8 light). Replaces this variant's share of a 45-cell active bg/fg/border matrix whose values were documented mirrors of rest and which WITHDREW the hover overlay rather than deepening it, an appearance legible only to a pointer user who had hovered first. |
| `--ki-icon-button-primary-active-shadow` | `0 1px 1px -0.5px rgba(0, 0, 0, 0.03), inset 0px 3px 3px 0px rgba(255, 255, 255, 0.12)` | shadow used by the icon button component for primary active shadow (MarsUI Component_effect/primary_default; MarsUI defines no pressed state, mirrors rest). |
| `--ki-icon-button-primary-danger-active-bg` | `#cd2118` | color used by the icon button component for primary danger active background (same semantic reference as the button matrix, 002). |
| `--ki-icon-button-primary-danger-active-border` | `rgba(0, 0, 0, 0.08)` | color used by the icon button component for primary danger active border (MarsUI bevel top/inline edge). |
| `--ki-icon-button-primary-danger-active-fg` | `#ffffff` | color used by the icon button component for primary danger active foreground — the icon inherits it via currentColor. |
| `--ki-icon-button-primary-danger-hover-bg` | `#cd2118` | color used by the icon button component for primary danger hover background (same semantic reference as the button matrix, 002). |
| `--ki-icon-button-primary-danger-hover-border` | `rgba(0, 0, 0, 0.08)` | color used by the icon button component for primary danger hover border (MarsUI bevel top/inline edge). |
| `--ki-icon-button-primary-danger-hover-fg` | `#ffffff` | color used by the icon button component for primary danger hover foreground — the icon inherits it via currentColor. |
| `--ki-icon-button-primary-danger-rest-bg` | `#cd2118` | color used by the icon button component for primary danger rest background (same semantic reference as the button matrix, 002). |
| `--ki-icon-button-primary-danger-rest-border` | `rgba(0, 0, 0, 0.08)` | color used by the icon button component for primary danger rest border (MarsUI bevel top/inline edge). |
| `--ki-icon-button-primary-danger-rest-fg` | `#ffffff` | color used by the icon button component for primary danger rest foreground — the icon inherits it via currentColor. |
| `--ki-icon-button-primary-disabled-shadow` | `none` | value used by the icon button component for primary disabled shadow (Figma disabled drops every shadow). |
| `--ki-icon-button-primary-hover-overlay` | `rgba(255, 255, 255, 0.05)` | color used by the icon button component for the primary hover overlay layer (MarsUI hover_overlay, identical to the button overlay). |
| `--ki-icon-button-primary-hover-shadow` | `0 1px 1px -0.5px rgba(0, 0, 0, 0.03), 0 3px 3px -1.5px rgba(0, 0, 0, 0.03), 0 20px 20px -12px rgba(0, 0, 0, 0.03), inset 0px 3px 3px 0px rgba(255, 255, 255, 0.18)` | shadow used by the icon button component for primary hover shadow (MarsUI Component_effect/primary_hover). |
| `--ki-icon-button-primary-neutral-active-bg` | `#845abe` | color used by the icon button component for primary neutral active background (same semantic reference as the button matrix, 002). |
| `--ki-icon-button-primary-neutral-active-border` | `rgba(0, 0, 0, 0.08)` | color used by the icon button component for primary neutral active border (MarsUI bevel top/inline edge). |
| `--ki-icon-button-primary-neutral-active-fg` | `#ffffff` | color used by the icon button component for primary neutral active foreground — the icon inherits it via currentColor. |
| `--ki-icon-button-primary-neutral-disabled-bg` | `#ececf0` | color used by the icon button component for primary disabled background (tone-independent, like the button disabled cells; deliberate 010 D2 deviation from Figma gray.100 for contrast, pending founder ratification). |
| `--ki-icon-button-primary-neutral-disabled-border` | `rgba(255, 255, 255, 0)` | color used by the icon button component for primary disabled border (Figma disabled drops the bevel). |
| `--ki-icon-button-primary-neutral-disabled-fg` | `#5b616d` | color used by the icon button component for primary disabled foreground (tone-independent; 010 D2 deviation from Figma text/base_em, pending founder ratification). |
| `--ki-icon-button-primary-neutral-hover-bg` | `#845abe` | color used by the icon button component for primary neutral hover background (same semantic reference as the button matrix, 002). |
| `--ki-icon-button-primary-neutral-hover-border` | `rgba(0, 0, 0, 0.08)` | color used by the icon button component for primary neutral hover border (MarsUI bevel top/inline edge). |
| `--ki-icon-button-primary-neutral-hover-fg` | `#ffffff` | color used by the icon button component for primary neutral hover foreground — the icon inherits it via currentColor. |
| `--ki-icon-button-primary-neutral-rest-bg` | `#845abe` | color used by the icon button component for primary neutral rest background (same semantic reference as the button matrix, 002). |
| `--ki-icon-button-primary-neutral-rest-border` | `rgba(0, 0, 0, 0.08)` | color used by the icon button component for primary neutral rest border (MarsUI bevel top/inline edge). |
| `--ki-icon-button-primary-neutral-rest-fg` | `#ffffff` | color used by the icon button component for primary neutral rest foreground — the icon inherits it via currentColor. |
| `--ki-icon-button-primary-rest-border-bottom` | `rgba(0, 0, 0, 0.18)` | color used by the icon button component for the primary bevel bottom edge (MarsUI Outline/primary_button_bottom, identical to the button bevel). |
| `--ki-icon-button-primary-rest-shadow` | `0 1px 1px -0.5px rgba(0, 0, 0, 0.03), inset 0px 3px 3px 0px rgba(255, 255, 255, 0.12)` | shadow used by the icon button component for primary rest shadow (MarsUI Component_effect/primary_default). |
| `--ki-icon-button-primary-success-active-bg` | `#2c6a2b` | color used by the icon button component for primary success active background (same semantic reference as the button matrix, 002). |
| `--ki-icon-button-primary-success-active-border` | `rgba(0, 0, 0, 0.08)` | color used by the icon button component for primary success active border (MarsUI bevel top/inline edge). |
| `--ki-icon-button-primary-success-active-fg` | `#ffffff` | color used by the icon button component for primary success active foreground — the icon inherits it via currentColor. |
| `--ki-icon-button-primary-success-hover-bg` | `#2c6a2b` | color used by the icon button component for primary success hover background (same semantic reference as the button matrix, 002). |
| `--ki-icon-button-primary-success-hover-border` | `rgba(0, 0, 0, 0.08)` | color used by the icon button component for primary success hover border (MarsUI bevel top/inline edge). |
| `--ki-icon-button-primary-success-hover-fg` | `#ffffff` | color used by the icon button component for primary success hover foreground — the icon inherits it via currentColor. |
| `--ki-icon-button-primary-success-rest-bg` | `#2c6a2b` | color used by the icon button component for primary success rest background (same semantic reference as the button matrix, 002). |
| `--ki-icon-button-primary-success-rest-border` | `rgba(0, 0, 0, 0.08)` | color used by the icon button component for primary success rest border (MarsUI bevel top/inline edge). |
| `--ki-icon-button-primary-success-rest-fg` | `#ffffff` | color used by the icon button component for primary success rest foreground — the icon inherits it via currentColor. |
| `--ki-icon-button-quaternary-active-shadow` | `none` | value used by the icon button component for quaternary active shadow. |
| `--ki-icon-button-quaternary-danger-active-bg` | `#ffebed` | color used by the icon button component for quaternary danger active background (same semantic reference as the button matrix, 002). |
| `--ki-icon-button-quaternary-danger-active-border` | `rgba(255, 255, 255, 0)` | color used by the icon button component for quaternary danger active border (MarsUI quaternary is borderless in every state). |
| `--ki-icon-button-quaternary-danger-active-fg` | `#cd2118` | color used by the icon button component for quaternary danger active foreground — the icon inherits it via currentColor. |
| `--ki-icon-button-quaternary-danger-hover-bg` | `rgba(240, 81, 73, 0.12)` | color used by the icon button component for quaternary danger hover background (same semantic reference as the button matrix, 002). |
| `--ki-icon-button-quaternary-danger-hover-border` | `rgba(255, 255, 255, 0)` | color used by the icon button component for quaternary danger hover border (MarsUI quaternary is borderless in every state). |
| `--ki-icon-button-quaternary-danger-hover-fg` | `#cd2118` | color used by the icon button component for quaternary danger hover foreground — the icon inherits it via currentColor. |
| `--ki-icon-button-quaternary-danger-rest-bg` | `rgba(255, 255, 255, 0)` | color used by the icon button component for quaternary danger rest background (same semantic reference as the button matrix, 002). |
| `--ki-icon-button-quaternary-danger-rest-border` | `rgba(255, 255, 255, 0)` | color used by the icon button component for quaternary danger rest border (MarsUI quaternary is borderless in every state). |
| `--ki-icon-button-quaternary-danger-rest-fg` | `#cd2118` | color used by the icon button component for quaternary danger rest foreground — the icon inherits it via currentColor. |
| `--ki-icon-button-quaternary-disabled-shadow` | `none` | value used by the icon button component for quaternary disabled shadow (Figma disabled drops every shadow). |
| `--ki-icon-button-quaternary-hover-shadow` | `none` | value used by the icon button component for quaternary hover shadow. |
| `--ki-icon-button-quaternary-neutral-active-bg` | `rgba(0, 0, 0, 0.05)` | color used by the icon button component for quaternary neutral active background (pressed wash one rung beyond hover: Black/5 light, White/5 dark). |
| `--ki-icon-button-quaternary-neutral-active-border` | `rgba(255, 255, 255, 0)` | color used by the icon button component for quaternary neutral active border (MarsUI quaternary is borderless in every state). |
| `--ki-icon-button-quaternary-neutral-active-fg` | `#5b616d` | color used by the icon button component for quaternary neutral active icon color (MarsUI quaternary glyph text/med_em gray; the outlined-purple rest identity was a pre-master synthesis). |
| `--ki-icon-button-quaternary-neutral-disabled-bg` | `rgba(255, 255, 255, 0)` | color used by the icon button component for quaternary disabled background (tone-independent, like the button disabled cells; deliberate 010 D2 deviation from Figma gray.100 for contrast, pending founder ratification). |
| `--ki-icon-button-quaternary-neutral-disabled-border` | `rgba(255, 255, 255, 0)` | color used by the icon button component for quaternary neutral disabled border (MarsUI quaternary is borderless in every state). |
| `--ki-icon-button-quaternary-neutral-disabled-fg` | `#5b616d` | color used by the icon button component for quaternary disabled foreground (tone-independent; 010 D2 deviation from Figma text/base_em, pending founder ratification). |
| `--ki-icon-button-quaternary-neutral-hover-bg` | `rgba(0, 0, 0, 0.03)` | color used by the icon button component for quaternary neutral hover background (MarsUI hover mechanism: hover_overlay_inverse Black/3 wash over the transparent rest fill). |
| `--ki-icon-button-quaternary-neutral-hover-border` | `rgba(255, 255, 255, 0)` | color used by the icon button component for quaternary neutral hover border (MarsUI quaternary is borderless in every state). |
| `--ki-icon-button-quaternary-neutral-hover-fg` | `#5b616d` | color used by the icon button component for quaternary neutral hover icon color (MarsUI quaternary glyph text/med_em gray; the outlined-purple rest identity was a pre-master synthesis). |
| `--ki-icon-button-quaternary-neutral-rest-bg` | `rgba(255, 255, 255, 0)` | color used by the icon button component for quaternary neutral rest background (same semantic reference as the button matrix, 002). |
| `--ki-icon-button-quaternary-neutral-rest-border` | `rgba(255, 255, 255, 0)` | color used by the icon button component for quaternary neutral rest border (MarsUI quaternary is borderless in every state). |
| `--ki-icon-button-quaternary-neutral-rest-fg` | `#5b616d` | color used by the icon button component for quaternary neutral rest icon color (MarsUI quaternary glyph text/med_em gray; the outlined-purple rest identity was a pre-master synthesis). |
| `--ki-icon-button-quaternary-rest-shadow` | `none` | value used by the icon button component for quaternary rest shadow. |
| `--ki-icon-button-quaternary-success-active-bg` | `#e3f6e2` | color used by the icon button component for quaternary success active background (same semantic reference as the button matrix, 002). |
| `--ki-icon-button-quaternary-success-active-border` | `rgba(255, 255, 255, 0)` | color used by the icon button component for quaternary success active border (MarsUI quaternary is borderless in every state). |
| `--ki-icon-button-quaternary-success-active-fg` | `#2c6a2b` | color used by the icon button component for quaternary success active foreground — the icon inherits it via currentColor. |
| `--ki-icon-button-quaternary-success-hover-bg` | `rgba(64, 155, 63, 0.12)` | color used by the icon button component for quaternary success hover background (same semantic reference as the button matrix, 002). |
| `--ki-icon-button-quaternary-success-hover-border` | `rgba(255, 255, 255, 0)` | color used by the icon button component for quaternary success hover border (MarsUI quaternary is borderless in every state). |
| `--ki-icon-button-quaternary-success-hover-fg` | `#2c6a2b` | color used by the icon button component for quaternary success hover foreground — the icon inherits it via currentColor. |
| `--ki-icon-button-quaternary-success-rest-bg` | `rgba(255, 255, 255, 0)` | color used by the icon button component for quaternary success rest background (same semantic reference as the button matrix, 002). |
| `--ki-icon-button-quaternary-success-rest-border` | `rgba(255, 255, 255, 0)` | color used by the icon button component for quaternary success rest border (MarsUI quaternary is borderless in every state). |
| `--ki-icon-button-quaternary-success-rest-fg` | `#2c6a2b` | color used by the icon button component for quaternary success rest foreground — the icon inherits it via currentColor. |
| `--ki-icon-button-secondary-active-overlay` | `rgba(0, 0, 0, 0.05)` | Pressed state layer for the secondary icon button: secondary paints on a light fill, so its wash darkens: the next rung above ki.surface.hover-overlay-inverse (Black/3 -> Black/5 light). Replaces this variant's share of a 45-cell active bg/fg/border matrix whose values were documented mirrors of rest and which WITHDREW the hover overlay rather than deepening it, an appearance legible only to a pointer user who had hovered first. |
| `--ki-icon-button-secondary-active-shadow` | `0 2px 1.5px -0.5px rgba(0, 0, 0, 0.03), inset 0px 2px 3px 0px rgba(255, 255, 255, 0.03)` | shadow used by the icon button component for secondary active shadow (MarsUI Component_effect/secondary_default; MarsUI defines no pressed state, mirrors rest). |
| `--ki-icon-button-secondary-danger-active-bg` | `#ffebed` | color used by the icon button component for secondary danger active background (same semantic reference as the button matrix, 002). |
| `--ki-icon-button-secondary-danger-active-border` | `rgba(0, 0, 0, 0.05)` | color used by the icon button component for secondary danger active border (MarsUI bevel top/inline edge). |
| `--ki-icon-button-secondary-danger-active-fg` | `#cd2118` | color used by the icon button component for secondary danger active foreground — the icon inherits it via currentColor. |
| `--ki-icon-button-secondary-danger-hover-bg` | `#ffebed` | color used by the icon button component for secondary danger hover background (same semantic reference as the button matrix, 002). |
| `--ki-icon-button-secondary-danger-hover-border` | `rgba(0, 0, 0, 0.05)` | color used by the icon button component for secondary danger hover border (MarsUI bevel top/inline edge). |
| `--ki-icon-button-secondary-danger-hover-fg` | `#cd2118` | color used by the icon button component for secondary danger hover foreground — the icon inherits it via currentColor. |
| `--ki-icon-button-secondary-danger-rest-bg` | `#fff3f2` | color used by the icon button component for secondary danger rest background (same semantic reference as the button matrix, 002). |
| `--ki-icon-button-secondary-danger-rest-border` | `rgba(0, 0, 0, 0.05)` | color used by the icon button component for secondary danger rest border (MarsUI bevel top/inline edge). |
| `--ki-icon-button-secondary-danger-rest-fg` | `#cd2118` | color used by the icon button component for secondary danger rest foreground — the icon inherits it via currentColor. |
| `--ki-icon-button-secondary-disabled-shadow` | `none` | value used by the icon button component for secondary disabled shadow (Figma disabled drops every shadow). |
| `--ki-icon-button-secondary-hover-overlay` | `rgba(0, 0, 0, 0.03)` | color used by the icon button component for the secondary hover overlay layer (MarsUI hover_overlay_inverse, identical to the button overlay). |
| `--ki-icon-button-secondary-hover-shadow` | `0 1px 1px -0.5px rgba(0, 0, 0, 0.03), 0 3px 3px -1.5px rgba(0, 0, 0, 0.03), 0 20px 20px -12px rgba(0, 0, 0, 0.03), inset 0px 2px 3px 0px rgba(255, 255, 255, 0.08)` | shadow used by the icon button component for secondary hover shadow (MarsUI Component_effect/secondary_hover). |
| `--ki-icon-button-secondary-neutral-active-bg` | `rgba(242, 242, 244, 0.8)` | color used by the icon button component for secondary neutral active background (same semantic reference as the button matrix, 002). |
| `--ki-icon-button-secondary-neutral-active-border` | `rgba(0, 0, 0, 0.05)` | color used by the icon button component for secondary neutral active border (MarsUI bevel top/inline edge). |
| `--ki-icon-button-secondary-neutral-active-fg` | `#0a0c11` | color used by the icon button component for secondary neutral active foreground — the icon inherits it via currentColor. |
| `--ki-icon-button-secondary-neutral-disabled-bg` | `#ececf0` | color used by the icon button component for secondary disabled background (tone-independent, like the button disabled cells; deliberate 010 D2 deviation from Figma gray.100 for contrast, pending founder ratification). |
| `--ki-icon-button-secondary-neutral-disabled-border` | `rgba(255, 255, 255, 0)` | color used by the icon button component for secondary disabled border (Figma disabled drops the bevel). |
| `--ki-icon-button-secondary-neutral-disabled-fg` | `#5b616d` | color used by the icon button component for secondary disabled foreground (tone-independent; 010 D2 deviation from Figma text/base_em, pending founder ratification). |
| `--ki-icon-button-secondary-neutral-hover-bg` | `rgba(242, 242, 244, 0.8)` | color used by the icon button component for secondary neutral hover background (same semantic reference as the button matrix, 002). |
| `--ki-icon-button-secondary-neutral-hover-border` | `rgba(0, 0, 0, 0.05)` | color used by the icon button component for secondary neutral hover border (MarsUI bevel top/inline edge). |
| `--ki-icon-button-secondary-neutral-hover-fg` | `#0a0c11` | color used by the icon button component for secondary neutral hover foreground — the icon inherits it via currentColor. |
| `--ki-icon-button-secondary-neutral-rest-bg` | `rgba(242, 242, 244, 0.8)` | color used by the icon button component for secondary neutral rest background (same semantic reference as the button matrix, 002). |
| `--ki-icon-button-secondary-neutral-rest-border` | `rgba(0, 0, 0, 0.05)` | color used by the icon button component for secondary neutral rest border (MarsUI bevel top/inline edge). |
| `--ki-icon-button-secondary-neutral-rest-fg` | `#0a0c11` | color used by the icon button component for secondary neutral rest foreground — the icon inherits it via currentColor. |
| `--ki-icon-button-secondary-rest-border-bottom` | `rgba(0, 0, 0, 0.12)` | color used by the icon button component for the secondary bevel bottom edge (MarsUI Outline/secondary_button_bottom, identical to the button bevel). |
| `--ki-icon-button-secondary-rest-shadow` | `0 2px 1.5px -0.5px rgba(0, 0, 0, 0.03), inset 0px 2px 3px 0px rgba(255, 255, 255, 0.03)` | shadow used by the icon button component for secondary rest shadow (MarsUI Component_effect/secondary_default). |
| `--ki-icon-button-secondary-success-active-bg` | `#e3f6e2` | color used by the icon button component for secondary success active background (same semantic reference as the button matrix, 002). |
| `--ki-icon-button-secondary-success-active-border` | `rgba(0, 0, 0, 0.05)` | color used by the icon button component for secondary success active border (MarsUI bevel top/inline edge). |
| `--ki-icon-button-secondary-success-active-fg` | `#2c6a2b` | color used by the icon button component for secondary success active foreground — the icon inherits it via currentColor. |
| `--ki-icon-button-secondary-success-hover-bg` | `#e3f6e2` | color used by the icon button component for secondary success hover background (same semantic reference as the button matrix, 002). |
| `--ki-icon-button-secondary-success-hover-border` | `rgba(0, 0, 0, 0.05)` | color used by the icon button component for secondary success hover border (MarsUI bevel top/inline edge). |
| `--ki-icon-button-secondary-success-hover-fg` | `#2c6a2b` | color used by the icon button component for secondary success hover foreground — the icon inherits it via currentColor. |
| `--ki-icon-button-secondary-success-rest-bg` | `#f4faf3` | color used by the icon button component for secondary success rest background (same semantic reference as the button matrix, 002). |
| `--ki-icon-button-secondary-success-rest-border` | `rgba(0, 0, 0, 0.05)` | color used by the icon button component for secondary success rest border (MarsUI bevel top/inline edge). |
| `--ki-icon-button-secondary-success-rest-fg` | `#2c6a2b` | color used by the icon button component for secondary success rest foreground — the icon inherits it via currentColor. |
| `--ki-icon-button-sm-icon-size` | `1.125rem` | dimension used by the icon button component for sm icon size (MarsUI Placeholder_sm 18px). |
| `--ki-icon-button-sm-radius` | `8px` | dimension used by the icon button component for sm radius (8, same ramp as button). |
| `--ki-icon-button-sm-size` | `2rem` | dimension used by the icon button component for sm box (32px square). |
| `--ki-icon-button-tertiary-active-shadow` | `0 2px 1.5px -0.5px rgba(0, 0, 0, 0.03), inset 0px 2px 3px 0px rgba(255, 255, 255, 0.03)` | shadow used by the icon button component for tertiary active shadow (MarsUI defines no pressed state; mirrors rest). |
| `--ki-icon-button-tertiary-danger-active-bg` | `#f2f2f4` | color used by the icon button component for tertiary danger active background (MarsUI defines no pressed state; one step beyond hover in both schemes). |
| `--ki-icon-button-tertiary-danger-active-border` | `rgba(0, 0, 0, 0.05)` | color used by the icon button component for tertiary danger active border (MarsUI secondary bevel top/inline edge — tertiary carries the secondary_button outline pair). |
| `--ki-icon-button-tertiary-danger-active-fg` | `#cd2118` | color used by the icon button component for tertiary danger active foreground — the icon inherits it via currentColor. |
| `--ki-icon-button-tertiary-danger-hover-bg` | `#f9f9fa` | color used by the icon button component for tertiary danger hover background (one light step over the rest fill; dark keeps the rest fill — the hover signal is Component_effect/secondary_hover). |
| `--ki-icon-button-tertiary-danger-hover-border` | `rgba(0, 0, 0, 0.05)` | color used by the icon button component for tertiary danger hover border (MarsUI secondary bevel top/inline edge — tertiary carries the secondary_button outline pair). |
| `--ki-icon-button-tertiary-danger-hover-fg` | `#cd2118` | color used by the icon button component for tertiary danger hover foreground — the icon inherits it via currentColor. |
| `--ki-icon-button-tertiary-danger-rest-bg` | `#ffffff` | color used by the icon button component for tertiary danger rest background (MarsUI tertiary fill Surface/special/light-s0_dark-s2: White light, Dark/800 dark). |
| `--ki-icon-button-tertiary-danger-rest-border` | `rgba(0, 0, 0, 0.05)` | color used by the icon button component for tertiary danger rest border (MarsUI secondary bevel top/inline edge — tertiary carries the secondary_button outline pair). |
| `--ki-icon-button-tertiary-danger-rest-fg` | `#cd2118` | color used by the icon button component for tertiary danger rest foreground — the icon inherits it via currentColor. |
| `--ki-icon-button-tertiary-disabled-shadow` | `none` | value used by the icon button component for tertiary disabled shadow (Figma disabled drops every shadow). |
| `--ki-icon-button-tertiary-hover-shadow` | `0 1px 1px -0.5px rgba(0, 0, 0, 0.03), 0 3px 3px -1.5px rgba(0, 0, 0, 0.03), 0 20px 20px -12px rgba(0, 0, 0, 0.03), inset 0px 2px 3px 0px rgba(255, 255, 255, 0.08)` | shadow used by the icon button component for tertiary hover shadow (MarsUI Component_effect/secondary_hover). |
| `--ki-icon-button-tertiary-neutral-active-bg` | `#f2f2f4` | color used by the icon button component for tertiary neutral active background (MarsUI defines no pressed state; one step beyond hover in both schemes). |
| `--ki-icon-button-tertiary-neutral-active-border` | `rgba(0, 0, 0, 0.05)` | color used by the icon button component for tertiary neutral active border (MarsUI secondary bevel top/inline edge — tertiary carries the secondary_button outline pair). |
| `--ki-icon-button-tertiary-neutral-active-fg` | `#5b616d` | color used by the icon button component for tertiary neutral active icon color (MarsUI tertiary glyph text/med_em gray — Kimen tertiary maps to Figma tertiary; the purple-glyph row is primary_flat, documented alternative). |
| `--ki-icon-button-tertiary-neutral-disabled-bg` | `#ececf0` | color used by the icon button component for tertiary disabled background (tone-independent, like the button disabled cells; deliberate 010 D2 deviation from Figma gray.100 for contrast, pending founder ratification). |
| `--ki-icon-button-tertiary-neutral-disabled-border` | `rgba(255, 255, 255, 0)` | color used by the icon button component for tertiary disabled border (Figma disabled drops the bevel). |
| `--ki-icon-button-tertiary-neutral-disabled-fg` | `#5b616d` | color used by the icon button component for tertiary disabled foreground (tone-independent; 010 D2 deviation from Figma text/base_em, pending founder ratification). |
| `--ki-icon-button-tertiary-neutral-hover-bg` | `#f9f9fa` | color used by the icon button component for tertiary neutral hover background (one light step over the rest fill; dark keeps the rest fill — the hover signal is Component_effect/secondary_hover). |
| `--ki-icon-button-tertiary-neutral-hover-border` | `rgba(0, 0, 0, 0.05)` | color used by the icon button component for tertiary neutral hover border (MarsUI secondary bevel top/inline edge — tertiary carries the secondary_button outline pair). |
| `--ki-icon-button-tertiary-neutral-hover-fg` | `#5b616d` | color used by the icon button component for tertiary neutral hover icon color (MarsUI tertiary glyph text/med_em gray — Kimen tertiary maps to Figma tertiary; the purple-glyph row is primary_flat, documented alternative). |
| `--ki-icon-button-tertiary-neutral-rest-bg` | `#ffffff` | color used by the icon button component for tertiary neutral rest background (MarsUI tertiary fill Surface/special/light-s0_dark-s2: White light, Dark/800 dark). |
| `--ki-icon-button-tertiary-neutral-rest-border` | `rgba(0, 0, 0, 0.05)` | color used by the icon button component for tertiary neutral rest border (MarsUI secondary bevel top/inline edge — tertiary carries the secondary_button outline pair). |
| `--ki-icon-button-tertiary-neutral-rest-fg` | `#5b616d` | color used by the icon button component for tertiary neutral rest icon color (MarsUI tertiary glyph text/med_em gray — Kimen tertiary maps to Figma tertiary; the purple-glyph row is primary_flat, documented alternative). |
| `--ki-icon-button-tertiary-rest-border-bottom` | `rgba(0, 0, 0, 0.12)` | color used by the icon button component for the tertiary bevel bottom edge (MarsUI Outline/secondary_button_bottom — same bevel pair as secondary). |
| `--ki-icon-button-tertiary-rest-shadow` | `0 2px 1.5px -0.5px rgba(0, 0, 0, 0.03), inset 0px 2px 3px 0px rgba(255, 255, 255, 0.03)` | shadow used by the icon button component for tertiary rest shadow (MarsUI Component_effect/secondary_default — replaces the migrated M2-era stack, which the material3 theme keeps via its own override). |
| `--ki-icon-button-tertiary-success-active-bg` | `#f2f2f4` | color used by the icon button component for tertiary success active background (MarsUI defines no pressed state; one step beyond hover in both schemes). |
| `--ki-icon-button-tertiary-success-active-border` | `rgba(0, 0, 0, 0.05)` | color used by the icon button component for tertiary success active border (MarsUI secondary bevel top/inline edge — tertiary carries the secondary_button outline pair). |
| `--ki-icon-button-tertiary-success-active-fg` | `#2c6a2b` | color used by the icon button component for tertiary success active foreground — the icon inherits it via currentColor. |
| `--ki-icon-button-tertiary-success-hover-bg` | `#f9f9fa` | color used by the icon button component for tertiary success hover background (one light step over the rest fill; dark keeps the rest fill — the hover signal is Component_effect/secondary_hover). |
| `--ki-icon-button-tertiary-success-hover-border` | `rgba(0, 0, 0, 0.05)` | color used by the icon button component for tertiary success hover border (MarsUI secondary bevel top/inline edge — tertiary carries the secondary_button outline pair). |
| `--ki-icon-button-tertiary-success-hover-fg` | `#2c6a2b` | color used by the icon button component for tertiary success hover foreground — the icon inherits it via currentColor. |
| `--ki-icon-button-tertiary-success-rest-bg` | `#ffffff` | color used by the icon button component for tertiary success rest background (MarsUI tertiary fill Surface/special/light-s0_dark-s2: White light, Dark/800 dark). |
| `--ki-icon-button-tertiary-success-rest-border` | `rgba(0, 0, 0, 0.05)` | color used by the icon button component for tertiary success rest border (MarsUI secondary bevel top/inline edge — tertiary carries the secondary_button outline pair). |
| `--ki-icon-button-tertiary-success-rest-fg` | `#2c6a2b` | color used by the icon button component for tertiary success rest foreground — the icon inherits it via currentColor. |
| `--ki-icon-button-xl-icon-size` | `1.75rem` | dimension used by the icon button component for xl icon size (MarsUI Placeholder_xl 28px). |
| `--ki-icon-button-xl-radius` | `14px` | dimension used by the icon button component for xl radius (14, same ramp as button). |
| `--ki-icon-button-xl-size` | `3.5rem` | dimension used by the icon button component for xl box (56px square). |
| `--ki-icon-button-xs-icon-size` | `1rem` | dimension used by the icon button component for xs icon size (MarsUI Icon/Placeholder_xs 16px — one step above the button xs icon). |
| `--ki-icon-button-xs-radius` | `6px` | dimension used by the icon button component for xs radius (Figma Radius/component/radius_xs 6, same ramp as button). |
| `--ki-icon-button-xs-size` | `1.5rem` | dimension used by the icon button component for xs box (24px square, Figma Space/7xl). |

### `ki-indicator`

| Token | Value (onmars light) | Description |
| --- | --- | --- |
| `--ki-indicator-dot-block-size` | `0.5rem` | dimension used by the indicator component for the resting dot block size: the 8px circle diameter measured in the State=inactive vector (the 16px frame height is row alignment, carried by the current dot). |
| `--ki-indicator-dot-color` | `rgba(0, 0, 0, 0.18)` | color used by the indicator component for the resting dot fill: Inverse_white/alpha_18, the exact variable bound to every Indicator variant fill (Black/18 light, White/18 dark) — the scheme flip rides the semantic layer. |
| `--ki-indicator-dot-current-block-size` | `1rem` | dimension used by the indicator component for the current dot block size: Space/3xl, the exact variable bound as the 16px height of the State=active pill (the full row line; declared family extension, design-extraction §Decisiones). |
| `--ki-indicator-dot-current-color` | `rgba(0, 0, 0, 0.18)` | color used by the indicator component for the current dot treatment: Inverse_white/alpha_18, the exact fill of the State=active ring vector (Black/18 light, White/18 dark) — painted as the pill ring in onmars, as the solid dot in themes that zero the hole. |
| `--ki-indicator-dot-current-hole-block-size` | `0.375rem` | dimension used by the indicator component for the block size of the transparent hole cut from the current pill: the 6px cutout height measured in the State=active vector; the ring width derives by construction as (current block size − hole block size) / 2 and a theme resolves it to zero for a solid dot (declared family extension, design-extraction §Decisiones). |
| `--ki-indicator-dot-current-inline-size` | `2rem` | dimension used by the indicator component for the current dot inline size: Space/10xl, the exact variable bound as the 32px width of the State=active pill — the MarsUI elongation lives here, never in an attribute (FR-007). |
| `--ki-indicator-dot-current-radius` | `1000px` | dimension used by the indicator component for the current dot shape: Radius/radius_round bound in Figma — the pill is a radius token, never an attribute (002 shape precedent). |
| `--ki-indicator-dot-inline-size` | `0.5rem` | dimension used by the indicator component for the resting dot inline size: Space/md, the exact variable bound as the 8px width of the State=inactive symbol; the painted circle is 8x8 centered in the 16px row line. |
| `--ki-indicator-dot-radius` | `1000px` | dimension used by the indicator component for the resting dot shape: Radius/radius_round bound in Figma — the round dot is a radius token, never an attribute (002 shape precedent). |
| `--ki-indicator-gap` | `0.375rem` | dimension used by the indicator component for the space between dots: Space/sm, the exact variable bound as the 6px gap of the composed Nav_indicator row (node 14195:5866, specs/024-ki-indicator/design-extraction.md). |
| `--ki-indicator-motion-duration` | `120ms` | duration used by the indicator component for the highlight transfer between dots: the Figma set documents no motion, so onmars resolves the single transition to the fast semantic duration (FR-008; disabled under reduced motion by construction). |
| `--ki-indicator-motion-easing` | `cubic-bezier(0.25, 0.1, 0.25, 1)` | easing used by the indicator component for the highlight-transfer transition (MarsUI standard curve; previously the UA default ease applied). |

### `ki-input`

| Token | Value (onmars light) | Description |
| --- | --- | --- |
| `--ki-input-border-block-end-width` | `1px` | dimension used by the input component for border block end width. |
| `--ki-input-border-block-start-width` | `1px` | dimension used by the input component for border block start width. |
| `--ki-input-border-inline-end-width` | `1px` | dimension used by the input component for border inline end width. |
| `--ki-input-border-inline-start-width` | `1px` | dimension used by the input component for border inline start width. |
| `--ki-input-border-width` | `1px` | dimension used by the input component for border width (MarsUI Input_cell stroke: 1px hairline; the border color stays ki.outline.control per the 2026-07-25 declared a11y deviation). |
| `--ki-input-disabled-bg` | `#f2f2f4` | color used by the input component for disabled background. |
| `--ki-input-disabled-border` | `rgba(0, 0, 0, 0.05)` | color used by the input component for disabled border. |
| `--ki-input-disabled-fg` | `#c3c6cc` | color used by the input component for disabled foreground (MarsUI disabled text Text/base_em; disabled cells are exempt from the contrast sweep). |
| `--ki-input-disabled-hint-fg` | `#c3c6cc` | color used by the input component for the disabled hint row foreground (MarsUI disabled ladder Text/base_em; disabled cells are exempt from the contrast sweep). |
| `--ki-input-disabled-label-fg` | `#c3c6cc` | color used by the input component for disabled label foreground. |
| `--ki-input-disabled-placeholder-fg` | `#c3c6cc` | color used by the input component for disabled placeholder foreground. |
| `--ki-input-focus-bg` | `#ffffff` | color used by the input component for focus background (MarsUI active state switches the fill to Surface/Special white / dark s2 — modeled with the scheme ladder s0). |
| `--ki-input-focus-border` | `#845abe` | color used by the input component for focus border. |
| `--ki-input-focus-fg` | `#0a0c11` | color used by the input component for focus foreground. |
| `--ki-input-focus-label-fg` | `#5b616d` | color used by the input component for focus label foreground (MarsUI label is Text/med_em in every non-disabled state — active and danger included; the danger signal lives in the hint). |
| `--ki-input-focus-placeholder-fg` | `#8c929c` | color used by the input component for focus placeholder foreground (MarsUI placeholder Text/low_em; placeholder is not the accessible name, so it sits outside the 4.5:1 text sweep). |
| `--ki-input-focus-ring-color` | `#845abe` | color used by the input component for focus ring color. |
| `--ki-input-focus-ring-offset` | `0.125rem` | dimension used by the input component for focus ring offset. |
| `--ki-input-focus-ring-shadow` | `0 0 0 3px rgba(132, 90, 190, 0.2)` | shadow used by the input component for the focus ring glow (MarsUI Focus/primary: 3px spread ring at 20% — see ki.focus.primary. specs/002-ki-button/design-extraction.md §2.5 recorded 40%, having read the alpha off the Dark canvas mode, same double mechanism as ki-button: glow plus the opaque indicator). |
| `--ki-input-focus-ring-width` | `0.125rem` | dimension used by the input component for focus ring width. |
| `--ki-input-font-size` | `0.8125rem` | dimension used by the input component for font size. |
| `--ki-input-font-weight` | `500` | font weight used by the input component for font weight. |
| `--ki-input-gap` | `0.25rem` | dimension used by the input component for gap. |
| `--ki-input-height` | `2.5rem` | dimension used by the input component for height (MarsUI Input_field md cell: min-height 40px; consume as min-block-size so the field can grow). |
| `--ki-input-hint-fg` | `#5b616d` | color used by the input component for the hint row foreground (MarsUI Hint_label Text/med_em; the hint row itself is pending its founder-gated API addition). |
| `--ki-input-hover-bg` | `#ececf0` | color used by the input component for hover background (MarsUI hover = s2 + Black/3 overlay; s3 is the nearest token). |
| `--ki-input-hover-border` | `rgba(0, 0, 0, 0.56)` | color used by the input component for hover border. |
| `--ki-input-hover-fg` | `#0a0c11` | color used by the input component for hover foreground. |
| `--ki-input-hover-label-fg` | `#5b616d` | color used by the input component for hover label foreground (MarsUI label is Text/med_em in every non-disabled state — active and danger included; the danger signal lives in the hint). |
| `--ki-input-hover-placeholder-fg` | `#8c929c` | color used by the input component for hover placeholder foreground (MarsUI placeholder Text/low_em; placeholder is not the accessible name, so it sits outside the 4.5:1 text sweep). |
| `--ki-input-icon-size` | `1.125rem` | dimension used by the input component for icon size. |
| `--ki-input-invalid-bg` | `#ffffff` | color used by the input component for invalid background (MarsUI danger state keeps the white active fill; the ring and hint carry the signal). |
| `--ki-input-invalid-border` | `#f05149` | color used by the input component for invalid border. |
| `--ki-input-invalid-fg` | `#0a0c11` | color used by the input component for invalid foreground. |
| `--ki-input-invalid-hint-fg` | `#cd2118` | color used by the input component for the invalid hint row foreground (MarsUI Hint_label danger: Text/danger_high_em carries the signal). |
| `--ki-input-invalid-label-fg` | `#5b616d` | color used by the input component for invalid label foreground (MarsUI label is Text/med_em in every non-disabled state — active and danger included; the danger signal lives in the hint). |
| `--ki-input-invalid-placeholder-fg` | `#8c929c` | color used by the input component for invalid placeholder foreground (MarsUI placeholder Text/low_em; placeholder is not the accessible name, so it sits outside the 4.5:1 text sweep). |
| `--ki-input-invalid-ring` | `0 0 0 3px rgba(240, 81, 73, 0.2)` | shadow used by the input component for the persistent invalid ring (MarsUI Focus/danger: 3px danger-500 alpha spread; applied as box-shadow while :state(user-invalid)). |
| `--ki-input-label-font-size` | `0.8125rem` | dimension used by the input component for label font size (MarsUI UI/Para/medium: body_1 13px). |
| `--ki-input-label-font-weight` | `600` | font weight used by the input component for label font weight. |
| `--ki-input-label-gap` | `0.25rem` | dimension used by the input component for label gap. |
| `--ki-input-label-line-height` | `1.25rem` | dimension used by the input component for label line height (MarsUI line_height/para 20px). |
| `--ki-input-label-padding-inline` | `0.125rem` | dimension used by the input component for label padding inline (MarsUI Input_label row 2px inset aligning the label ink with the cell radius). |
| `--ki-input-line-height` | `1.5rem` | dimension used by the input component for line height. |
| `--ki-input-min-target` | `1.5rem` | dimension used by the input component for min target. |
| `--ki-input-padding-inline` | `0.625rem` | dimension used by the input component for padding inline. |
| `--ki-input-radius` | `10px` | dimension used by the input component for radius (MarsUI Radius/component/radius_md 10px, the md Input_cell binding; component ramp so personality modes reach it). |
| `--ki-input-readonly-bg` | `#f9f9fa` | color used by the input component for readonly background (retires the deprecated ki.surface.raised alias; same resolved value). |
| `--ki-input-readonly-border` | `rgba(0, 0, 0, 0.48)` | color used by the input component for readonly border. |
| `--ki-input-readonly-fg` | `#0a0c11` | color used by the input component for readonly foreground. |
| `--ki-input-readonly-label-fg` | `#5b616d` | color used by the input component for readonly label foreground (MarsUI label is Text/med_em in every non-disabled state — active and danger included; the danger signal lives in the hint). |
| `--ki-input-readonly-placeholder-fg` | `#8c929c` | color used by the input component for readonly placeholder foreground (MarsUI placeholder Text/low_em; placeholder is not the accessible name, so it sits outside the 4.5:1 text sweep). |
| `--ki-input-rest-bg` | `#f2f2f4` | color used by the input component for rest background (MarsUI Input_field rest fill Surface/s2: the fill identifies the field, not the outline). |
| `--ki-input-rest-border` | `rgba(0, 0, 0, 0.48)` | color used by the input component for rest border. |
| `--ki-input-rest-fg` | `#0a0c11` | color used by the input component for rest foreground. |
| `--ki-input-rest-label-fg` | `#5b616d` | color used by the input component for rest label foreground (MarsUI label is Text/med_em in every non-disabled state — active and danger included; the danger signal lives in the hint). |
| `--ki-input-rest-placeholder-fg` | `#8c929c` | color used by the input component for rest placeholder foreground (MarsUI placeholder Text/low_em; placeholder is not the accessible name, so it sits outside the 4.5:1 text sweep). |

### `ki-list`

| Token | Value (onmars light) | Description |
| --- | --- | --- |
| `--ki-list-bg` | `#ffffff` | color used by the list component for background. |
| `--ki-list-gap` | `0rem` | dimension used by the list component for gap (MarsUI compositions stack rows contiguously; --ki-list-gap stays as the theme hook). |
| `--ki-list-item-divider-color` | `rgba(0, 0, 0, 0.03)` | color used by the list component for item divider color. |
| `--ki-list-item-divider-width` | `0rem` | dimension used by the list component for item divider width (no divider renders in any master variant; the color hook ki.list.item.divider-color stays so consumers can opt hairlines back in). |
| `--ki-list-item-gap` | `0.75rem` | dimension used by the list component for item gap (MarsUI md/lg rows: Space/xl 12px between start/content/end regions). |
| `--ki-list-item-min-height` | `2.5rem` | dimension used by the list component for item min height (MarsUI md text-only row is ~40px content-sized: 20px line + 2x10 padding). |
| `--ki-list-item-min-height-multiline` | `3.5rem` | dimension used by the list component for multiline item min height (MarsUI md two-line row measures 56px). |
| `--ki-list-item-padding-block` | `0.625rem` | dimension used by the list component for item padding block (MarsUI md rows: Space/lg 10px). |
| `--ki-list-item-padding-inline` | `0.5rem` | dimension used by the list component for item padding inline. |
| `--ki-list-item-primary-fg` | `#0a0c11` | color used by the list component for item primary foreground. |
| `--ki-list-item-primary-font-size` | `0.8125rem` | dimension used by the list component for item primary font size. |
| `--ki-list-item-primary-font-weight` | `600` | font weight used by the list component for item primary font weight. |
| `--ki-list-item-primary-line-height` | `1.25rem` | dimension used by the list component for primary line height (MarsUI primary is UI/Para/medium 13/20, not UI/Body 1 13/24). |
| `--ki-list-item-radius` | `0px` | dimension used by the list component for item radius. |
| `--ki-list-item-secondary-fg` | `#5b616d` | color used by the list component for item secondary foreground. |
| `--ki-list-item-secondary-font-size` | `0.75rem` | dimension used by the list component for secondary font size (MarsUI description line is UI/Caption 2: 12px). |
| `--ki-list-item-secondary-font-weight` | `500` | font weight used by the list component for item secondary font weight. |
| `--ki-list-item-secondary-line-height` | `1rem` | dimension used by the list component for secondary line height (MarsUI caption_2 16px; same value as before with the correct provenance). |
| `--ki-list-padding` | `0rem` | dimension used by the list component for padding. |

### `ki-option`

| Token | Value (onmars light) | Description |
| --- | --- | --- |
| `--ki-option-disabled-bg` | `#f2f2f4` | color used by the option component for disabled background. |
| `--ki-option-disabled-fg` | `#8c929c` | color used by the option component for disabled foreground. |
| `--ki-option-font-size` | `0.8125rem` | dimension used by the option component for font size. |
| `--ki-option-highlight-bg` | `rgba(0, 0, 0, 0.03)` | color used by the option component for keyboard-highlight background (MarsUI active row = same Black/3 wash plus Elevation/e1; purple is reserved for the selected tint). |
| `--ki-option-highlight-fg` | `#0a0c11` | color used by the option component for highlight foreground. |
| `--ki-option-hover-bg` | `rgba(0, 0, 0, 0.03)` | color used by the option component for hover background (MarsUI hover_overlay_inverse Black/3 wash over the glass panel — not a solid surface step). |
| `--ki-option-hover-fg` | `#0a0c11` | color used by the option component for hover foreground. |
| `--ki-option-min-target` | `2.25rem` | dimension used by the option component for min target (MarsUI Dropmenu row height Space/11xl 36px; comfortably above the 24px a11y floor). |
| `--ki-option-padding-inline` | `0.5rem` | dimension used by the option component for padding inline. |
| `--ki-option-radius` | `12px` | dimension used by the option component for radius (MarsUI Radius/component/radius_lg 12px bound on Dropmenu rows). |
| `--ki-option-rest-bg` | `rgba(255, 255, 255, 0)` | color used by the option component for rest background (MarsUI Dropmenu rows are transparent over the glass panel; only hover/selected paint). |
| `--ki-option-rest-fg` | `#0a0c11` | color used by the option component for rest foreground. |
| `--ki-option-selected-bg` | `#ece4f5` | color used by the option component for selected background. |
| `--ki-option-selected-fg` | `#0a0c11` | color used by the option component for selected foreground. |

### `ki-progress`

| Token | Value (onmars light) | Description |
| --- | --- | --- |
| `--ki-progress-circular-size` | `2.5rem` | dimension used by the progress component for circular size. |
| `--ki-progress-circular-track-width` | `0.25rem` | dimension used by the progress component for circular track width. |
| `--ki-progress-indeterminate-duration` | `1600ms` | duration used by the progress component for progress indeterminate duration. |
| `--ki-progress-indicator-color` | `#845abe` | MarsUI fills components with the med-em (500) tone of the ramp (marsui-design-dna.md §1; specs/002-ki-button/design-extraction.md §§2.1-2.4 delta 500-vs-high-em): progress indicator uses surface.primary-med-em. |
| `--ki-progress-linear-radius` | `1000px` | dimension used by the progress component for linear radius. |
| `--ki-progress-linear-thickness` | `0.25rem` | dimension used by the progress component for linear thickness. |
| `--ki-progress-track-color` | `#ececf0` | color used by the progress component for track color. |

### `ki-qr`

| Token | Value (onmars light) | Description |
| --- | --- | --- |
| `--ki-qr-background` | `#ffffff` | color used by the qr component for the light tile behind the modules: the Figma symbol is transparent over the showcase card's Surface/s0 white, but a scannable code owns its tile — the scheme-stable white keeps dark-on-light module contrast in both schemes (FR-010, a documented theme obligation). |
| `--ki-qr-color` | `#0a0c11` | color used by the qr component for module/finder ink (the master binds Text/high_em, whose light-scheme hue is Gray/950 #0a0c11; referenced as the scheme-stable primitive because FR-010 keeps QR ink from flipping in dark — 18.9:1 on white). |
| `--ki-qr-finder-radius` | `0px` | dimension used by the qr component for the corner radius of the three finder rings, applied to the ring's midline in the code's internal coordinate space of 8px per module: Type=square renders square concentric finders (radius 0); the documented round value is 14px (14px midline = 18px outer edge, the 2.25-module radius measured on Type=round); the inner-edge and center radii derive by construction (stroke geometry and a 3/7 scale — design-extraction §5). |
| `--ki-qr-module-radius` | `0px` | dimension used by the qr component for the corner radius of every data module, in the code's internal coordinate space of 8px per module: Type=square (the set default) renders pure squares (radius 0); the documented round value is 4px (4px = half a module, the MarsUI Type=round dot carried as a token value, never an attribute — FR-007, 002 shape precedent). |
| `--ki-qr-quiet-zone` | `0.5rem` | dimension used by the qr component for the light quiet zone around the modules: the 8px band measured on both 128px variants (about two modules of the sample matrices), painted as tile padding and preserved at any token-driven size (FR-008). |
| `--ki-qr-radius` | `0px` | dimension used by the qr component for the tile corners: the QR_code frames are square with no own radius (the rounded showcase card beneath is framing, not anatomy — design-extraction §1). |
| `--ki-qr-size` | `8rem` | dimension used by the qr component for size (MarsUI 128x128 symbol frame via the dedicated ki.size.qr surface size; replaces the 120px space-scale approximation). |

### `ki-radio`

| Token | Value (onmars light) | Description |
| --- | --- | --- |
| `--ki-radio-border-width` | `0.125rem` | dimension used by the radio component for border width. |
| `--ki-radio-control-radius` | `1000px` | dimension used by the radio component for control radius. |
| `--ki-radio-control-size` | `1.25rem` | dimension used by the radio component for control size. |
| `--ki-radio-dot-shadow` | `0 2px 3px -1.5px rgba(0, 0, 0, 0.32)` | shadow used by the radio component for the selected dot (MarsUI Small_dark_shadow: drop 0/2/3/-1.5 Elevation/shadow_dark, the lifted-dot look of the master). |
| `--ki-radio-dot-size` | `0.5rem` | dimension used by the radio component for dot size. |
| `--ki-radio-focus-ring-color` | `#845abe` | color used by the radio component for focus ring color. |
| `--ki-radio-focus-ring-offset` | `0.125rem` | dimension used by the radio component for focus ring offset. |
| `--ki-radio-focus-ring-shadow` | `0 0 0 3px rgba(132, 90, 190, 0.2)` | shadow used by the radio component for the focus ring glow (MarsUI Focus/primary: 3px spread ring at 20% — see ki.focus.primary. specs/002-ki-button/design-extraction.md §2.5 recorded 40%, having read the alpha off the Dark canvas mode, same double mechanism as ki-button: glow plus the opaque indicator). |
| `--ki-radio-focus-ring-width` | `0.125rem` | dimension used by the radio component for focus ring width. |
| `--ki-radio-gap` | `0.75rem` | dimension used by the radio component for gap (MarsUI md label pairing: Space/xl 12px for the 20px circle / 24px target geometry). |
| `--ki-radio-label-font-size` | `0.9375rem` | dimension used by the radio component for label font size (MarsUI md pairing: body_2 15px; line-height stays 24 and weight 500). |
| `--ki-radio-label-font-weight` | `500` | font weight used by the radio component for label font weight. |
| `--ki-radio-label-line-height` | `1.5rem` | dimension used by the radio component for label line height. |
| `--ki-radio-min-target` | `1.5rem` | dimension used by the radio component for min target. |
| `--ki-radio-selected-active-bg` | `#845abe` | color used by the radio component for selected active background (MarsUI defines no pressed state; mirrors rest). |
| `--ki-radio-selected-active-border` | `#845abe` | color used by the radio component for selected active border (same as the fill: borderless look). |
| `--ki-radio-selected-active-fg` | `#ffffff` | color used by the radio component for selected active foreground. |
| `--ki-radio-selected-active-overlay` | `rgba(255, 255, 255, 0.08)` | color used by the radio component for the selected pressed wash (ki-button active/overlay precedent). MarsUI defines no pressed fill and the brand ramp has no darker rung that survives the dark scheme, so the press composites the pressed state layer over the rest fill — a keyboard hold shows a real paint delta even under reduced motion, where the transform compress is gated off (bare-page state-delta contract). |
| `--ki-radio-selected-disabled-bg` | `#dddfe4` | color used by the radio component for selected disabled background (MarsUI Surface/disabled_med_em Gray/300). |
| `--ki-radio-selected-disabled-border` | `#dddfe4` | color used by the radio component for selected disabled border (same as the fill: borderless look). |
| `--ki-radio-selected-disabled-fg` | `#f2f2f4` | color used by the radio component for selected disabled dot (MarsUI Surface/disabled_base_em Gray/100 — a light dot one quiet value step above the gray donut, not a dark dot). |
| `--ki-radio-selected-hover-bg` | `#5f3993` | color used by the radio component for selected hover background (the master has no selected-hover variant; mirrors ki-checkbox checked hover: a real delta in both schemes). |
| `--ki-radio-selected-hover-border` | `#5f3993` | color used by the radio component for selected hover border (same as the fill: borderless look). |
| `--ki-radio-selected-hover-fg` | `#ffffff` | color used by the radio component for selected hover foreground. |
| `--ki-radio-selected-rest-bg` | `#845abe` | color used by the radio component for selected rest background (MarsUI active radio is a solid Surface/primary_med_em #845abe circle — the filled donut with a white dot is the signature, mirroring ki-checkbox checked). |
| `--ki-radio-selected-rest-border` | `#845abe` | color used by the radio component for selected rest border (same as the fill: the selected circle is borderless in the master). |
| `--ki-radio-selected-rest-fg` | `#ffffff` | color used by the radio component for selected rest dot (MarsUI Text/primary_on_primary white 8px dot). |
| `--ki-radio-unselected-active-bg` | `#f2f2f4` | color used by the radio component for unselected active background. Brief §1: neutral surface em ramp one step above hover (s1→s2), matching ki-checkbox unchecked active; the primary-tinted alpha stays in radio.material3. |
| `--ki-radio-unselected-active-border` | `#845abe` | color used by the radio component for unselected active border. |
| `--ki-radio-unselected-active-fg` | `#5f3993` | color used by the radio component for unselected active foreground. |
| `--ki-radio-unselected-disabled-bg` | `#dddfe4` | color used by the radio component for unselected disabled background (MarsUI Surface/disabled_med_em Gray/300). |
| `--ki-radio-unselected-disabled-border` | `#dddfe4` | color used by the radio component for unselected disabled border (same as the fill: borderless look in the master). |
| `--ki-radio-unselected-disabled-fg` | `#c3c6cc` | color used by the radio component for unselected disabled dot color (dot is hidden unselected; kept coherent with the disabled ladder, retiring the deprecated ki.text.muted alias). |
| `--ki-radio-unselected-hover-bg` | `#f9f9fa` | color used by the radio component for unselected hover background. Brief §1: onmars hover is neutral (hover_overlay / surface em ramp), matching ki-checkbox unchecked hover (s1); the former primary-tinted alpha is the M3 state-layer pattern and stays in radio.material3. |
| `--ki-radio-unselected-hover-border` | `#845abe` | color used by the radio component for unselected hover border. |
| `--ki-radio-unselected-hover-fg` | `#5f3993` | color used by the radio component for unselected hover foreground. |
| `--ki-radio-unselected-rest-bg` | `#ffffff` | color used by the radio component for unselected rest background. |
| `--ki-radio-unselected-rest-border` | `rgba(0, 0, 0, 0.48)` | color used by the radio component for unselected rest border. |
| `--ki-radio-unselected-rest-fg` | `#5f3993` | color used by the radio component for unselected rest foreground. |

### `ki-radio-group`

| Token | Value (onmars light) | Description |
| --- | --- | --- |
| `--ki-radio-group-gap` | `0.375rem` | dimension used by the radio group component for gap. |
| `--ki-radio-group-label-font-size` | `0.8125rem` | dimension used by the radio group component for label font size. |
| `--ki-radio-group-label-font-weight` | `600` | font weight used by the radio group component for label font weight. |
| `--ki-radio-group-label-line-height` | `1.25rem` | dimension used by the radio group component for label line height (closest measured analogue: Radio_label sm 13/20; no group-title master exists — low-confidence refinement). |

### `ki-scroller`

| Token | Value (onmars light) | Description |
| --- | --- | --- |
| `--ki-scroller-focus-ring-color` | `#845abe` | color used by the scroller component for the viewport focus ring: the keyboard-focusable scroll region (FR-005) adopts the ki-dialog focus-ring pair verbatim. |
| `--ki-scroller-focus-ring-offset` | `0.125rem` | dimension used by the scroller component for the viewport focus ring offset (ki-dialog precedent). |
| `--ki-scroller-focus-ring-shadow` | `0 0 0 3px rgba(132, 90, 190, 0.2)` | shadow used by the scroller component for the viewport focus ring glow (MarsUI Focus/primary 3px 20%-alpha halo, the ki-button double mechanism; completes the theming hook ki-scroller.css already consumes). |
| `--ki-scroller-focus-ring-width` | `0.125rem` | dimension used by the scroller component for the viewport focus ring width (ki-dialog precedent). |
| `--ki-scroller-gutter` | `0.125rem` | dimension used by the scroller component for the clearance on each side of the thumb: the 2px padding-inline of the 8px Scroller rail (rail thickness = thumb-thickness + 2×gutter). |
| `--ki-scroller-surface` | `rgba(255, 255, 255, 0)` | color used by the scroller component for the viewport surface: transparent in onmars — the scroller shows the consumer's own surface; a theme may resolve a proper surface here (FR-007). |
| `--ki-scroller-thumb-color` | `rgba(0, 0, 0, 0.05)` | color used by the scroller component for the indicator thumb: Inverse_white/alpha_6, the exact variable bound to the scroll pill on both Scroller variants (Black/5 light, White/5 dark) — the scheme flip rides the semantic layer (specs/023-ki-scroller/design-extraction.md). |
| `--ki-scroller-thumb-min-length` | `1rem` | dimension used by the scroller component for the minimum thumb length along the scroll axis: the 16px min-height on the Figma scroll pill; the actual length stays the native visible-fraction proportion (FR-003). |
| `--ki-scroller-thumb-radius` | `1000px` | dimension used by the scroller component for the thumb shape: Radius/radius_round bound in Figma — the pill is a radius token, never an attribute (002 shape precedent). |
| `--ki-scroller-thumb-thickness` | `0.25rem` | dimension used by the scroller component for the thumb cross size: the 4px pill measured inside the 8px MarsUI rail (rail = thumb + 2×gutter, verified 2026-07-17). |
| `--ki-scroller-track-color` | `rgba(255, 255, 255, 0)` | color used by the scroller component for the track: the MarsUI pill floats directly on the content surface with no visible track — transparent in onmars; a theme opts into a visible track through this token, never an attribute. |
| `--ki-scroller-track-inset` | `0.25rem` | dimension used by the scroller component for the rail end insets: Space/xs bound as the 4px padding along the scroll axis of both Scroller variants (design-extraction §Decisiones 5). |

### `ki-select`

| Token | Value (onmars light) | Description |
| --- | --- | --- |
| `--ki-select-border-block-end-width` | `1px` | dimension used by the select component for border block end width. |
| `--ki-select-border-block-start-width` | `1px` | dimension used by the select component for border block start width. |
| `--ki-select-border-inline-end-width` | `1px` | dimension used by the select component for border inline end width. |
| `--ki-select-border-inline-start-width` | `1px` | dimension used by the select component for border inline start width. |
| `--ki-select-border-width` | `1px` | dimension used by the select component for border width (MarsUI Input_cell strokeWeight 1px; the color stays ki.outline.control per the declared a11y deviation — only the 2px width was undocumented). |
| `--ki-select-disabled-bg` | `#f2f2f4` | color used by the select component for disabled background. |
| `--ki-select-disabled-border` | `rgba(0, 0, 0, 0.03)` | color used by the select component for disabled border (MarsUI Outline/base_em Black/3). |
| `--ki-select-disabled-fg` | `#c3c6cc` | color used by the select component for disabled foreground (MarsUI disabled text Text/base_em Gray/400; disabled cells are exempt from the contrast sweep). |
| `--ki-select-disabled-label-fg` | `#c3c6cc` | color used by the select component for disabled label foreground. |
| `--ki-select-focus-bg` | `#ffffff` | color used by the select component for focus background (MarsUI Dropdown focus fill Surface/Special/light-s0_dark-s4). |
| `--ki-select-focus-border` | `rgba(0, 0, 0, 0.48)` | color used by the select component for focus border (MarsUI active is glow-only: the border stays the rest hairline while Focus/primary carries the emphasis — repointed from ki.outline.primary-high-em per the fidelity audit; material3 keeps the opaque primary border via its override). |
| `--ki-select-focus-fg` | `#0a0c11` | color used by the select component for focus foreground. |
| `--ki-select-focus-label-fg` | `#0a0c11` | color used by the select component for focus label foreground. |
| `--ki-select-focus-ring-color` | `#845abe` | color used by the select component for focus ring color. |
| `--ki-select-focus-ring-offset` | `0.125rem` | dimension used by the select component for focus ring offset. |
| `--ki-select-focus-ring-shadow` | `0 0 0 3px rgba(132, 90, 190, 0.2)` | shadow used by the select component for the focus ring glow (MarsUI Focus/primary: 3px spread ring at 20% — see ki.focus.primary. specs/002-ki-button/design-extraction.md §2.5 recorded 40%, having read the alpha off the Dark canvas mode, same double mechanism as ki-button: glow plus the opaque indicator). |
| `--ki-select-focus-ring-width` | `0.125rem` | dimension used by the select component for focus ring width. |
| `--ki-select-font-size` | `0.8125rem` | dimension used by the select component for font size. |
| `--ki-select-font-weight` | `600` | font weight used by the select component for the trigger value (MarsUI UI/Body 1/medium: weight 600). |
| `--ki-select-gap` | `0.25rem` | dimension used by the select component for gap. |
| `--ki-select-height` | `2.5rem` | dimension used by the select component for height. |
| `--ki-select-hover-bg` | `#f2f2f4` | color used by the select component for hover background (MarsUI hover = rest fill + hover_overlay_inverse; nearest cross-scheme step that keeps the ramp monotonic in dark). |
| `--ki-select-hover-border` | `rgba(0, 0, 0, 0.56)` | color used by the select component for hover border. Brief §1: hover moves along the neutral outline em ramp (Black/12), matching ki-input/ki-textarea; the primary accent outline is the focus language (Focus/primary), not hover. |
| `--ki-select-hover-fg` | `#0a0c11` | color used by the select component for hover foreground. |
| `--ki-select-hover-label-fg` | `#0a0c11` | color used by the select component for hover label foreground. |
| `--ki-select-indicator-glyph-size` | `0.5rem` | dimension used by the select component for the chevron glyph box inside the 18px indicator slot (MarsUI Dropdown chevron 8px border-box square drawn by [part=indicator]::before). |
| `--ki-select-indicator-size` | `1.125rem` | dimension used by the select component for indicator size. |
| `--ki-select-label-font-size` | `0.8125rem` | dimension used by the select component for label font size (MarsUI UI/Para/medium: body_1 13px). |
| `--ki-select-label-font-weight` | `600` | font weight used by the select component for label font weight. |
| `--ki-select-label-gap` | `0.25rem` | dimension used by the select component for label gap. |
| `--ki-select-label-line-height` | `1.25rem` | dimension used by the select component for label line height (MarsUI line_height/para 20px). |
| `--ki-select-label-padding-inline` | `0.125rem` | dimension used by the select component for label padding inline (MarsUI Input_label row 2px inset aligning the label ink with the cell radius; matches ki-input/ki-textarea). |
| `--ki-select-line-height` | `1.5rem` | dimension used by the select component for the trigger value line height (MarsUI body_1 24px). |
| `--ki-select-listbox-backdrop-blur` | `24px` | backdrop blur used by the select component listbox glass panel (MarsUI BACKGROUND_BLUR Blur/48 = CSS 24px; material3 resolves to blur none via its override). |
| `--ki-select-listbox-bg` | `rgba(255, 255, 255, 0.8)` | color used by the select component for listbox background (MarsUI Dropmenu glass panel Surface/Special/blur_base_light White/80; pair with backdrop-filter blur(var(--ki-select-listbox-backdrop-blur))). |
| `--ki-select-listbox-elevation` | `rgba(0, 0, 0, 0.05)` | color used by the select component for listbox elevation. Legacy of the pre-ramp hand-composed listbox shadow, superseded by ki.select.listbox.shadow; kept published because the public-api contract requires deprecation metadata at least one minor before removal. |
| `--ki-select-listbox-max-block-size` | `20rem` | dimension used by the select component for listbox max block size (surface size scale; the former ki.space.25xl reference misused the spacing scale). |
| `--ki-select-listbox-offset` | `0.25rem` | dimension used by the select component for listbox offset. |
| `--ki-select-listbox-padding` | `0.75rem` | dimension used by the select component for listbox padding (MarsUI Dropmenu content inset Space/xl 12px). |
| `--ki-select-listbox-radius` | `20px` | dimension used by the select component for listbox radius (MarsUI Radius/big_component/radius_sm 20px bound on the Dropmenu panel). |
| `--ki-select-listbox-shadow` | `0 1px 1px -0.5px rgba(0, 0, 0, 0.03), 0 3px 3px -1.5px rgba(0, 0, 0, 0.03), 0 20px 20px -12px rgba(0, 0, 0, 0.03)` | shadow used by the select component for the listbox: MarsUI elevation ramp e3 (brief §5), the medium floating-surface level, replacing the former hand-composed offset/color shadow. Declared level: e3 (M3 analog: menu/dialog band; material3 overrides to e2 = md.sys.elevation.level2 for menus). |
| `--ki-select-min-target` | `1.5rem` | dimension used by the select component for min target. |
| `--ki-select-padding-inline` | `0.625rem` | dimension used by the select component for padding inline. |
| `--ki-select-placeholder-fg` | `#5b616d` | color used by the select component for placeholder foreground. |
| `--ki-select-radius` | `10px` | dimension used by the select component for radius (MarsUI Radius/component/radius_md 10px; component ramp so personality modes reach it). |
| `--ki-select-rest-bg` | `#ffffff` | color used by the select component for rest background (MarsUI Dropdown rest fill Surface/Special/light-s0_dark-s2: white light, Dark/800 dark). |
| `--ki-select-rest-border` | `rgba(0, 0, 0, 0.48)` | color used by the select component for rest border. |
| `--ki-select-rest-fg` | `#0a0c11` | color used by the select component for rest foreground. |
| `--ki-select-rest-label-fg` | `#5b616d` | color used by the select component for rest label foreground. |

### `ki-status`

| Token | Value (onmars light) | Description |
| --- | --- | --- |
| `--ki-status-danger-color` | `#f05149` | color used by the status component for the danger tone fill: Surface/danger_med_em, the exact variable bound to the Type=danger Status variant (Danger/500). |
| `--ki-status-info-color` | `#13b9ec` | color used by the status component for the info tone fill: no Figma Status variant ships an info Type, so the fill extrapolates the verified med-em 500 pattern to the Info ramp (001 contract, 010/015 precedent) — declared for founder ratification. |
| `--ki-status-neutral-color` | `#afb3bb` | color used by the status component for the neutral tone fill: Surface/disabled_high_em, the exact variable bound to the Type=disabled Status variant (Gray/500 light, White/32 dark) — MarsUI's disabled gray maps to the charter neutral tone (spec Assumptions). |
| `--ki-status-radius` | `1000px` | dimension used by the status component for the dot shape: Radius/radius_round bound in Figma — the round dot is a radius token, never an attribute (002 shape precedent). |
| `--ki-status-ring-color` | `#ffffff` | color used by the status component for the separating ring: Outline/inverse_black, the exact variable bound to Outline=True variants — white over light surfaces, black over dark ones, so the ring reads as a cutout from the underlying media (FR-002). |
| `--ki-status-ring-width` | `0.125rem` | dimension used by the status component for the separating ring: the 2px stroke on every Outline=True Status variant (FR-002). |
| `--ki-status-shadow` | `0 1px 1px -0.5px rgba(0, 0, 0, 0.03), inset 0px 3px 3px 0px rgba(255, 255, 255, 0.12)` | shadow used by the status component for the dot effects: the exact MarsUI pair on every Status variant — e1 drop (0/1/1/-0.5 in the scheme shadow color) plus inner White/12 highlight (0/3/3), i.e. Component_effect/primary_default's box-shadow layers. The effect's backdrop blur is deliberately NOT applied: a 4px dot has no glass surface to blur (brief §5 perf note, glass restraint). |
| `--ki-status-size` | `0.25rem` | dimension used by the status component for the dot: the MarsUI 4px dot, Space/xs bound to every Status variant (specs/021-ki-status/design-extraction.md). One size per theme, never an attribute (FR-006). |
| `--ki-status-success-color` | `#409b3f` | color used by the status component for the success tone fill: Surface/success_med_em, the exact variable bound to the Type=success Status variant (Success/500). |
| `--ki-status-warning-color` | `#f8ac3a` | color used by the status component for the warning tone fill: Surface/warning_med_em, the exact variable bound to the Type=warning Status variant (Warning/500). |

### `ki-switch`

| Token | Value (onmars light) | Description |
| --- | --- | --- |
| `--ki-switch-border-width` | `1px` | dimension used by the switch component for border width (MarsUI tracks are borderless; the 1px hairline in ki.outline.control is the declared WCAG 1.4.11 deviation at half the former weight, so the control stops reading as an outlined pill). |
| `--ki-switch-checked-active-border` | `rgba(255, 255, 255, 0)` | color used by the switch component for checked active border. |
| `--ki-switch-checked-active-thumb` | `#ffffff` | color used by the switch component for checked active thumb. |
| `--ki-switch-checked-active-track` | `#5f3993` | color used by the switch component for checked active track. |
| `--ki-switch-checked-disabled-border` | `#dddfe4` | color used by the switch component for checked disabled border. |
| `--ki-switch-checked-disabled-thumb` | `#ffffff` | color used by the switch component for checked disabled thumb (MarsUI pointer keeps its light construction in every state including disabled). |
| `--ki-switch-checked-disabled-track` | `#dddfe4` | color used by the switch component for checked disabled track (MarsUI disabled track keeps the OFF rest fill surface/disabled_med_em; the disabled signal lives in the glyph in the master). |
| `--ki-switch-checked-hover-border` | `rgba(255, 255, 255, 0)` | color used by the switch component for checked hover border. |
| `--ki-switch-checked-hover-thumb` | `#ffffff` | color used by the switch component for checked hover thumb. |
| `--ki-switch-checked-hover-track` | `#5f3993` | color used by the switch component for checked hover track. |
| `--ki-switch-checked-rest-border` | `rgba(255, 255, 255, 0)` | color used by the switch component for checked rest border. |
| `--ki-switch-checked-rest-thumb` | `#ffffff` | color used by the switch component for checked rest thumb. |
| `--ki-switch-checked-rest-track` | `#845abe` | color used by the switch component for checked rest track. |
| `--ki-switch-focus-ring-color` | `#845abe` | color used by the switch component for focus ring color. |
| `--ki-switch-focus-ring-offset` | `0.125rem` | dimension used by the switch component for focus ring offset. |
| `--ki-switch-focus-ring-shadow` | `0 0 0 3px rgba(132, 90, 190, 0.2)` | shadow used by the switch component for the focus ring glow (MarsUI Focus/primary: 3px spread ring at 20% — see ki.focus.primary. specs/002-ki-button/design-extraction.md §2.5 recorded 40%, having read the alpha off the Dark canvas mode, same double mechanism as ki-button: glow plus the opaque indicator). |
| `--ki-switch-focus-ring-width` | `0.125rem` | dimension used by the switch component for focus ring width. |
| `--ki-switch-gap` | `0.5rem` | dimension used by the switch component for gap (MarsUI sm label pairing: Space/md 8px). |
| `--ki-switch-glyph-dash-height` | `0.125rem` | dimension used by the switch component for the disabled dash glyph block size (Figma pointer exports: 8x2 rounded dash). |
| `--ki-switch-glyph-height` | `0.375rem` | dimension used by the switch component for the thumb state glyph block size (Figma pointer exports: 4px check ink height). |
| `--ki-switch-glyph-width` | `0.5rem` | dimension used by the switch component for the thumb state glyph inline size (Figma pointer exports: 8px check/dash ink box inside the thumb). |
| `--ki-switch-min-target` | `1.75rem` | dimension used by the switch component for min target (MarsUI component frame is 28px tall for sm/md — the designed interactive footprint). |
| `--ki-switch-thumb-inset` | `0.125rem` | dimension used by the switch component for thumb inset. |
| `--ki-switch-thumb-radius` | `1000px` | dimension used by the switch component for thumb radius (MarsUI pointer corner radius is half the height with corner smoothing; radius_round clamps to the smoothed pill). |
| `--ki-switch-thumb-size` | `1.125rem` | dimension used by the switch component for thumb size. |
| `--ki-switch-thumb-width` | `1.375rem` | dimension used by the switch component for thumb inline size (MarsUI md pointer is 22x18 — wider than tall; thumb-size remains the block size). |
| `--ki-switch-track-height` | `1.375rem` | dimension used by the switch component for track height. |
| `--ki-switch-track-radius` | `8px` | dimension used by the switch component for track radius. |
| `--ki-switch-track-width` | `2.5rem` | dimension used by the switch component for track width. |
| `--ki-switch-unchecked-active-border` | `rgba(0, 0, 0, 0.56)` | color used by the switch component for unchecked active border. |
| `--ki-switch-unchecked-active-thumb` | `#ffffff` | color used by the switch component for unchecked active thumb. |
| `--ki-switch-unchecked-active-track` | `#afb3bb` | color used by the switch component for unchecked active track (one step beyond hover; the master defines no pressed variant). |
| `--ki-switch-unchecked-disabled-border` | `#dddfe4` | color used by the switch component for unchecked disabled border. |
| `--ki-switch-unchecked-disabled-thumb` | `#ffffff` | color used by the switch component for unchecked disabled thumb (MarsUI pointer keeps its light construction in every state including disabled). |
| `--ki-switch-unchecked-disabled-track` | `#dddfe4` | color used by the switch component for unchecked disabled track (MarsUI disabled track keeps the OFF rest fill surface/disabled_med_em; the disabled signal lives in the glyph in the master). |
| `--ki-switch-unchecked-hover-border` | `rgba(0, 0, 0, 0.56)` | color used by the switch component for unchecked hover border. |
| `--ki-switch-unchecked-hover-thumb` | `#ffffff` | color used by the switch component for unchecked hover thumb. |
| `--ki-switch-unchecked-hover-track` | `#c3c6cc` | color used by the switch component for unchecked hover track (Kimen-invented one-step ramp over the corrected rest fill; the master defines no hover variant). |
| `--ki-switch-unchecked-rest-border` | `rgba(0, 0, 0, 0.48)` | color used by the switch component for unchecked rest border. |
| `--ki-switch-unchecked-rest-thumb` | `#ffffff` | color used by the switch component for unchecked rest thumb. |
| `--ki-switch-unchecked-rest-track` | `#dddfe4` | color used by the switch component for unchecked rest track (MarsUI OFF track = surface/disabled_med_em #dddfe4 light / White-18 dark — a solid soft fill, not an outlined pill). |

### `ki-tab`

| Token | Value (onmars light) | Description |
| --- | --- | --- |
| `--ki-tab-focus-ring-color` | `#845abe` | color used by the tab component for focus ring color. |
| `--ki-tab-focus-ring-offset` | `0.125rem` | dimension used by the tab component for focus ring offset. |
| `--ki-tab-focus-ring-shadow` | `0 0 0 3px rgba(132, 90, 190, 0.2)` | shadow used by the tab component for the focus ring glow (MarsUI Focus/primary: 3px spread ring at 20% — see ki.focus.primary. specs/002-ki-button/design-extraction.md §2.5 recorded 40%, having read the alpha off the Dark canvas mode, same double mechanism as ki-button: glow plus the opaque indicator). |
| `--ki-tab-focus-ring-width` | `0.125rem` | dimension used by the tab component for focus ring width. |
| `--ki-tab-font-size` | `0.8125rem` | dimension used by the tab component for font size. |
| `--ki-tab-font-weight` | `600` | font weight used by the tab component for font weight. |
| `--ki-tab-gap` | `0.25rem` | dimension used by the tab component for gap. |
| `--ki-tab-indicator-color` | `#000000` | color used by the tab component for indicator color (MarsUI active underline is Outline/inverse_white: black in light, white in dark — the inverse neutral, not primary). |
| `--ki-tab-indicator-radius` | `0px` | dimension used by the tab component for indicator radius (the master underline is a square-ended 2px bottom border). |
| `--ki-tab-indicator-size` | `0.125rem` | dimension used by the tab component for indicator size. |
| `--ki-tab-line-height` | `1.5rem` | dimension used by the tab component for line height. |
| `--ki-tab-min-block-size` | `2.5rem` | dimension used by the tab component for min block size (MarsUI Tab_nav md bar height 40px: 24px body_1 label + 8px block-end clearance + underline; retargeted from ki.space.7xl per the fidelity audit — material3 pins its own value). |
| `--ki-tab-padding-block-end` | `0.5rem` | dimension used by the tab component for padding block end (MarsUI Tab_nav_item: 8px inset keeping the 2px underline clear of the 13/24 label; formalizes the fallback ki-tab.css already consumes). |
| `--ki-tab-padding-inline` | `0rem` | dimension used by the tab component for padding inline (MarsUI items carry no inline padding: spacing comes from the bar gap and the underline spans the full item width). |
| `--ki-tab-radius` | `6px` | dimension used by the tab component for radius. |
| `--ki-tab-selected-active-bg` | `rgba(255, 255, 255, 0)` | color used by the tab component for selected active background (MarsUI Tab_nav is transparent_inverse in every state; background pills are Switcher language). |
| `--ki-tab-selected-active-fg` | `#0a0c11` | color used by the tab component for selected active foreground (MarsUI active tab label is neutral Text/high_em — never purple; master 10048:1240). |
| `--ki-tab-selected-disabled-bg` | `rgba(255, 255, 255, 0)` | color used by the tab component for selected disabled background (no disabled variant exists in Tab_nav; disabled must be quieter than rest, not a filled pill). |
| `--ki-tab-selected-disabled-fg` | `#afb3bb` | color used by the tab component for selected disabled foreground (MarsUI disabled ladder: Gray/500 light / White-32 dark, the value ki-status and ki-switch already use for disabled glyphs). |
| `--ki-tab-selected-hover-bg` | `rgba(255, 255, 255, 0)` | color used by the tab component for selected hover background (MarsUI Tab_nav is transparent_inverse in every state; background pills are Switcher language). |
| `--ki-tab-selected-hover-fg` | `#0a0c11` | color used by the tab component for selected hover foreground (MarsUI active tab label is neutral Text/high_em — never purple; master 10048:1240). |
| `--ki-tab-selected-rest-bg` | `rgba(255, 255, 255, 0)` | color used by the tab component for selected rest background (MarsUI Tab_nav is transparent_inverse in every state; background pills are Switcher language). |
| `--ki-tab-selected-rest-fg` | `#0a0c11` | color used by the tab component for selected rest foreground (MarsUI active tab label is neutral Text/high_em — never purple; master 10048:1240). |
| `--ki-tab-unselected-active-bg` | `rgba(255, 255, 255, 0)` | color used by the tab component for unselected active background (MarsUI Tab_nav is transparent_inverse in every state). |
| `--ki-tab-unselected-active-fg` | `#0a0c11` | color used by the tab component for unselected active foreground (mirrors hover; MarsUI defines no pressed variant). |
| `--ki-tab-unselected-disabled-bg` | `rgba(255, 255, 255, 0)` | color used by the tab component for unselected disabled background (no disabled variant exists in Tab_nav; disabled must be quieter than rest, not a filled pill). |
| `--ki-tab-unselected-disabled-fg` | `#afb3bb` | color used by the tab component for unselected disabled foreground (MarsUI disabled ladder: Gray/500 light / White-32 dark, the value ki-status and ki-switch already use for disabled glyphs). |
| `--ki-tab-unselected-hover-bg` | `rgba(255, 255, 255, 0)` | color used by the tab component for unselected hover background (MarsUI Tab_nav is transparent_inverse in every state). |
| `--ki-tab-unselected-hover-fg` | `#0a0c11` | color used by the tab component for unselected hover foreground (with rest at med_em, hover darkens one em step, preserving the master rest-to-hover direction). |
| `--ki-tab-unselected-rest-bg` | `rgba(255, 255, 255, 0)` | color used by the tab component for unselected rest background (MarsUI Tab_nav is transparent_inverse in every state). |
| `--ki-tab-unselected-rest-fg` | `#5b616d` | color used by the tab component for unselected rest foreground (Figma inactive is Text/low_em, which fails the 4.5:1 text gate at 13px; med_em is the gate-compliant rest value — declared deviation). |

### `ki-tab-panel`

| Token | Value (onmars light) | Description |
| --- | --- | --- |
| `--ki-tab-panel-bg` | `rgba(255, 255, 255, 0)` | color used by the tab panel component for background (Tab_nav masters have no panel chrome: content sits on the ambient surface; the panel never paints a surface it does not own). |
| `--ki-tab-panel-fg` | `#0a0c11` | color used by the tab panel component for foreground (pairs a scheme-correct text color with whatever ground the transparent panel sits on — fixes black-on-black slotted text in dark). |
| `--ki-tab-panel-focus-ring-color` | `#845abe` | color used by the tab panel component for focus ring color. |
| `--ki-tab-panel-focus-ring-offset` | `0.125rem` | dimension used by the tab panel component for focus ring offset. |
| `--ki-tab-panel-focus-ring-width` | `0.125rem` | dimension used by the tab panel component for focus ring width. |
| `--ki-tab-panel-padding-block` | `0.625rem` | dimension used by the tab panel component for padding block. |
| `--ki-tab-panel-padding-inline` | `0.625rem` | dimension used by the tab panel component for padding inline. |

### `ki-tabs`

| Token | Value (onmars light) | Description |
| --- | --- | --- |
| `--ki-tabs-divider-color` | `rgba(0, 0, 0, 0.05)` | color used by the tabs component for divider color. |
| `--ki-tabs-divider-width` | `0rem` | dimension used by the tabs component for the tablist divider width (the Tab_nav bar has no stroke; the hook stays for consumers to opt back in — material3 keeps its hairline via its override). |
| `--ki-tabs-gap` | `1.625rem` | dimension used by the tabs component for gap (MarsUI md bar item gap Space/8xl 26px; the gap carries all spacing now that items have zero inline padding). |

### `ki-textarea`

| Token | Value (onmars light) | Description |
| --- | --- | --- |
| `--ki-textarea-border-block-end-width` | `1px` | dimension used by the textarea component for border block end width. |
| `--ki-textarea-border-block-start-width` | `1px` | dimension used by the textarea component for border block start width. |
| `--ki-textarea-border-inline-end-width` | `1px` | dimension used by the textarea component for border inline end width. |
| `--ki-textarea-border-inline-start-width` | `1px` | dimension used by the textarea component for border inline start width. |
| `--ki-textarea-border-width` | `1px` | dimension used by the textarea component for border width (MarsUI 1px hairline; the border color stays ki.outline.control per the declared a11y deviation). |
| `--ki-textarea-disabled-bg` | `#f2f2f4` | color used by the textarea component for disabled background (MarsUI Surface/disabled_base_em Gray/100; aligned with ki-input). |
| `--ki-textarea-disabled-border` | `rgba(0, 0, 0, 0.08)` | color used by the textarea component for disabled border. |
| `--ki-textarea-disabled-fg` | `#c3c6cc` | color used by the textarea component for disabled foreground. |
| `--ki-textarea-disabled-hint-fg` | `#c3c6cc` | color used by the textarea component for the disabled hint row foreground (MarsUI disabled ladder Text/base_em; disabled cells are exempt from the contrast sweep). |
| `--ki-textarea-disabled-label-fg` | `#c3c6cc` | color used by the textarea component for disabled label foreground. |
| `--ki-textarea-disabled-placeholder-fg` | `#c3c6cc` | color used by the textarea component for disabled placeholder foreground. |
| `--ki-textarea-focus-bg` | `#ffffff` | color used by the textarea component for focus background (MarsUI active fill switches to white / dark s2 — modeled with the scheme ladder s0). |
| `--ki-textarea-focus-border` | `#845abe` | color used by the textarea component for focus border. |
| `--ki-textarea-focus-fg` | `#0a0c11` | color used by the textarea component for focus foreground. |
| `--ki-textarea-focus-label-fg` | `#5b616d` | color used by the textarea component for focus label foreground (MarsUI label is Text/med_em in every non-disabled state; matches ki-input). |
| `--ki-textarea-focus-placeholder-fg` | `#8c929c` | color used by the textarea component for focus placeholder foreground (MarsUI placeholder Text/low_em; placeholder is not the accessible name, so it sits outside the 4.5:1 text sweep). |
| `--ki-textarea-focus-ring-color` | `#845abe` | color used by the textarea component for focus ring color. |
| `--ki-textarea-focus-ring-offset` | `0.125rem` | dimension used by the textarea component for focus ring offset. |
| `--ki-textarea-focus-ring-shadow` | `0 0 0 3px rgba(132, 90, 190, 0.2)` | shadow used by the textarea component for the focus ring glow (MarsUI Focus/primary: 3px spread ring at 20% — see ki.focus.primary. specs/002-ki-button/design-extraction.md §2.5 recorded 40%, having read the alpha off the Dark canvas mode, same double mechanism as ki-button: glow plus the opaque indicator). |
| `--ki-textarea-focus-ring-width` | `0.125rem` | dimension used by the textarea component for focus ring width. |
| `--ki-textarea-font-size` | `0.8125rem` | dimension used by the textarea component for font size. |
| `--ki-textarea-font-weight` | `500` | font weight used by the textarea component for font weight. |
| `--ki-textarea-hint-fg` | `#5b616d` | color used by the textarea component for the hint row foreground (MarsUI Hint_label Text/med_em; the hint row itself is pending its founder-gated API addition). |
| `--ki-textarea-hover-bg` | `#ececf0` | color used by the textarea component for hover background (MarsUI hover = s2 + Black/3 overlay; s3 is the nearest token). |
| `--ki-textarea-hover-border` | `rgba(0, 0, 0, 0.56)` | color used by the textarea component for hover border. |
| `--ki-textarea-hover-fg` | `#0a0c11` | color used by the textarea component for hover foreground. |
| `--ki-textarea-hover-label-fg` | `#5b616d` | color used by the textarea component for hover label foreground (MarsUI label is Text/med_em in every non-disabled state; matches ki-input). |
| `--ki-textarea-hover-placeholder-fg` | `#8c929c` | color used by the textarea component for hover placeholder foreground (MarsUI placeholder Text/low_em; placeholder is not the accessible name, so it sits outside the 4.5:1 text sweep). |
| `--ki-textarea-invalid-bg` | `#ffffff` | color used by the textarea component for invalid background (MarsUI danger keeps the white active fill — no tinted slab; ring and hint carry the signal). |
| `--ki-textarea-invalid-border` | `#f05149` | color used by the textarea component for invalid border. |
| `--ki-textarea-invalid-fg` | `#0a0c11` | color used by the textarea component for invalid foreground. |
| `--ki-textarea-invalid-hint-fg` | `#cd2118` | color used by the textarea component for the invalid hint row foreground (MarsUI Hint_label danger: Text/danger_high_em carries the signal). |
| `--ki-textarea-invalid-label-fg` | `#5b616d` | color used by the textarea component for invalid label foreground (MarsUI label is Text/med_em in every non-disabled state; matches ki-input). |
| `--ki-textarea-invalid-placeholder-fg` | `#8c929c` | color used by the textarea component for invalid placeholder foreground (MarsUI placeholder Text/low_em; placeholder is not the accessible name, so it sits outside the 4.5:1 text sweep). |
| `--ki-textarea-invalid-ring` | `0 0 0 3px rgba(240, 81, 73, 0.2)` | shadow used by the textarea component for the persistent invalid ring (MarsUI Focus/danger: 3px danger-500 alpha spread; applied as box-shadow while :state(user-invalid)). |
| `--ki-textarea-label-font-size` | `0.8125rem` | dimension used by the textarea component for label font size (MarsUI UI/Para/medium: body_1 13px). |
| `--ki-textarea-label-font-weight` | `600` | font weight used by the textarea component for label font weight. |
| `--ki-textarea-label-gap` | `0.25rem` | dimension used by the textarea component for label gap. |
| `--ki-textarea-label-line-height` | `1.25rem` | dimension used by the textarea component for label line height (MarsUI line_height/para 20px). |
| `--ki-textarea-label-padding-inline` | `0.125rem` | dimension used by the textarea component for label padding inline (MarsUI Input_label row 2px inset aligning the label ink with the cell radius; matches ki-input). |
| `--ki-textarea-line-height` | `1.5rem` | dimension used by the textarea component for line height. |
| `--ki-textarea-min-target` | `1.5rem` | dimension used by the textarea component for min target. |
| `--ki-textarea-padding-block` | `0.75rem` | dimension used by the textarea component for padding block (MarsUI 96px md textarea: 12px ink inset). |
| `--ki-textarea-padding-inline` | `0.75rem` | dimension used by the textarea component for padding inline (MarsUI 96px md textarea: 12px ink inset). |
| `--ki-textarea-radius` | `10px` | dimension used by the textarea component for radius (MarsUI Radius/component/radius_md 10px for the md size; component ramp so personality modes reach it). |
| `--ki-textarea-readonly-bg` | `#ffffff` | color used by the textarea component for readonly background. |
| `--ki-textarea-readonly-border` | `rgba(0, 0, 0, 0.48)` | color used by the textarea component for readonly border. |
| `--ki-textarea-readonly-fg` | `#0a0c11` | color used by the textarea component for readonly foreground. |
| `--ki-textarea-readonly-label-fg` | `#5b616d` | color used by the textarea component for readonly label foreground (MarsUI label is Text/med_em in every non-disabled state; matches ki-input). |
| `--ki-textarea-readonly-placeholder-fg` | `#8c929c` | color used by the textarea component for readonly placeholder foreground (MarsUI placeholder Text/low_em; placeholder is not the accessible name, so it sits outside the 4.5:1 text sweep). |
| `--ki-textarea-rest-bg` | `#f2f2f4` | color used by the textarea component for rest background (MarsUI textarea fill Surface/s2). |
| `--ki-textarea-rest-border` | `rgba(0, 0, 0, 0.48)` | color used by the textarea component for rest border. |
| `--ki-textarea-rest-fg` | `#0a0c11` | color used by the textarea component for rest foreground. |
| `--ki-textarea-rest-label-fg` | `#5b616d` | color used by the textarea component for rest label foreground (MarsUI label is Text/med_em in every non-disabled state; matches ki-input). |
| `--ki-textarea-rest-placeholder-fg` | `#8c929c` | color used by the textarea component for rest placeholder foreground (MarsUI placeholder Text/low_em; placeholder is not the accessible name, so it sits outside the 4.5:1 text sweep). |

### `ki-tooltip`

| Token | Value (onmars light) | Description |
| --- | --- | --- |
| `--ki-tooltip-backdrop-blur` | `12px` | backdrop blur used by the tooltip component glass card (MarsUI BACKGROUND_BLUR 24 = CSS 12px; material3 resolves to blur none via the semantic override). |
| `--ki-tooltip-bg` | `#ffffff` | color used by the tooltip component for background (flat fallback of the MarsUI glass card gradient; the v1 inverse bubble predates the master — specs/013 correction block. material3 keeps the inverse bubble via its override). |
| `--ki-tooltip-bg-end` | `#ffffff` | color used by the tooltip component for the glass gradient end (MarsUI Surface/special/light-s0_dark-s2). |
| `--ki-tooltip-bg-start` | `rgba(255, 255, 255, 0.8)` | color used by the tooltip component for the glass gradient start (MarsUI Surface/special/blur_base_light White/80; dark Dark/700 blur base). Paint background: linear-gradient(180deg, var(--ki-tooltip-bg-start), var(--ki-tooltip-bg-end)). |
| `--ki-tooltip-border-width` | `1px` | dimension used by the tooltip component for the bevel outline width (MarsUI glass card 1px Outline/secondary_button_top stroke; replaces the 1px literal ki-tooltip.css shipped while the token was pending). |
| `--ki-tooltip-fg` | `#0a0c11` | color used by the tooltip component for foreground (MarsUI title row Text/high_em; lands together with the glass background — white-on-glass would break contrast). |
| `--ki-tooltip-font-size` | `0.8125rem` | dimension used by the tooltip component for font size (MarsUI Heading_top style UI/Para/medium: body_1 13px). |
| `--ki-tooltip-font-weight` | `600` | font weight used by the tooltip component for font weight. |
| `--ki-tooltip-hide-delay` | `100ms` | duration used by the tooltip component for tooltip hide delay. |
| `--ki-tooltip-line-height` | `1.25rem` | dimension used by the tooltip component for line height (MarsUI line_height/para 20px). |
| `--ki-tooltip-max-inline-size` | `18rem` | dimension used by the tooltip component for max inline size (288px; the master card is fixed 280px wide in its rich variants — the 8px divergence is a documented text-only reduction). |
| `--ki-tooltip-offset` | `0.5rem` | dimension used by the tooltip component for offset (MarsUI caret depth 8px with zero extra gap = trigger-to-card distance). |
| `--ki-tooltip-padding-block` | `0.75rem` | dimension used by the tooltip component for padding block (MarsUI card padding Space/xl 12px on all sides). |
| `--ki-tooltip-padding-inline` | `0.75rem` | dimension used by the tooltip component for padding inline (MarsUI card padding Space/xl 12px on all sides). |
| `--ki-tooltip-radius` | `16px` | dimension used by the tooltip component for radius (MarsUI Tooltip master 12089:7621 List_wrap: Radius/big_component/radius_xs 16px). |
| `--ki-tooltip-shadow` | `0 1px 1px -0.5px rgba(0, 0, 0, 0.03), 0 3px 3px -1.5px rgba(0, 0, 0, 0.03), 0 20px 20px -12px rgba(0, 0, 0, 0.03), 0 32px 32px -16px rgba(0, 0, 0, 0.03)` | shadow used by the tooltip component for shadow (MarsUI Elevation/e4 on the master wrap). |
| `--ki-tooltip-show-delay` | `500ms` | duration used by the tooltip component for tooltip show delay. |

### `ki-video`

| Token | Value (onmars light) | Description |
| --- | --- | --- |
| `--ki-video-backdrop-blur` | `12px` | dimension used by the video component for the glass backdrop of the scrim and the play container (MarsUI Blur/24, identical to the button glass). material3 resolves to blur none via the semantic override. |
| `--ki-video-focus-ring-color` | `#845abe` | color used by the video component for the play control focus ring: the keyboard-reachable facade control (FR-004, S6) adopts the ki-dialog/ki-icon-button focus-ring pair verbatim. |
| `--ki-video-focus-ring-offset` | `0.125rem` | dimension used by the video component for the play control focus ring offset (ki-dialog precedent). |
| `--ki-video-focus-ring-width` | `0.125rem` | dimension used by the video component for the play control focus ring width (ki-dialog precedent). |
| `--ki-video-frame-radius` | `16px` | dimension used by the video component for the frame corners: Radius/big_component/radius_xs, the exact variable bound to the Size=md Video frame (node 12089:6570, specs/025-ki-video/design-extraction.md) — the sm frame's radius_md is the scale demo, not a second API value. |
| `--ki-video-motion-duration` | `120ms` | duration used by the video component for the facade dismissal fade: the Figma set documents no motion, so onmars resolves the single transition to the fast semantic duration (FR-013; disabled under reduced motion by construction, 024 precedent). |
| `--ki-video-play-bg` | `#000000` | color used by the video component for the play container surface: Surface/inverse_white, the exact variable bound to the Icon_button fill (black over light, white over dark) — the control carries its own surface so glyph contrast never depends on the poster imagery (FR-008). |
| `--ki-video-play-border-color` | `rgba(0, 0, 0, 0.08)` | color used by the video component for the play container bevel: Outline/primary_button_top, the exact variable bound to the Icon_button stroke — the verified ki-button glass bevel. |
| `--ki-video-play-border-width` | `1px` | dimension used by the video component for the play container bevel width (MarsUI hairline stroke, ki-icon-button precedent). |
| `--ki-video-play-fg` | `#ffffff` | color used by the video component for the play glyph: Text/inverse_black, the counterpart of the inverse container surface (white over light, black over dark) — the glyph inherits it via currentColor; the -bg/-fg pair is swept by the contrast gate (FR-008). |
| `--ki-video-play-icon-size` | `1.75rem` | dimension used by the video component for the play glyph: the 28px Icon/Placeholder_xl slot measured inside the Size=md play container (the ki-icon-button xl icon size). |
| `--ki-video-play-radius` | `14px` | dimension used by the video component for the play container shape: Radius/component/radius_xl, the exact variable bound to the Size=md Icon_button instance (14px, the ki-icon-button xl radius). |
| `--ki-video-play-shadow` | `0 1px 1px -0.5px rgba(0, 0, 0, 0.03), 0 3px 3px -1.5px rgba(0, 0, 0, 0.03), 0 20px 20px -12px rgba(0, 0, 0, 0.03), 0 32px 32px -16px rgba(0, 0, 0, 0.03), 0 56px 56px -28px rgba(0, 0, 0, 0.03), 0 72px 72px -36px rgba(0, 0, 0, 0.03), inset 0px 3px 3px 0px rgba(255, 255, 255, 0.12)` | shadow used by the video component for the play control (MarsUI stacks Elevation/e6 on the button_wrap beneath the Component_effect/primary_default inner White/12 highlight — the measured master stack). |
| `--ki-video-play-size` | `3.5rem` | dimension used by the video component for the play container box: the 56px Icon_button instance of the Size=md Video (the ki-icon-button xl box; the sm variant's 40px box is the scale demo). |
| `--ki-video-scrim-color` | `rgba(0, 0, 0, 0.05)` | color used by the video component for the overlay scrim behind the play control: Inverse_white/alpha_6, the exact variable bound to the Play halo on both Video variants (Black/5 light, White/5 dark) — the scheme flip rides the semantic layer. |
| `--ki-video-scrim-padding` | `1rem` | dimension used by the video component for the scrim clearance around the play container: Space/3xl, the exact variable bound as the Size=md Play halo padding (16px on every side). |
| `--ki-video-scrim-radius` | `28px` | dimension used by the video component for the overlay scrim shape: Radius/component/radius_6xl, the exact variable bound to the Size=md Play halo (28px pill-cornered square). |
