/**
 * @kimen/angular (spec 034): generated Angular bindings for the Kimen
 * `ki-*` web components — standalone typed components with outputs for
 * `ki-*` events, plus ControlValueAccessor directives wiring the form
 * components to template-driven and reactive forms over their
 * re-dispatched native `input`/`change` events. Client-side rendering
 * only. Everything under `./directives` is generated from the component
 * contract and never hand-edited (constitution Art. I); this entry point
 * is the one hand-written shell.
 */
export * from './directives/proxies';
export { DIRECTIVES } from './directives/index';
export { BooleanValueAccessor } from './directives/boolean-value-accessor';
export { SelectValueAccessor } from './directives/select-value-accessor';
export { TextValueAccessor } from './directives/text-value-accessor';
