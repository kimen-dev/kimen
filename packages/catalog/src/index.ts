/**
 * Kimen neutral catalog: placeholder entry point.
 *
 * The real catalog (Zod schemas, guardrail boundary, renderer) is Fase 3 work
 * and enters through the Spec Kit flow with its own approved spec (Art. II).
 * This file only anchors the package so the deterministic gates have a target.
 */
export const CATALOG_SCHEMA_VERSION = '0.0.0';
