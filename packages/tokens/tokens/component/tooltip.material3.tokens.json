{
  "ki": {
    "tooltip": {
      "radius": {
        "$value": "{ki.radius.component.xs}",
        "$description": "M3 plain tooltip container shape: md.sys.shape.corner.extra-small (4dp) via the material3 radius scale."
      },
      "padding-inline": {
        "$value": "{ki.space.md}",
        "$description": "Material 3 dimension used by the tooltip component for padding inline."
      },
      "padding-block": {
        "$value": "{ki.space.xs}",
        "$description": "Material 3 dimension used by the tooltip component for padding block."
      },
      "max-inline-size": {
        "$value": "{ki.size.surface.xxs}",
        "$description": "Material 3 dimension used by the tooltip component for max inline size (surface size scale; to be refined in Fase M3)."
      },
      "offset": {
        "$value": "{ki.space.xs}",
        "$description": "Material 3 dimension used by the tooltip component for offset."
      },
      "bg": {
        "$value": "{ki.surface.inverse-white}",
        "$description": "Material 3 color used by the tooltip component for background."
      },
      "fg": {
        "$value": "{ki.text.inverse-black}",
        "$description": "Material 3 color used by the tooltip component for foreground."
      },
      "font-size": {
        "$value": "{ki.font.size.caption-2}",
        "$description": "Material 3 dimension used by the tooltip component for font size."
      },
      "font-weight": {
        "$value": "{ki.font.weight.body.medium}",
        "$description": "Material 3 font weight used by the tooltip component for font weight."
      },
      "line-height": {
        "$value": "{ki.font.line-height.caption-2}",
        "$description": "Material 3 dimension used by the tooltip component for line height."
      },
      "shadow": {
        "$value": {
          "color": "{ki.color.transparent.black}",
          "offsetX": 0,
          "offsetY": 0,
          "blur": 0,
          "spread": 0
        },
        "$description": "M3 plain tooltip is flat: md.comp.plain-tooltip container elevation level0, so the shadow is transparent (replaces the inherited card elevation).",
        "$type": "shadow"
      },
      "show-delay": {
        "$type": "duration",
        "$value": "{ki.foundation.migrated.component-tooltip.tooltip.show-delay}",
        "$description": "Material 3 duration used by the tooltip component for tooltip show delay."
      },
      "hide-delay": {
        "$type": "duration",
        "$value": "{ki.foundation.migrated.component-tooltip.tooltip.hide-delay}",
        "$description": "Material 3 duration used by the tooltip component for tooltip hide delay."
      }
    }
  }
}
