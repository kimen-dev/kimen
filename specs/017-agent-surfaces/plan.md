# Implementation Plan: Agent surfaces

<!-- KIMEN OVERRIDE of plan-template. Resolved with priority 1 by resolve_template().
     Constitutional basis: .specify/memory/constitution.md Articles I-XI (Constitution
     Check below enumerates all of them) and Art. III's definition of done. -->

**Branch**: `feat/agent-surfaces` | **Date**: 2026-07-08 | **Spec**: [spec.md](./spec.md)

**Input**: Feature specification from `/specs/017-agent-surfaces/spec.md`
(status: Draft, in the Fase-2 gate-1 batch on `feat/fase2-specs`; the
pre-implement gate blocks `/speckit-implement` until the founder records the
`.approved` marker — this plan exists so gate 1 can review spec + plan together).

## Summary

Complete the Art. I machine-surfaces pipeline beyond Stencil's docs-json:
derive the community-standard Custom Elements Manifest
(`custom-elements.json`) and an `llms.txt` language-model summary from the
component contract, carry when-to-use / when-NOT-to-use guidance as dedicated
JSDoc tags (`@whenToUse` / `@whenNotToUse`), enforce documentation
completeness at build time, and verify all committed surfaces with a new
deterministic `surfaces-sync` gate (tokens-sync precedent). Technical
approach: zero new dependencies — a small ESM library
(`packages/elements/scripts/agent-surfaces.mjs`) maps the already-generated
`generated/docs.json` (normalized per FR-008: no timestamp, no absolute
paths) into both surfaces as a pure JSON→text transformation, chained into
the existing `@kimen/elements` build target so every future component (003 to
016) inherits the surfaces with zero wiring (FR-007). Details and rationales
in [research.md](./research.md); emitted formats in
[contracts/agent-surfaces-contract.md](./contracts/agent-surfaces-contract.md).

## Technical Context

**Language/Version**: Node ≥ 22 ESM (`.mjs`) for the generator scripts (the
`packages/tokens/build.mjs` precedent); TypeScript strict for the test files.

**Primary Dependencies**: none — zero new runtime AND zero new build-time
dependencies. The mapping is docs.json → JSON/text with `node:fs`,
`node:path` only (pnpm supply-chain policy: `minimumReleaseAge` 7d,
`trustPolicy: no-downgrade`; the cheapest dependency is the one not added).

**Storage**: N/A (committed generated files are the artifact).

**Testing**: Vitest 4, node environment, via a new
`packages/elements/vitest.node.config.ts` project chained into the existing
`test` target. Test files are `*.spec.ts` under `packages/elements/scripts/`
(the traceability gate only counts `*.spec.ts|*.spec.tsx|*.e2e.ts` under
`packages/`), marker `// @spec:017-agent-surfaces`, S-IDs in test titles.
No browser suite: no rendered surface exists (research.md D7).

**Target Platform**: build environment only (Node ≥ 22, per repo `engines`).
Generated artifacts target machine consumers (editors, agents, LLMs).

**Project Type**: infrastructure — build pipeline of `packages/elements`
plus one gate in `scripts/gates/gates-suite.sh`.

**Performance Goals**: surface generation adds < 1 s to the elements build
(single-pass JSON transform); no runtime cost, no size-limit entries (the
artifacts ship as package metadata, not as loadable code).

**Constraints**: byte-determinism (FR-006/S6: no timestamps, no absolute
paths, stable serialization, LF endings); all new files pass the existing
deterministic layer (biome format, ESLint, tsc, knip) without weakening any
rule; generated outputs live where biome/ESLint/knip already exclude
generated content (`packages/elements/generated/`) or are inert to them
(`llms.txt`); single writer on `feat/agent-surfaces`.

**Scale/Scope**: 6 approved-pending scenarios (S1–S6); one library module +
one CLI orchestrator + one preamble template; four committed artifacts
(normalized `docs.json`, `generated/custom-elements.json`,
`packages/elements/llms.txt`, root `llms.txt`); 14 downstream component
specs (003–016) inherit the T019 obligation this feature makes satisfiable
(FR-009).

## Constitution Check

- **Art. I — AI-First, one source of truth**: this feature IS the Art. I
  pipeline segment "docs-json → Custom Elements Manifest → llms.txt".
  Generated artifacts touched: `generated/docs.json` (normalized, FR-008),
  `generated/custom-elements.json` (new), `packages/elements/llms.txt` +
  root `llms.txt` (new) — all regenerated by `@kimen/elements:build` and
  committed. JSDoc completeness stops being aspiration: FR-004 makes an
  undocumented public member (or an empty guidance tag) a build failure,
  mechanically. The remaining Art. I gaps (Zod catalog → Fase 3, docs site →
  Fase 5, AGENTS.md generation) stay explicitly deferred per the spec's
  Assumptions. **PASS**
- **Art. II — Specs before code (NON-NEGOTIABLE)**: implements exactly
  `specs/017-agent-surfaces/feature.feature` S1–S6. The four UI scenario
  families are N/A-justified in the spec's coverage table (no interactive,
  rendered, form or themed surface exists). Spec is awaiting gate-1
  approval; no implementation task may start before the `.approved` marker
  (pre-implement gate enforces this). Nothing in this plan exceeds the
  approved scenarios — no docs site, no AGENTS.md generation, no catalog.
  **PASS (implementation gated on approval)**
- **Art. III — Test-first, deterministic gates (NON-NEGOTIABLE)**: RED
  first — vitest node specs against S1–S6 written and verified failing
  before any generator code (tasks enforce the order). Tests are
  deterministic: fixture docs.json inputs, tmpdirs, no network, no clock.
  Core-logic surface for the mutation gate: the generator library is pure
  functions; Stryker is still not wired into gates-suite.sh (factory gap
  declared since 001/002, tracked as a factory chore) — compensating
  control: exhaustive unit cases per branch of `normalizeDocs`,
  `validateDocs`, `buildManifest`, `buildLlmsTxt`. SC-001's agent eval is a
  scheduled eval, never merge-blocking (spec's own language); the
  deterministic oracle here is the sync gate + tests. Done =
  `gates-suite.sh` exit 0. **PASS (with the declared, inherited factory gap)**
- **Art. IV — Web standards & lightness**: no runtime code, no KB impact,
  new runtime dependency: "none" (and no build-time dependency either). The
  Custom Elements Manifest is the web-components community standard —
  standards-first applied to metadata. **PASS**
- **Art. V — Accessibility WCAG 2.2 AA + EN 301 549 (NON-NEGOTIABLE)**:
  **N/A** — no rendered surface, no interaction pattern; nothing to
  keyboard, focus or contrast (spec Constitutional Surface says the same).
- **Art. VI — Closed tokens, layered customization**: **N/A** — no token is
  introduced or consumed; the manifest *describes* CSS custom properties by
  name when component contracts document them (`styles` → `cssProperties`
  mapping is data, not styling).
- **Art. VII — Simplicity & anti-abstraction**: one library module + one
  thin CLI, plain functions over plain JSON; no plugin architecture, no
  schema-validation library, no generator framework, no speculative
  multi-format support (llms-full.txt, JSON-LD etc. are not built).
  Guidance tags are two flat strings, not a DSL. **PASS**
- **Art. VIII — Neutral catalog, disposable adapters**: no protocol or
  adapter surface touched; no type leaks anywhere (the scripts are not part
  of any package's public API). The Fase-3 Zod catalog will *consume* the
  manifest this feature emits — boundary impact today: "none". **N/A**
- **Art. IX — Public API stability**: zero runtime API delta. Adds
  published artifacts (`generated/custom-elements.json` + `llms.txt` in the
  packed files, and the standard `customElements` package.json field) that
  version alongside the package from first release. Packaging correctness
  stays validated by the existing publint/attw gate. **PASS**
- **Art. X — Deterministic static analysis (NON-NEGOTIABLE)**: this feature
  exists because Art. X demanded a rule where review was being trusted: the
  new `surfaces-sync` gate (git diff over the four committed artifacts after
  a fresh build) plus the FR-004 completeness check (build failure naming
  the member) convert "keep docs in sync" from reviewer duty into gate
  verdicts. The new scripts/tests are themselves covered by the existing
  layer: biome format, ESLint (relax-block pattern extended to
  `packages/*/scripts/**`, same policy as root `scripts/**`), tsc (new
  `packages/elements/scripts/tsconfig.json` wired into the root typecheck
  script, browser-tests precedent) and knip (entries resolve through
  package.json scripts and the new vitest config). **PASS — the gate is the
  point**
- **Art. XI — Operational security of agents (NON-NEGOTIABLE)**: implement
  phase runs in the Art. XI sandbox (`sandbox/loop.sh`, credential-free,
  egress allowlist) or as a supervised local session without permission
  bypass; the generators read/write only workspace files; no new credential
  surface, no network at build time. **PASS**

**Definition of done (Art. III)**: done is exclusively the deterministic gates
exiting 0 (`bash scripts/gates/gates-suite.sh`: constitution, traceability,
lint, typecheck, build, tests, and per-surface gates — now including
`surfaces-sync`). Never self-assessed, by agent or human.

### Constitutional Surface (echo from spec.md)

- **Public API delta** (Art. IX): no runtime API change. Adds two published
  artifacts (`custom-elements.json`, `llms.txt`) that become versioned
  alongside the public API from their first release.
- **Bundle budget** (Art. IV): zero runtime impact — build-time artifacts
  only; no new runtime dependency. Build-time dependencies require the usual
  supply-chain review.
- **Accessibility** (Art. V): not applicable — no rendered surface.
- **Tokens** (Art. VI): none introduced; styling tokens are *described* in
  the manifest by name.
- **Catalog/agent legibility** (Art. I): this feature IS the agent-legibility
  backbone; the Fase 3 Zod catalog will consume the manifest, not replace it.
- **Guardrail/security boundary** (Art. VIII): untouched — no spec
  rendering, no actions, no adapter surface.

Every echoed obligation maps to a plan element: API delta →
contracts/agent-surfaces-contract.md §Publication surface; budget → zero
new dependencies (Technical Context; supply-chain review trivially satisfied
by adding none); accessibility → Art. V N/A line; tokens → Art. VI N/A line
+ `styles → cssProperties` mapping in data-model.md; agent legibility →
the whole plan (D1–D8); guardrail → none.

## Project Structure

### Documentation (this feature)

```text
specs/017-agent-surfaces/
├── spec.md              # gate-1 batch (approval pending)
├── feature.feature      # extracted Gherkin contract, S1–S6
├── plan.md              # this file
├── research.md          # Phase 0: decisions D1–D8
├── data-model.md        # Phase 1: docs.json → CEM / llms.txt field mapping
├── quickstart.md        # Phase 1: validation guide
├── contracts/
│   └── agent-surfaces-contract.md   # emitted formats, script names, gate name
└── tasks.md             # /speckit-tasks output
```

### Source Code (repository root)

```text
packages/elements/
├── scripts/                          # NEW — build-time only, never shipped as code
│   ├── agent-surfaces.mjs            # library: normalizeDocs, validateDocs,
│   │                                 #   buildManifest, buildLlmsTxt (pure functions)
│   ├── build-surfaces.mjs            # thin CLI orchestrator (all file I/O), called by build
│   ├── llms-preamble.txt             # committed preamble template (FR-002 source #2)
│   ├── agent-surfaces.spec.ts        # vitest node suite; // @spec:017-agent-surfaces
│   ├── fixtures/                     # minimal docs.json fixtures (undocumented prop,
│   │                                 #   two absolute-path roots, missing guidance)
│   └── tsconfig.json                 # typecheck project (browser-tests precedent)
├── vitest.node.config.ts             # NEW — node-env vitest project for scripts/
├── generated/
│   ├── docs.json                     # NORMALIZED in place (FR-008): no timestamp,
│   │                                 #   no absolute paths; still Storybook's source
│   └── custom-elements.json          # NEW — the CEM (biome/eslint/knip already
│                                     #   exclude generated/; discovery via package.json)
├── llms.txt                          # NEW — published copy (in "files")
├── src/components/ki-button/ki-button.tsx  # prose guidance → @whenToUse/@whenNotToUse
└── package.json                      # build/test script chains, customElements field,
                                      #   files += generated/custom-elements.json, llms.txt,
                                      #   nx build inputs/outputs for the new artifacts

llms.txt                              # NEW — repo-root copy, byte-identical (spec assumption)
scripts/gates/gates-suite.sh          # NEW gate: surfaces-sync (after tokens-sync)
eslint.config.mjs                     # relax-block files += 'packages/*/scripts/**'
package.json                          # typecheck += tsc -p packages/elements/scripts
tools/kimen-plugin/src/generators/component/files/__name__.tsx.template
                                      # scaffold @whenToUse/@whenNotToUse TODO(spec) tags (FR-007)
.specify/templates/overrides/tasks-template.md   # T019 line renamed to the real pipeline (FR-009)
```

**Structure Decision**: generator scripts live inside `packages/elements`
(the package whose build they extend — `packages/tokens/scripts/` precedent),
not in root `scripts/` (which holds repo-wide gates) nor `tools/` (Nx
generators). All generated JSON stays under `packages/elements/generated/`
so the existing biome/ESLint/knip exclusions apply with zero config churn;
`llms.txt` is text that no formatter or linter touches.

## Complexity Tracking

No constitutional violations to justify. The one declared gap (mutation gate
not wired into gates-suite.sh) predates this feature, is inherited from
001/002, and belongs to a factory chore, not to this plan.
