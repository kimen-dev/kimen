{
  "name": "@kimen/nx-plugin",
  "version": "0.0.0",
  "private": true,
  "license": "Apache-2.0",
  "description": "Kimen deterministic scaffolding (constitution Art. X): component, feature-spec and adapter generators. Components are created from generators, never by hand, so structure and gate wiring are reproducible.",
  "generators": "./generators.json",
  "dependencies": {
    "@nx/devkit": "23.0.1"
  }
}
