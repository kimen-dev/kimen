{
  "baselineSha256": "b5cf9427802783f74a2b3467a3f02bb3207a7064be984e086fa2dcbccbfb71c7",
  "baselineVersion": "0.0.0",
  "candidateSha256": "9595a7e24c729d4c2a78f1cb1081db9a32ee52df1a77ff4ddc8f516dbe2d818d",
  "packages": [
    "@kimen/elements",
    "@kimen/tokens"
  ],
  "reason": "Wave 1 host contract (same 0.0.0 baseline as 003/004/005; this declaration advances the sealed candidate, so the cumulative delta stays MAJOR). No public name is added, renamed or removed: the delta is entirely deliberate effective-value changes to already-published leaves, plus component CSS that now consumes tokens it already published. (1) Typography contract: 23 components gained a font-family declaration on :host resolving from --ki-typography-family-body (or their existing --ki-{avatar,badge,button}-font-family), and ki-icon-button/ki-video re-inherit it on their native <button>, whose user-agent `font` shorthand beat host inheritance. On a bare consumer page 24 of 29 components previously rendered in the user-agent serif; the visual harness masked this by setting font-family on the capture wrapper. ki-textarea stops consuming the deprecated --ki-typography-family-base alias in favour of --ki-typography-family-body. (2) ki-switch re-based on the MarsUI Toggle master (page Toggle 10102:4096, frame Toggle 10023:1125, size md), which the spec-008 design-source analysis wrongly recorded as absent: track 32->40px (ki.space.10xl -> 12xl), thumb 20->18px (5xl -> 4xl), inset 4->2px (xs -> xxs), track radius round -> ki.radius.md (8px), thumb radius round -> ki.radius.sm (6px), giving 14px of thumb travel where the shipped geometry gave 0. The thumb moves off the text role ki.text.high-em (a near-black dot) onto the surface role ki.surface.special.light-s0-dark-s4 the master paints it from, and the checked track moves from ki.surface.primary-high-em (brand 600) to ki.surface.primary-med-em (brand 500, the master's ON value). MarsUI's Toggle defines no hover or pressed state, so checked hover and active both resolve to brand 600; the shipped ramp already resolved both to the same value. (3) The material3 sheet inherited the onmars switch geometry, so re-basing onmars silently reshaped the second theme; material3 now pins its own track 52x32 fully rounded with a 24px handle and 4px inset per the Material 3 switch specification, and the two themes no longer share switch geometry. (4) --ki-focus-primary is repointed from ki.outline.primary-low-em-alpha (brand 500 at 40%) to ki.outline.primary-base-em-alpha (20%), the value the MarsUI Focus/primary effect style actually binds (#845abe33, read directly from the Toggle master's variable set). specs/002-ki-button/design-extraction.md §2.5 recorded the 40% it measured on the Dark canvas mode as the Light value; the ring shipped 2x too strong in light and 2.8x in dark across the twelve components that consume it. (5) New additive export @kimen/tokens/css/base: the opt-in page contract carrying color-scheme, a token-resolved page background, colour and body family. It publishes no tokens - it only consumes them - so the public-token surface is unchanged and the stylesheet is recorded as a page contract rather than a theme sheet.",
  "release": "major",
  "schemaVersion": 1
}
